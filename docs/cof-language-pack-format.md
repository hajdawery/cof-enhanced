# Moved: Language pack format

This page now lives at [`docs/design/language-pack-format.md`](design/language-pack-format.md).

This stub stays at the old path because files that are generated or hashed
still name it: the header line of every `languages/*/maps/*.entpatch` (pinned
in each pack's `MANIFEST.tsv`), the generator that writes that line, and a
comment in `patches/cof-language-packs.patch`. See the
[documentation index](README.md).
