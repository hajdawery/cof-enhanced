diff --git a/engine/client/cl_main.c b/engine/client/cl_main.c
index fd7eab3..5e7a06e 100644
--- a/engine/client/cl_main.c
+++ b/engine/client/cl_main.c
@@ -92,6 +92,7 @@
 CVAR_DEFINE_AUTO( ui_renderworld, "0", FCVAR_ARCHIVE, "render world when UI is visible" );
 static CVAR_DEFINE_AUTO( cof_ui_menu_map_redirect, "1", 0, "unified UI gate: turn a client- or server-issued \"map c_game_menu1\" into a return to the engine menu over the background map (0 = stock behaviour)" );
 static CVAR_DEFINE_AUTO( cof_ui_death_menu, "1", 0, "unified UI gate: open the engine menu's own death page when the game DLL sends the Cry of Fear GAME OVER screen (0 = stock behaviour, the client's own panel)" );
+static CVAR_DEFINE_AUTO( cof_ui_death_keep_running, "1", 0, "unified UI gate: keep the world simulating and audible behind the GAME OVER page, the way the original death screen did (0 = the page pauses the game like every other menu)" );
 static CVAR_DEFINE_AUTO( cof_mp3_stop_on_map, "1", 0, "run the client's \"stopmp3\" command before a fresh map, newgame, save load or disconnect, so the client's own MP3 player does not bleed across the transition (changelevel is deliberately untouched)" );
 static CVAR_DEFINE_AUTO( cof_sky_reset_per_map, "1", 0, "restore the client's \"gl_customsky\" on every map load when the client turned it off after a failed sky load, so one map's missing sky faces do not disable the sky for the rest of the session (0 = stock behaviour)" );
 // Milestone 4, in-game UI scaling. Archived, unlike the gate cvars above,
@@ -190,6 +191,11 @@
 	if( cl.background || cl.maxclients > 1 )
 		return true; // always active for multiplayer or background map
 
+	// Cry of Fear death page (cof_ui_death_keep_running): the one engine menu
+	// the world must keep running behind. See CL_CoF_DeathWorldLive.
+	if( CL_CoF_DeathWorldLive( ))
+		return true;
+
 	return ( cls.key_dest == key_game ); // active if not menu or console
 }
 
@@ -1672,6 +1678,39 @@
 qboolean CL_CoF_DeathMenuActive( void )
 {
 	return cof_death_menu_open && cof_ui_death_menu.value != 0.0f;
+}
+
+/*
+====================
+CL_CoF_DeathWorldLive
+
+Cry of Fear death page (cof_ui_death_keep_running, default on).
+
+The engine pauses a single player game whenever a menu owns input. There is no
+"pause" flag involved: CL_IsInGame() goes false as soon as key_dest leaves
+key_game, SV_IsSimulating() reads it and stops running the world frame, and the
+mixer reads the same key_dest and mutes every non-local channel
+(S_MixNormalChannelsToRoombuffer). The engine background track - which is where
+the client's own "playmp3" ends up, pfnMP3_InitStream -> S_StartBackgroundTrack
+- is paused by source in S_StreamBackgroundTrack for the same reason.
+
+That is right for the pause menu and wrong for GAME OVER. The original death
+screen is a client VGUI panel drawn over a still-running game: the ambience, the
+rain, a chainsaw that was already running, the death music - all of it keeps
+playing while the player reads the screen. Our page replaces that panel, so it
+has to reproduce it, and this is the single predicate the three gates ask.
+
+Input stays gated exactly as before: key_dest is still key_menu, so the client
+DLL gets no keys (cof_ui_input_gate), IN_EngineAppendMove returns early and the
+player's movement commands are zeroed in SV_ExecuteClientMessage, which asks
+this function too. The world runs, the player does not.
+
+Scoped to the death page alone; every other menu keeps the stock pause.
+====================
+*/
+qboolean CL_CoF_DeathWorldLive( void )
+{
+	return CL_CoF_DeathMenuActive() && cof_ui_death_keep_running.value != 0.0f;
 }
 
 /*
@@ -5391,6 +5430,7 @@
 	Cvar_RegisterVariable( &ui_renderworld );
 	Cvar_RegisterVariable( &cof_ui_menu_map_redirect );
 	Cvar_RegisterVariable( &cof_ui_death_menu );
+	Cvar_RegisterVariable( &cof_ui_death_keep_running );
 	Cvar_RegisterVariable( &cof_mp3_stop_on_map );
 	Cvar_RegisterVariable( &cof_sky_reset_per_map );
 	Cvar_RegisterVariable( &cof_ui_scale );
diff --git a/engine/common/common.h b/engine/common/common.h
index e650e9f..f6b631a 100644
--- a/engine/common/common.h
+++ b/engine/common/common.h
@@ -762,9 +762,14 @@
 // Cry of Fear music bleed (cof_mp3_stop_on_map): defined in cl_main.c, called
 // from the host state transitions in host_state.c
 void CL_CoF_StopMP3( const char *reason );
+// Cry of Fear death page (cof_ui_death_keep_running): true while the GAME OVER
+// page is up and the world is meant to keep running and sounding behind it.
+// Defined in cl_main.c; read by SV_IsSimulating and SV_ExecuteClientMessage.
+qboolean CL_CoF_DeathWorldLive( void );
 #else
 static inline qboolean CL_Initialized( void ) { return false; }
 static inline qboolean CL_IsInGame( void ) { return true; } // always true for dedicated
+static inline qboolean CL_CoF_DeathWorldLive( void ) { return false; } // no client, no death page
 static inline qboolean CL_IsInConsole( void ) { return false; }
 static inline qboolean CL_IsIntermission( void ) { return false; }
 static inline qboolean CL_DisableVisibility( void ) { return false; }
diff --git a/engine/client/sound/s_mix.c b/engine/client/sound/s_mix.c
index fe5ece4..4c09ee4 100644
--- a/engine/client/sound/s_mix.c
+++ b/engine/client/sound/s_mix.c
@@ -339,7 +339,11 @@
 			{
 				// play, playvol
 			}
-			else if(( cls.key_dest == key_menu || cl.paused ) && !FBitSet( ch->flags, FL_CHAN_LOCAL_SOUND ) && sp )
+			else if(( cls.key_dest == key_menu || cl.paused ) && !FBitSet( ch->flags, FL_CHAN_LOCAL_SOUND ) && sp
+				// Cry of Fear death page (cof_ui_death_keep_running): the world
+				// keeps running behind GAME OVER, so it has to keep sounding
+				// too - ambience, rain, a chainsaw that was already running.
+				&& !CL_CoF_DeathWorldLive( ))
 			{
 				// play only local sounds, keep pause for other
 				continue;
diff --git a/engine/client/sound/s_stream.c b/engine/client/sound/s_stream.c
index e74a4d2..1f5ab60 100644
--- a/engine/client/sound/s_stream.c
+++ b/engine/client/sound/s_stream.c
@@ -236,7 +236,10 @@
 	if( !cl.background )
 	{
 		// pause music by source type
-		if( s_bgTrack.source == key_game && cls.key_dest == key_menu ) return;
+		// Cry of Fear death page (cof_ui_death_keep_running): the client's own
+		// MP3 player runs through this stream (pfnMP3_InitStream), and the
+		// original GAME OVER screen kept it playing over the live scene.
+		if( s_bgTrack.source == key_game && cls.key_dest == key_menu && !CL_CoF_DeathWorldLive( )) return;
 		if( s_bgTrack.source == key_menu && cls.key_dest != key_menu ) return;
 	}
 	else if( cls.key_dest == key_console )
diff --git a/engine/server/sv_client.c b/engine/server/sv_client.c
index 508d5c7..e8cc519 100644
--- a/engine/server/sv_client.c
+++ b/engine/server/sv_client.c
@@ -3347,7 +3347,10 @@
 		return;
 
 	// check for pause or frozen
-	if( sv.paused || !CL_IsInGame() || SV_PlayerIsFrozen( player ))
+	// Cry of Fear death page (cof_ui_death_keep_running): the world keeps
+	// running behind GAME OVER but the player does not - CL_IsInGame() is
+	// deliberately true there, so the input gate has to be named explicitly.
+	if( sv.paused || !CL_IsInGame() || CL_CoF_DeathWorldLive() || SV_PlayerIsFrozen( player ))
 	{
 		for( i = 0; i < numcmds; i++ )
 		{
diff --git a/engine/server/sv_cmds.c b/engine/server/sv_cmds.c
index 6d186d6..a5bdabf 100644
--- a/engine/server/sv_cmds.c
+++ b/engine/server/sv_cmds.c
@@ -1027,6 +1027,29 @@
 
 /*
 ==================
+SV_CoFWorldProbe_f
+
+cfg-only test hook (Cry of Fear, milestone 4b). One line naming everything the
+"does the world keep running behind the GAME OVER page" question depends on:
+the server clock, which only advances while SV_IsSimulating() is true, the
+number of world frames run since the map started, and the flags that gate
+them. Two of these, some hundreds of frames apart, are
+the whole proof - no screenshot can show a clock.
+
+sv.framecount is the physics frame counter, which SV_RunGameFrame only ever
+increments while sv.simulating is true.
+==================
+*/
+static void SV_CoFWorldProbe_f( void )
+{
+	Con_Printf( "[cof-world] %s sv.time %.3f worldframes %i simulating %i ingame %i paused %i deathpage %i\n",
+		Cmd_Argc() > 1 ? Cmd_Argv( 1 ) : "probe",
+		sv.time, sv.framecount, sv.simulating ? 1 : 0, CL_IsInGame() ? 1 : 0,
+		sv.paused ? 1 : 0, CL_CoF_DeathWorldLive() ? 1 : 0 );
+}
+
+/*
+==================
 SV_InitHostCommands
 
 commands that create server
@@ -1036,6 +1059,7 @@
 void SV_InitHostCommands( void )
 {
 	Cmd_AddRestrictedCommand( "map", SV_Map_f, "start new level" );
+	Cmd_AddCommand( "cof_world_probe", SV_CoFWorldProbe_f, "developer: print the server clock, the world frame counter and the pause gates" );
 	Cmd_AddCommand( "maps", SV_Maps_f, "list maps" );
 
 	if( host.type == HOST_NORMAL )
