diff --git a/3rdparty/mainui/BaseMenu.cpp b/3rdparty/mainui/BaseMenu.cpp
index 5f8eb42..c7f0c5b 100644
--- a/3rdparty/mainui/BaseMenu.cpp
+++ b/3rdparty/mainui/BaseMenu.cpp
@@ -20,6 +20,7 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 
 */
 #include "extdll_menu.h"
+#include "menus/CryOfFear.h"
 #include "BaseMenu.h"
 #include "PicButton.h"
 #include "keydefs.h"
@@ -57,6 +58,46 @@ const char	*uiSounds[] = {
 	""
 };
 
+/*
+=================
+uiSoundsCoF
+
+Cry of Fear ships none of the Half-Life menu sounds above - no media/launch_*,
+no sound/common/launch_*, no buttons/blip*, and there is no valve/ fallback
+directory and no PAK to hide them in (measured against the canonical read-only
+copy). FS_LoadSound fails for every one of them and S_LoadSound substitutes a
+second of silence, which is why the engine menu made no sound at all.
+
+The game's own UI sounds are in cryoffear/sound/ui: 3d_click.wav and
+3d_rollover.wav (the in-world 3D menu panel) and buttonclick.wav,
+buttonclickrelease.wav, buttonrollover.wav (its VGUI2 GameUI, referenced from
+resource/*.res rather than from code). wpn_denyselect.wav in sound/common is the
+one "no" sound the game ships. Mapped in the same order as uiSounds.
+
+This cannot reach the engine's gallery-MAIN-MENU hook. That hook
+(CL_CoF_MenuPanelSound, engine/client/cl_main.c) is called only from
+pfnPlaySoundByName in engine/client/dll_int/cl_game.c - the *client DLL's*
+callback - and it only answers to volume 0.5. The menu plays through
+EngFuncs::PlayLocalSound -> pfnPlaySound (cl_gameui.c) -> S_StartLocalSound at
+VOL_NORM, a different function and a different volume, and the hook additionally
+requires cl_background 0 and ca_active, neither of which holds for the menu over
+the background map. Verified in the engine sources; ui/3d_click.wav from here is
+safe.
+=================
+*/
+const char	*uiSoundsCoF[] = {
+	"ui/3d_click.wav",		// SND_IN
+	"ui/buttonclickrelease.wav",	// SND_OUT
+	"ui/3d_click.wav",		// SND_LAUNCH
+	"ui/3d_rollover.wav",		// SND_ROLLOVER
+	"ui/buttonclick.wav",		// SND_GLOW
+	"common/wpn_denyselect.wav",	// SND_BUZZ
+	"ui/buttonclick.wav",		// SND_KEY
+	"common/wpn_denyselect.wav",	// SND_REMOVEKEY
+	"ui/3d_rollover.wav",		// SND_MOVE
+	""				// SND_NULL
+};
+
 // they match default WON colors.lst now, except alpha
 // a1ba: made uiColorHelp brighter so it's easier to read
 unsigned int uiColorHelp        = 0xFFA0A0A0; // 160, 160, 160, 255 // hint letters color
@@ -627,6 +668,9 @@ void UI_UpdateMenu( float flTime )
 		// load scr
 		UI_LoadScriptConfig();
 
+		// archived defaults the theme wants, now that config.cfg has run
+		UI_ThemeApplyDeferredDefaults();
+
 		loadStuff = false;
 	}
 
@@ -996,6 +1040,10 @@ static void UI_LoadBackgroundMapList( void )
 
 static void UI_LoadSounds( void )
 {
+	// Cry of Fear substitutes its own set; every other game keeps the stock
+	// names exactly as before, including the media/ -> sound/common/ rewrite.
+	const char **table = UI_IsCryOfFear() ? uiSoundsCoF : uiSounds;
+
 	memset( uiStatic.sounds, 0, sizeof( uiStatic.sounds ) );
 
 	if ( uiStatic.lowmemory )
@@ -1003,18 +1051,32 @@ static void UI_LoadSounds( void )
 
 	for ( int i = 0; i < SND_COUNT; i++ )
 	{
-		if ( !uiSounds[i] || *uiSounds[i] == '\0' )
+		if ( !table[i] || *table[i] == '\0' )
 			continue;
 
-		if ( !EngFuncs::FileExists( uiSounds[i] ) )
+		if ( !EngFuncs::FileExists( table[i] ) )
 		{
 			size_t len = strlen( uiSoundOldPrefix );
 
-			if ( !strncmp( uiSounds[i], uiSoundOldPrefix, len ) )
-				snprintf( uiStatic.sounds[i], sizeof( uiStatic.sounds[i] ), "%s%s", uiSoundNewPrefix, uiSounds[i] + len );
+			if ( !strncmp( table[i], uiSoundOldPrefix, len ) )
+				snprintf( uiStatic.sounds[i], sizeof( uiStatic.sounds[i] ), "%s%s", uiSoundNewPrefix, table[i] + len );
+			else
+			{
+				// FS_LoadSound tries "sound/<name>" before "<name>"
+				// (engine/client/soundlib/snd_main.c), and the Cry of Fear
+				// names are relative to sound/, so a FileExists miss on the
+				// bare name is expected and the name is still correct.
+				Q_strncpy( uiStatic.sounds[i], table[i], sizeof( uiStatic.sounds[i] ) );
+			}
 		}
 		else
-			Q_strncpy( uiStatic.sounds[i], uiSounds[i], sizeof( uiStatic.sounds[i] ) );
+			Q_strncpy( uiStatic.sounds[i], table[i], sizeof( uiStatic.sounds[i] ) );
+	}
+
+	if( UI_IsCryOfFear( ))
+	{
+		for( int i = 0; i < SND_COUNT; i++ )
+			Con_DPrintf( "Cry of Fear menu sound %d: \"%s\"\n", i, uiStatic.sounds[i] );
 	}
 }
 
@@ -1060,6 +1122,13 @@ int UI_VidInit( void )
 
 	UI_ScaleCoords( NULL, NULL, &uiStatic.outlineWidth, NULL );
 
+	// The stock 4 virtual units scale to ~5.6 px at 1080p and ~11 px at 2160p,
+	// which is what makes every control read as a heavy WON frame. The Source
+	// look wants a hairline, so make the library-wide default one physical
+	// pixel per 768 units; every UI_DrawRectangle call site follows.
+	if( UI_ThemeActive( ))
+		uiStatic.outlineWidth = UI_ThemeHairline();
+
 	// trying to load chapterbackgrounds.txt
 	UI_LoadBackgroundMapList ();
 
@@ -1179,6 +1248,10 @@ void UI_Init( void )
 
 	uiStatic.renderPicbuttonText = gMenu.m_gameinfo.flags & GFL_RENDER_PICBUTTON_TEXT;
 
+	// Source-style theme. Runs before colors.lst so a colors.lst still wins on
+	// the seven keys it can express.
+	UI_ThemeInit();
+
 	// trying to load colors.lst
 	UI_ApplyCustomColors ();
 }
diff --git a/3rdparty/mainui/BaseMenu.h b/3rdparty/mainui/BaseMenu.h
index bd3c25d..85d150b 100644
--- a/3rdparty/mainui/BaseMenu.h
+++ b/3rdparty/mainui/BaseMenu.h
@@ -25,6 +25,7 @@ GNU General Public License for more details.
 #include "WindowSystem.h"
 #include "Image.h"
 #include "utlstring.h"
+#include "Theme.h"
 
 #define UI_PULSE_DIVISOR		75.0f
 
@@ -98,6 +99,18 @@ typedef struct
 	HFont hLightBlur;
 	HFont hHeavyBlur;
 
+	// Source-style theme handles. On Windows the menu uses the GDI backend and
+	// CBaseFont::DrawCharacter ignores the charH argument (SCALE_FONTS is not
+	// defined for win32), so a new text size needs a new HFont, not a new
+	// charSize. See Theme.h for the sizes.
+	HFont hThemeTitle;
+	HFont hThemeLabel;
+	HFont hThemeBody;
+	HFont hThemeButton;
+	HFont hThemeItem;
+	HFont hThemeLogo;
+	HFont hThemeLogoSub;
+
 	bool	m_fDemosPlayed;
 	bool	m_fNoOldBackground;
 	int 	m_iOldMenuDepth;
diff --git a/3rdparty/mainui/Theme.cpp b/3rdparty/mainui/Theme.cpp
new file mode 100644
index 0000000..33f3a01
--- /dev/null
+++ b/3rdparty/mainui/Theme.cpp
@@ -0,0 +1,515 @@
+/*
+Theme.cpp -- Source-style theme for the Cry of Fear unified UI (milestone 3a)
+
+This program is free software: you can redistribute it and/or modify
+it under the terms of the GNU General Public License as published by
+the Free Software Foundation, either version 3 of the License, or
+(at your option) any later version.
+
+This program is distributed in the hope that it will be useful,
+but WITHOUT ANY WARRANTY; without even the implied warranty of
+MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
+GNU General Public License for more details.
+*/
+#include "extdll_menu.h"
+#include "BaseMenu.h"
+#include "Utils.h"
+#include "Theme.h"
+#include "menus/CryOfFear.h"
+
+bool g_bThemeActive = false;
+
+cvar_t *ui_theme = NULL;
+
+// marker for UI_ThemeApplyDeferredDefaults. It has to be *registered* here, in
+// UI_Init, and only read later: UI_Init runs inside CL_Init, before Host_Init
+// execs config.cfg, and a name config.cfg sets before the menu has registered
+// it does not reach the menu at all. Registering early means config.cfg's own
+// line lands on this cvar, which is the whole point of the marker.
+static cvar_t *ui_cof_scene_defaults = NULL;
+
+// the marker's generation. 0 = nothing applied yet, 1 = ui_renderworld and
+// r_detailtextures, 2 = ui_sound_volume, 3 = hud_scale cleared. Raise this when
+// a new one-shot default is added; UI_ThemeApplyDeferredDefaults then applies
+// only the steps between the recorded generation and this one.
+#define COF_SCENE_DEFAULTS_GEN	3
+
+/*
+=================
+UI_ThemeInit
+
+Sampled once. Switching ui_theme at runtime would also have to re-pick the font
+family and rebuild every handle, so the cvar is read at startup only and is
+documented as needing a restart.
+=================
+*/
+void UI_ThemeInit( void )
+{
+	ui_theme = EngFuncs::CvarRegister( "ui_theme", "1", FCVAR_ARCHIVE );
+
+	g_bThemeActive = ui_theme && ui_theme->value != 0.0f;
+
+	if( !g_bThemeActive )
+		return;
+
+	if( UI_IsCryOfFear( ))
+		ui_cof_scene_defaults = EngFuncs::CvarRegister( "ui_cof_scene_defaults", "0", FCVAR_ARCHIVE );
+
+	// Re-tint every control that already reads a global. colors.lst can still
+	// override the seven keys it knows, because UI_ApplyCustomColors runs after
+	// this - but it cannot set alpha (UI_ParseColor -> PackRGB), so the
+	// translucent panels and fills below are only reachable from here.
+	uiColorHelp        = THEME_TEXT_DIM;
+	uiPromptBgColor    = THEME_PANEL;
+	uiPromptTextColor  = THEME_TEXT;
+	uiPromptFocusColor = THEME_TEXT_HI;
+	uiInputTextColor   = THEME_ACCENT;
+	uiInputBgColor     = THEME_INPUT_BG;
+	uiInputFgColor     = THEME_INPUT_BORDER;
+	uiColorDkGrey      = THEME_DISABLED;
+	uiColorBlack       = THEME_INPUT_BG;
+
+	// UI_ApplyCustomColors only pushes the console colour when a colors.lst
+	// exists, and Cry of Fear ships none, so push it here. A colors.lst can
+	// still override it afterwards.
+	{
+		int r, g, b;
+
+		UnpackRGB( r, g, b, THEME_TEXT );
+		EngFuncs::SetConsoleDefaultColor( r, g, b );
+	}
+}
+
+/*
+=================
+UI_ThemeApplyDeferredDefaults
+
+The translucent pause scrim (controls/BackgroundBitmap.cpp) only replaces the
+opaque background fill while the engine is still drawing the world behind the
+menu, which it does only when ui_renderworld is on - and FWGS registers that
+cvar with a default of "0" (engine/client/cl_main.c:92), so in the deployed
+build the paused scene was black behind the scrim.
+
+ui_renderworld is FCVAR_ARCHIVE, so it cannot simply be forced every startup:
+that would ignore a user who turned it off. Instead flip it once, the first
+time Cry of Fear runs a build that knows about it, and record that in an
+archived marker of the menu's own. Afterwards config.cfg carries whatever the
+user chose and nothing here touches it again.
+
+This cannot live in UI_ThemeInit: UI_Init runs from SCR_Init inside CL_Init,
+and Host_Init execs config.cfg afterwards, so anything set there is overwritten
+by the archived value a moment later. UI_UpdateMenu's one-time block is the
+first point that is safely past it.
+=================
+*/
+void UI_ThemeApplyDeferredDefaults( void )
+{
+	// The marker is a generation number, not a flag. A build that adds a new
+	// deferred default raises COF_SCENE_DEFAULTS_GEN, so an installation that
+	// already ran an older build gets only the defaults it has never seen -
+	// every choice it made about the older ones still stands.
+	int generation;
+
+	if( !g_bThemeActive || !ui_cof_scene_defaults )
+		return;
+
+	generation = (int)ui_cof_scene_defaults->value;
+
+	if( generation >= COF_SCENE_DEFAULTS_GEN )
+		return; // nothing new here; the user's choices stand
+
+	if( generation < 3 )
+	{
+		// Generation 3: clear a hud_scale this build's predecessors let the
+		// user set. The Video page used to offer "Auto scale HUD", and for
+		// Cry of Fear that is actively harmful: the client builds its own
+		// renderer's viewport and projection out of the same screen size the
+		// engine reports for the HUD, so a virtual screen size zooms and
+		// shifts the 3D view, in game and on the menu background map alike.
+		// The checkbox is gone for this game and the engine ignores the cvar
+		// for it, but an archived value from an older build is still sitting
+		// in config.cfg, so clear it once. HUD scale on the Game options page
+		// is the supported control now (docs/cof-ui-m4-scaling.md).
+		if( EngFuncs::GetCvarFloat( "hud_scale" ) != 0.0f )
+		{
+			EngFuncs::CvarSetValue( "hud_scale", 0.0f );
+			Con_Printf( "Cry of Fear: hud_scale cleared, it breaks this game's own 3D view. Use HUD scale in Options > Game\n" );
+		}
+	}
+
+	if( generation < 2 )
+	{
+		// Generation 2: the engine menu's own sounds. Cry of Fear's UI sounds
+		// (ui/3d_click.wav and friends, mixed for the client's own panels) are
+		// noticeably louder than the game, which the user reported. The engine
+		// cvar is registered by the engine and applies to the menu library's
+		// sounds only (engine/client/dll_int/cl_gameui.c, pfnPlaySound); its
+		// stock default is 1.0 and every other game keeps it.
+		EngFuncs::CvarSetValue( "ui_sound_volume", 0.5f );
+		Con_Printf( "Cry of Fear: ui_sound_volume defaulted to 0.5 (the game's own UI sounds are loud)\n" );
+	}
+
+	if( generation >= 1 )
+	{
+		// the generation-1 defaults below have already been offered once
+		EngFuncs::CvarSetValue( "ui_cof_scene_defaults", (float)COF_SCENE_DEFAULTS_GEN );
+		return;
+	}
+
+	EngFuncs::CvarSetValue( "ui_renderworld", 1.0f );
+
+	// Detail textures: Cry of Fear has no detail textures. It ships no
+	// maps/<map>_detail.txt at all, and its own renderer has a separate cvar
+	// of its own (gl_detailtex, registered by client.dll) for the thing it
+	// does do. The engine's r_detailtextures is FCVAR_GLCONFIG and defaults to
+	// 1 (ref/gl/gl_opengl.c), and the Video page's checkbox for it is removed
+	// for this game, so default the cvar off through the same one-shot marker
+	// - an explicit value the user put in opengl.cfg still wins, because the
+	// marker is already 1 by then.
+	EngFuncs::CvarSetValue( "r_detailtextures", 0.0f );
+
+	EngFuncs::CvarSetValue( "ui_cof_scene_defaults", (float)COF_SCENE_DEFAULTS_GEN );
+
+	Con_Printf( "Cry of Fear: ui_renderworld defaulted to 1 so the paused scene shows through the menu scrim\n" );
+	Con_Printf( "Cry of Fear: r_detailtextures defaulted to 0 (this game has no detail textures)\n" );
+}
+
+// ---------------------------------------------------------------------------
+// font family
+// ---------------------------------------------------------------------------
+
+// Inter (SIL Open Font License 1.1) is shipped with the game data under
+// cryoffear/gfx/fonts and is read by the stb_truetype backend through
+// CFontManager::FindFontDataFile. If that file is missing or unreadable, or if
+// the build has no TTF backend, CFontBuilder falls through this chain on the
+// Windows GDI backend instead of dropping the whole menu to the bitmap font,
+// which is a visible catastrophe rather than a graceful degradation.
+//
+// "Inter" is first because the static faces may also be installed on the
+// system; Tahoma is present on every Windows install.
+static const char *s_szThemeFonts[] =
+{
+	"Inter",
+	"Tahoma",
+	"Verdana",
+	"Microsoft Sans Serif",
+	"Arial",
+	NULL
+};
+
+const char *UI_ThemeFontFamily( void )
+{
+	return s_szThemeFonts[0];
+}
+
+const char *UI_ThemeFontFallback( int i )
+{
+	if( i < 0 || i >= (int)( sizeof( s_szThemeFonts ) / sizeof( s_szThemeFonts[0] )))
+		return NULL;
+	return s_szThemeFonts[i];
+}
+
+// ---------------------------------------------------------------------------
+// primitives
+// ---------------------------------------------------------------------------
+
+int UI_ThemeHairline( void )
+{
+	int w = (int)( uiStatic.scaleY + 0.5f );
+
+	return w < 1 ? 1 : w;
+}
+
+void UI_DrawHairline( Point pos, Size size, unsigned int color )
+{
+	UI_DrawRectangleExt( pos.x, pos.y, size.w, size.h, color, UI_ThemeHairline( ));
+}
+
+void UI_ThemeSeparator( int x, int y, int w, unsigned int color )
+{
+	UI_FillRect( x, y, w, UI_ThemeHairline( ), color );
+}
+
+/*
+=================
+UI_ThemeDeathBacking
+
+A faint dark strip behind one line of the death page. The page draws over the
+live scene with no panel and no scrim, exactly like Cry of Fear's own GAME OVER
+screen, so each line needs its own backing to stay readable whatever is behind
+it. Coordinates are already scaled; the padding is not.
+=================
+*/
+void UI_ThemeDeathBacking( int x, int y, int w, int h )
+{
+	const int px = (int)( THEME_DEATH_PAD_X * uiStatic.scaleX );
+	const int py = (int)( THEME_DEATH_PAD_Y * uiStatic.scaleY );
+
+	UI_FillRect( x - px, y - py, w + px * 2, h + py * 2, THEME_DEATH_BACKING );
+}
+
+static bool g_bThemeSceneUnveiled = false;
+
+void UI_ThemeSetSceneUnveiled( bool on )
+{
+	g_bThemeSceneUnveiled = on;
+}
+
+bool UI_ThemeSceneUnveiled( void )
+{
+	return g_bThemeActive && g_bThemeSceneUnveiled;
+}
+
+void UI_ThemeDrawCross( Point pos, Size size, unsigned int color )
+{
+	const int t = UI_ThemeHairline() * 2;
+	const int n = size.w < size.h ? size.w : size.h;
+
+	if( n <= t )
+		return;
+
+	// two diagonals of small filled squares - there is no line primitive
+	for( int i = 0; i <= n - t; i++ )
+	{
+		UI_FillRect( pos.x + i, pos.y + i, t, t, color );
+		UI_FillRect( pos.x + ( n - t - i ), pos.y + i, t, t, color );
+	}
+}
+
+void UI_ThemeDrawTriangle( Point pos, Size size, int dir, unsigned int color )
+{
+	if( size.w <= 0 || size.h <= 0 )
+		return;
+
+	if( dir == 0 || dir == 1 ) // left / right
+	{
+		for( int i = 0; i < size.w; i++ )
+		{
+			// the apex is the narrow end: column 0 is narrow for a left arrow,
+			// wide for a right arrow
+			int k = ( dir == 0 ) ? ( size.w - 1 - i ) : i;
+			int h = size.h - ( size.h * k ) / size.w;
+
+			if( h < 1 ) h = 1;
+			UI_FillRect( pos.x + i, pos.y + ( size.h - h ) / 2, 1, h, color );
+		}
+	}
+	else // up / down
+	{
+		for( int i = 0; i < size.h; i++ )
+		{
+			int k = ( dir == 2 ) ? ( size.h - 1 - i ) : i;
+			int w = size.w - ( size.w * k ) / size.h;
+
+			if( w < 1 ) w = 1;
+			UI_FillRect( pos.x + ( size.w - w ) / 2, pos.y + i, w, 1, color );
+		}
+	}
+}
+
+void UI_ThemeDrawTick( Point pos, Size size, unsigned int color )
+{
+	const int t = UI_ThemeHairline() * 2;
+	const int n = ( size.w < size.h ? size.w : size.h );
+	const int shortArm = n / 3;
+	const int longArm  = n - shortArm;
+
+	// down-right stroke of the tick
+	for( int i = 0; i < shortArm; i++ )
+		UI_FillRect( pos.x + i, pos.y + n / 2 + i, t, t, color );
+
+	// up-right stroke
+	for( int i = 0; i < longArm; i++ )
+		UI_FillRect( pos.x + shortArm + i, pos.y + n / 2 + shortArm - i, t, t, color );
+}
+
+int UI_ThemeButtonWidth( const char *name )
+{
+	if( uiStatic.scaleX <= 0.0f )
+		return THEME_BTN_MIN_W;
+
+	int px = g_FontMgr->GetTextWideScaled( uiStatic.hThemeButton, name ? name : "",
+		(int)( THEME_BUTTON_TALL * uiStatic.scaleY ));
+	int v = (int)( px / uiStatic.scaleX ) + 28;
+
+	return v < THEME_BTN_MIN_W ? THEME_BTN_MIN_W : v;
+}
+
+void UI_ThemeDrawDialogPanel( Point pos, Size size, const char *title )
+{
+	const int bandH = (int)( THEME_TITLE_BAND_H * uiStatic.scaleY );
+
+	UI_FillRect( pos, size, THEME_PANEL );
+	UI_DrawHairline( pos, size, THEME_PANEL_BORDER );
+
+	UI_FillRect( pos.x, pos.y, size.w, bandH, THEME_TITLE_BAND );
+	UI_ThemeSeparator( pos.x, pos.y + bandH, size.w, THEME_SEPARATOR );
+
+	if( title )
+	{
+		const int chSize = (int)( THEME_TITLE_TALL * uiStatic.scaleY );
+
+		UI_DrawString( uiStatic.hThemeTitle,
+			Point( pos.x + (int)( THEME_PAD_X * uiStatic.scaleX ), pos.y + ( bandH - chSize ) / 2 ),
+			Size( size.w - (int)( THEME_PAD_X * 2 * uiStatic.scaleX ), chSize ),
+			title, THEME_TITLE_TEXT, chSize, QM_LEFT,
+			ETF_FORCECOL | ETF_NO_WRAP | ETF_NOSIZELIMIT );
+	}
+}
+
+// ---------------------------------------------------------------------------
+// message panels
+//
+// The stock confirmation box is a fixed 520x200 panel and the message is drawn
+// into whatever is left after the title band and the button row. A two-sentence
+// warning does not fit that, so UI_DrawString ran out of lines and ellipsised
+// the text into the button row. Measure the wrapped message first and size the
+// panel to it instead.
+// ---------------------------------------------------------------------------
+
+int UI_ThemeWrappedLines( int font, const char *str, int charH, int widthPx )
+{
+	int lines = 1;
+	int wide = 0;
+	int wordWide = 0;   // width of the word being accumulated
+	bool haveBreak = false; // a space has been seen on this line
+
+	if( !str || !str[0] || widthPx <= 0 )
+		return 0;
+
+	for( const char *p = str; *p; p++ )
+	{
+		if( IsColorString( p ))
+		{
+			p++; // the colour code is not drawn
+			continue;
+		}
+
+		if( *p == '\n' )
+		{
+			lines++;
+			wide = wordWide = 0;
+			haveBreak = false;
+			continue;
+		}
+
+		int cw = g_FontMgr->GetCharacterWidthScaled( font, (unsigned char)*p, charH );
+
+		if( *p == ' ' )
+		{
+			wide += wordWide + cw;
+			wordWide = 0;
+			haveBreak = true;
+			continue;
+		}
+
+		if( wide + wordWide + cw > widthPx )
+		{
+			lines++;
+
+			if( haveBreak )
+			{
+				// the unfinished word moves down with the new line
+				wide = 0;
+				haveBreak = false;
+			}
+			else
+			{
+				// one word longer than the line: hard break
+				wide = wordWide = 0;
+			}
+		}
+
+		wordWide += cw;
+	}
+
+	return lines;
+}
+
+void UI_ThemeMessagePanelSize( const char *str, int *w, int *h )
+{
+	// three measures, narrowest first: a short confirmation keeps the compact
+	// Source dialog, a long warning is allowed to get wider before it gets tall
+	static const int widths[] = { 520, 660, 800 };
+	const int charH = (int)( THEME_BODY_TALL * uiStatic.scaleY );
+	const int chrome = THEME_TITLE_BAND_H + THEME_PAD_TOP + THEME_PAD_BOTTOM
+		+ THEME_BTNROW_H + THEME_BTNROW_GAP;
+	const int nWidths = (int)( sizeof( widths ) / sizeof( widths[0] ));
+	const int maxH = 768 - 64;
+	const float sy = uiStatic.scaleY > 0.0f ? uiStatic.scaleY : 1.0f;
+	int best = nWidths - 1, bestH = 200;
+
+	for( int i = 0; i < nWidths; i++ )
+	{
+		int textW = (int)(( widths[i] - THEME_PAD_X * 2 ) * uiStatic.scaleX );
+		int lines = UI_ThemeWrappedLines( uiStatic.hThemeBody, str, charH, textW );
+
+		// one line of headroom once the message wraps at all: this count and
+		// UI_DrawString's own word wrapping agree on ordinary text but not
+		// necessarily on every colour-coded or hard-broken edge case, and being
+		// one line short is what ellipsised the text in the first place
+		if( lines > 1 )
+			lines++;
+
+		int total = chrome + (int)( lines * charH / sy ) + 8;
+
+		best = i;
+		bestH = total;
+
+		// the narrowest measure that still reads as a compact dialog wins;
+		// only a genuinely long message is allowed to get wider
+		if( total <= 300 )
+			break;
+	}
+
+	if( bestH < 200 )
+		bestH = 200;
+	if( bestH > maxH )
+		bestH = maxH;
+
+	if( w ) *w = widths[best];
+	if( h ) *h = bestH;
+}
+
+// ---------------------------------------------------------------------------
+// letter-spaced text
+//
+// UI_DrawString advances strictly by DrawCharacter's return value, so tracking
+// is not expressible through it. The logo is the one place the look wants it.
+// ---------------------------------------------------------------------------
+
+int UI_ThemeDrawTracked( int font, int x, int y, const char *str,
+	unsigned int color, int charH, int tracking )
+{
+	int start = x;
+
+	if( !str )
+		return 0;
+
+	for( const char *p = str; *p; p++ )
+	{
+		if( *p == ' ' )
+		{
+			x += g_FontMgr->GetCharacterWidthScaled( font, ' ', charH ) + tracking;
+			continue;
+		}
+
+		x += g_FontMgr->DrawCharacter( font, (unsigned char)*p, Point( x, y ), charH, color );
+		x += tracking;
+	}
+
+	return x - start;
+}
+
+int UI_ThemeTrackedWidth( int font, const char *str, int charH, int tracking )
+{
+	int w = 0;
+
+	if( !str )
+		return 0;
+
+	for( const char *p = str; *p; p++ )
+		w += g_FontMgr->GetCharacterWidthScaled( font, (unsigned char)*p, charH ) + tracking;
+
+	return w > 0 ? w - tracking : 0;
+}
diff --git a/3rdparty/mainui/Theme.h b/3rdparty/mainui/Theme.h
new file mode 100644
index 0000000..63aeb54
--- /dev/null
+++ b/3rdparty/mainui/Theme.h
@@ -0,0 +1,204 @@
+/*
+Theme.h -- Source-style theme for the Cry of Fear unified UI (milestone 3a)
+
+This program is free software: you can redistribute it and/or modify
+it under the terms of the GNU General Public License as published by
+the Free Software Foundation, either version 3 of the License, or
+(at your option) any later version.
+
+This program is distributed in the hope that it will be useful,
+but WITHOUT ANY WARRANTY; without even the implied warranty of
+MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
+GNU General Public License for more details.
+
+------------------------------------------------------------------------------
+
+One place for every colour, metric and primitive the minimalist Source-era look
+needs. The theme is a compile-time addition guarded by the archived cvar
+"ui_theme" (1 = this theme, 0 = the upstream WON look); the value is sampled
+once in UI_Init, because turning it on also picks the font family and rewrites
+the library's colour globals.
+
+Nothing here loads a bitmap: Cry of Fear ships no gfx/shell artwork at all, so
+checkboxes, sliders, spinner arrows, table arrows and the panel close button are
+drawn from filled rectangles instead of pictures.
+
+All sizes below are in the 768-unit-tall virtual space (UI_VidInit), not pixels.
+*/
+#ifndef MAINUI_THEME_H
+#define MAINUI_THEME_H
+
+#include "Primitive.h"
+
+// ---------------------------------------------------------------------------
+// palette, packed 0xAARRGGBB as PackRGBA builds it
+// ---------------------------------------------------------------------------
+#define THEME_SCRIM         0x96000000u // 0 0 0 150      full-screen dim behind a modal panel
+#define THEME_PANEL         0xE6202020u // 32 32 32 230   dialog body
+#define THEME_PANEL_BORDER  0xC8969696u // 150 150 150 200
+#define THEME_TITLE_BAND    0xF0141414u // 20 20 20 240
+#define THEME_TITLE_TEXT    0xFFDCDCDCu // 220 220 220
+#define THEME_TEXT          0xFFBEBEBEu // 190 190 190    body, rows, items at rest
+#define THEME_TEXT_DIM      0xFF8C8C8Cu // 140 140 140    labels above controls, hints
+#define THEME_TEXT_HI       0xFFF0F0F0u // 240 240 240    hovered
+#define THEME_ACCENT        0xFFF0B418u // 240 180 24     the single accent
+#define THEME_SELECT_FILL   0x33F0B418u
+#define THEME_HOVER_FILL    0x14FFFFFFu
+#define THEME_PRESS_FILL    0x28FFFFFFu
+#define THEME_DISABLED      0xFF646464u // 100 100 100
+#define THEME_INPUT_BG      0xC80C0C0Cu // 12 12 12 200
+#define THEME_INPUT_BORDER  0xFF5A5A5Au // 90 90 90
+#define THEME_TRACK         0xFF3C3C3Cu // 60 60 60
+#define THEME_SEPARATOR     0x40FFFFFFu
+// The death page has no panel and no scrim, so each of its text lines carries
+// its own faint dark backing strip - the same trick Cry of Fear's own GAME
+// OVER panel uses to stay readable over an arbitrary scene.
+#define THEME_DEATH_BACKING 0x5A000000u // 0 0 0 90
+
+// ---------------------------------------------------------------------------
+// type scale, "tall" values handed to CFontBuilder (multiplied by scaleY)
+// ---------------------------------------------------------------------------
+#define THEME_TITLE_TALL    17
+#define THEME_LABEL_TALL    16
+#define THEME_BODY_TALL     19
+#define THEME_BUTTON_TALL   19
+#define THEME_ITEM_TALL     24
+#define THEME_LOGO_TALL     46
+#define THEME_LOGOSUB_TALL  20
+
+// ---------------------------------------------------------------------------
+// panel metrics
+// ---------------------------------------------------------------------------
+#define THEME_TITLE_BAND_H  34
+#define THEME_CLOSE_SIZE    26
+#define THEME_CLOSE_INSET   6
+#define THEME_PAD_X         24
+#define THEME_PAD_TOP       20
+#define THEME_PAD_BOTTOM    20
+#define THEME_BTNROW_H      32
+#define THEME_BTNROW_GAP    20
+#define THEME_BTN_MIN_W     110
+#define THEME_BTN_GAP       12
+#define THEME_GUTTER2       28
+#define THEME_GUTTER3       22
+#define THEME_ROW_H         30 // a full-width selectable row (difficulty, language, ...)
+#define THEME_ROW_PITCH     34
+#define THEME_CTRL_H        30 // spin control, edit field, drop-down
+#define THEME_CTRL_PITCH    62 // label line + control + gap
+#define THEME_CHECK_SIZE    22
+#define THEME_SLIDER_TRACK  6
+#define THEME_SLIDER_H      26
+
+// main menu (centred) and pause menu (lower left)
+#define THEME_MENU_MARGIN   64
+#define THEME_MENU_PITCH    34
+#define THEME_MENU_BOTTOM   96 // pause menu: last item baseline sits at 768 - this
+#define THEME_MENU_CENTER_PCT 52 // main menu: the logo+list block is centred on this % of the height
+#define THEME_LOGO_GAP      28 // between the logo block and the first item
+#define THEME_LOGO_GAP_CENTER 48
+#define THEME_LOGO_TRACKING 4  // extra letter spacing for "CRY OF FEAR", virtual units
+
+// death page: the wordmark-style title and the two links, as percentages of
+// the 768-unit height, matching the original CGameOver panel's own placement
+#define THEME_DEATH_TITLE_PCT 35
+#define THEME_DEATH_ITEM_PCT  60
+#define THEME_DEATH_PITCH     36 // between "Load Game" and "Exit"
+#define THEME_DEATH_PAD_X     28 // backing strip padding, virtual units
+#define THEME_DEATH_PAD_Y     6
+
+// ---------------------------------------------------------------------------
+// state
+// ---------------------------------------------------------------------------
+extern bool g_bThemeActive;
+
+inline bool UI_ThemeActive( void ) { return g_bThemeActive; }
+
+// registers ui_theme, samples it, and rewrites the uiColor* globals.
+// Called from UI_Init *before* UI_ApplyCustomColors, so a gfx/shell/colors.lst
+// is still the last word on the seven keys it can reach (it cannot set alpha).
+void UI_ThemeInit( void );
+
+// Defaults that have to be applied *after* config.cfg has run, because
+// UI_Init happens inside CL_Init, before Host_Init execs it. Called once from
+// the same UI_UpdateMenu block that loads the backgrounds and the strings.
+void UI_ThemeApplyDeferredDefaults( void );
+
+// the GDI family the theme asks for, and the chain tried after it
+const char *UI_ThemeFontFamily( void );
+const char *UI_ThemeFontFallback( int i ); // NULL when the chain is exhausted
+
+// ---------------------------------------------------------------------------
+// hover / focus
+//
+// Every themed control draws its "hot" state from the item's own focus flags,
+// never from "this == m_pParent->ItemAtCursor()". CMenuItemsHolder::MouseMove
+// leaves m_iCursor on the last item the pointer touched, so the cursor-index
+// test keeps an item lit after the cursor has left it - the hover that never
+// clears. QMF_HASMOUSEFOCUS is set only on the item the pointer is inside and
+// cleared on every other item on each move; QMF_HASKEYBOARDFOCUS follows arrow
+// key navigation and is dropped as soon as the mouse moves focus elsewhere. So
+// the pointer wins while it is over an item, the keyboard item stays lit when
+// the pointer is over nothing, and nothing is lit when neither applies.
+// ---------------------------------------------------------------------------
+#define UI_THEME_HOT( item ) \
+	( FBitSet(( item )->iFlags, QMF_HASMOUSEFOCUS | QMF_HASKEYBOARDFOCUS ) != 0 )
+
+// ---------------------------------------------------------------------------
+// primitives
+// ---------------------------------------------------------------------------
+
+// one physical pixel at 1080p, two at 2160p - never the scaled 4-unit outline
+int  UI_ThemeHairline( void );
+
+// a hairline border *outside* the given (already scaled) rect, like every other
+// UI_DrawRectangle call site in the library
+void UI_DrawHairline( Point pos, Size size, unsigned int color );
+
+// a filled 1px separator line inside a rect
+void UI_ThemeSeparator( int x, int y, int w, unsigned int color );
+
+// the death page's per-line backing strip, around an already-scaled text box
+void UI_ThemeDeathBacking( int x, int y, int w, int h );
+
+// While this is set the in-game background draws nothing at all, so the live
+// scene shows through untouched. Only the death page uses it: Cry of Fear's own
+// GAME OVER screen does not dim the world, and neither should ours.
+void UI_ThemeSetSceneUnveiled( bool on );
+bool UI_ThemeSceneUnveiled( void );
+
+// diagonal cross, used for the panel close button. Scaled coordinates.
+void UI_ThemeDrawCross( Point pos, Size size, unsigned int color );
+
+// solid triangle from filled rows. dir: 0 left, 1 right, 2 up, 3 down.
+void UI_ThemeDrawTriangle( Point pos, Size size, int dir, unsigned int color );
+
+// a tick mark for a checked checkbox
+void UI_ThemeDrawTick( Point pos, Size size, unsigned int color );
+
+// width of a themed button, in virtual units, from the button font metrics
+int  UI_ThemeButtonWidth( const char *name );
+
+// panel fill, hairline border, title band and uppercase title. Scaled
+// coordinates. Shared by CMenuFramework's panel mode and the message boxes.
+void UI_ThemeDrawDialogPanel( Point pos, Size size, const char *title );
+
+// ---------------------------------------------------------------------------
+// message panels
+// ---------------------------------------------------------------------------
+
+// Number of lines UI_DrawString will wrap str into inside widthPx, counting
+// explicit newlines. Mirrors UI_DrawString's own word wrapping.
+int  UI_ThemeWrappedLines( int font, const char *str, int charH, int widthPx );
+
+// Panel size, in virtual units, for a message box holding str: wide enough for
+// a readable measure, tall enough for the wrapped message plus the button row.
+// Never taller than the 768-unit space.
+void UI_ThemeMessagePanelSize( const char *str, int *w, int *h );
+
+// UI_DrawString with extra advance between glyphs; returns the drawn width.
+// Used only by the main-menu logo, the one place the look wants tracking.
+int  UI_ThemeDrawTracked( int font, int x, int y, const char *str,
+	unsigned int color, int charH, int tracking );
+int  UI_ThemeTrackedWidth( int font, const char *str, int charH, int tracking );
+
+#endif // MAINUI_THEME_H
diff --git a/3rdparty/mainui/controls/Action.cpp b/3rdparty/mainui/controls/Action.cpp
index 067b43a..6de7a27 100644
--- a/3rdparty/mainui/controls/Action.cpp
+++ b/3rdparty/mainui/controls/Action.cpp
@@ -34,6 +34,24 @@ CMenuAction::Init
 */
 void CMenuAction::VidInit( )
 {
+	// VidInit runs on every resolution change, so only remap once
+	if( UI_ThemeActive() && font != uiStatic.hThemeLabel && font != uiStatic.hThemeBody )
+	{
+		// labels and captions follow the theme scale: anything the menu asked
+		// to be small becomes the label size, everything else the body size
+		if( charSize <= UI_SMALL_CHAR_HEIGHT )
+		{
+			font = uiStatic.hThemeLabel;
+			charSize = THEME_LABEL_TALL;
+		}
+		else
+		{
+			font = uiStatic.hThemeBody;
+			charSize = THEME_BODY_TALL;
+		}
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	m_iBackcolor.SetDefault( 0 );
 
 	if( !forceCalcW )
@@ -143,7 +161,8 @@ void CMenuAction::Draw( )
 		}
 	}
 
-	if( szStatusText && iFlags & QMF_NOTIFY )
+	// in theme mode the framework draws one hint line in the theme font
+	if( szStatusText && iFlags & QMF_NOTIFY && !UI_ThemeActive( ) )
 	{
 		Point coord;
 
diff --git a/3rdparty/mainui/controls/BackgroundBitmap.cpp b/3rdparty/mainui/controls/BackgroundBitmap.cpp
index 5c86f39..218b4f7 100644
--- a/3rdparty/mainui/controls/BackgroundBitmap.cpp
+++ b/3rdparty/mainui/controls/BackgroundBitmap.cpp
@@ -19,6 +19,7 @@ GNU General Public License for more details.
 #include "BackgroundBitmap.h"
 #include "Utils.h"
 #include "BaseWindow.h"
+#include "Theme.h"
 
 bool CMenuBackgroundBitmap::s_bEnableLogoMovie = false;
 bool CMenuBackgroundBitmap::s_bGameHasSteamBackground = false;
@@ -73,6 +74,12 @@ void CMenuBackgroundBitmap::DrawInGameBackground()
 	// otherwise, so that case keeps the original fill.
 	if( EngFuncs::GetCvarFloat( "ui_renderworld" ) != 0.0f )
 	{
+		// The death page draws over the untouched scene - Cry of Fear's own
+		// GAME OVER screen does not dim the world either, it only puts a faint
+		// strip behind each line of its own text.
+		if( UI_ThemeSceneUnveiled( ))
+			return;
+
 		int alpha = (int)EngFuncs::GetCvarFloat( "ui_scrim_alpha" );
 
 		if( alpha < 0 ) alpha = 0;
diff --git a/3rdparty/mainui/controls/CheckBox.cpp b/3rdparty/mainui/controls/CheckBox.cpp
index 77d013b..61684c2 100644
--- a/3rdparty/mainui/controls/CheckBox.cpp
+++ b/3rdparty/mainui/controls/CheckBox.cpp
@@ -45,10 +45,32 @@ CMenuCheckBox::Init
 */
 void CMenuCheckBox::VidInit( void )
 {
+	if( UI_ThemeActive( ))
+	{
+		// the stock 32x32 is sized for gfx/shell/cb_*.tga; the primitive box is
+		// 22x22 so it sits on the label's cap height
+		if( size.w == 32 && size.h == 32 )
+			SetSize( THEME_CHECK_SIZE, THEME_CHECK_SIZE );
+
+		colorText.SetDefault( THEME_TEXT );
+		font = uiStatic.hThemeBody;
+		charSize = THEME_BODY_TALL;
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	colorText.SetDefault( uiColorHelp );
 	BaseClass::VidInit();
-	m_scTextPos.x = m_scPos.x + ( m_scSize.w * 1.25f );
-	m_scTextPos.y = m_scPos.y;
+
+	if( UI_ThemeActive( ))
+	{
+		m_scTextPos.x = m_scPos.x + m_scSize.w + (int)( 10 * uiStatic.scaleX );
+		m_scTextPos.y = m_scPos.y + ( m_scSize.h - m_scChSize ) / 2;
+	}
+	else
+	{
+		m_scTextPos.x = m_scPos.x + ( m_scSize.w * 1.25f );
+		m_scTextPos.y = m_scPos.y;
+	}
 
 	m_scTextSize.w = g_FontMgr->GetTextWideScaled( font, szName, m_scChSize );
 	m_scTextSize.h = m_scChSize;
@@ -112,6 +134,33 @@ void CMenuCheckBox::Draw( void )
 {
 	uint textflags = ( iFlags & QMF_DROPSHADOW ? ETF_SHADOW : 0 ) | ETF_NOSIZELIMIT | ETF_FORCECOL;
 
+	if( UI_ThemeActive( ))
+	{
+		// Cry of Fear ships no gfx/shell/cb_* artwork, and UI_DrawPic silently
+		// no-ops on a null handle, so the stock control is an invisible hole
+		// with a label next to it. Draw the box from primitives instead.
+		const bool grayed = FBitSet( iFlags, QMF_GRAYED ) != 0;
+		const bool hot = !grayed && UI_THEME_HOT( this );
+
+		UI_DrawString( font, m_scTextPos, m_scTextSize, szName,
+			grayed ? THEME_DISABLED : ( hot ? THEME_TEXT_HI : colorText ),
+			m_scChSize, eTextAlignment, textflags );
+
+		UI_FillRect( m_scPos, m_scSize, THEME_INPUT_BG );
+		UI_DrawHairline( m_scPos, m_scSize,
+			grayed ? THEME_DISABLED : ( hot ? THEME_ACCENT : THEME_INPUT_BORDER ));
+
+		if( bChecked )
+		{
+			int inset = m_scSize.w / 4;
+
+			UI_ThemeDrawTick( Point( m_scPos.x + inset, m_scPos.y + inset ),
+				Size( m_scSize.w - inset * 2, m_scSize.h - inset * 2 ),
+				grayed ? THEME_DISABLED : THEME_ACCENT );
+		}
+		return;
+	}
+
 	UI_DrawString( font, m_scTextPos, m_scTextSize, szName, colorText, m_scChSize, eTextAlignment, textflags );
 
 	if( szStatusText && iFlags & QMF_NOTIFY )
diff --git a/3rdparty/mainui/controls/DropDown.cpp b/3rdparty/mainui/controls/DropDown.cpp
index df9a835..4dc9813 100644
--- a/3rdparty/mainui/controls/DropDown.cpp
+++ b/3rdparty/mainui/controls/DropDown.cpp
@@ -37,6 +37,17 @@ CMenuDropDown::CMenuDropDown() : BaseClass()
 
 void CMenuDropDown::VidInit()
 {
+	if( UI_ThemeActive( ))
+	{
+		iBackgroundColor.SetDefault( THEME_INPUT_BG );
+		iFgTextColor.SetDefault( THEME_TEXT_HI );
+		iBgTextColor.SetDefault( THEME_TEXT );
+		colorFocus.SetDefault( THEME_HOVER_FILL );
+		font = uiStatic.hThemeBody;
+		charSize = THEME_BODY_TALL;
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	iBackgroundColor.SetDefault( uiColorBlack );
 	iFgTextColor.SetDefault( uiInputFgColor );
 	iBgTextColor.SetDefault( uiPromptTextColor );
@@ -56,6 +67,12 @@ void CMenuDropDown::VidInit()
 		m_ArrowOpened.Load(UI_DOWNARROWFOCUS);
 	}
 	m_ArrowSize = EngFuncs::PIC_Size( m_ArrowClosed );
+
+	// gfx/shell arrow artwork is not shipped by this game, and PIC_Size of a
+	// missing image is the engine's fallback texture - big enough to eat the
+	// whole control. The theme draws a triangle in a square gutter instead.
+	if( UI_ThemeActive( ))
+		m_ArrowSize = Size( m_scItemSize.h, m_scItemSize.h );
 }
 
 int CMenuDropDown::IsNewStateByMouseClick()
@@ -168,7 +185,8 @@ void CMenuDropDown::Draw()
 			pt.y += m_scItemSize.h;
 
 		// all other items
-		UI_FillRect( pt.x, pt.y, m_scItemSize.w, m_scSize.h - m_scItemSize.h, 0xff000000 );
+		UI_FillRect( pt.x, pt.y, m_scItemSize.w, m_scSize.h - m_scItemSize.h,
+			UI_ThemeActive() ? THEME_PANEL : 0xff000000 );
 		for( int i = 0; i < m_szNames.Count(); i++ )
 		{
 			uint textColor = iBgTextColor;
@@ -208,11 +226,25 @@ void CMenuDropDown::Draw()
 
 	// arrow background
 	uint arrowX = selectedPos.x + selectedSize.w;
-	UI_FillRect( arrowX, selectedPos.y, m_ArrowSize.w, selectedSize.h, uiInputFgColor );
+	UI_FillRect( arrowX, selectedPos.y, m_ArrowSize.w, selectedSize.h,
+		UI_ThemeActive() ? selectedBgColor : uiInputFgColor );
 	// arrow
-	CImage &arrow = m_isOpen ? m_ArrowOpened : m_ArrowClosed;
-	Point arrowPoint( arrowX, selectedPos.y + (selectedSize.h - m_ArrowSize.h)/2 );
-	UI_DrawPic( arrowPoint, m_ArrowSize, -1, arrow );
+	if( UI_ThemeActive( ))
+	{
+		// gfx/shell arrow artwork is not shipped by this game
+		const int inset = m_ArrowSize.w / 3;
+
+		UI_ThemeDrawTriangle(
+			Point( arrowX + inset, selectedPos.y + ( selectedSize.h - m_ArrowSize.h ) / 2 + inset ),
+			Size( m_ArrowSize.w - inset * 2, m_ArrowSize.h - inset * 2 ),
+			m_isOpen ? 2 : 3, THEME_TEXT );
+	}
+	else
+	{
+		CImage &arrow = m_isOpen ? m_ArrowOpened : m_ArrowClosed;
+		Point arrowPoint( arrowX, selectedPos.y + (selectedSize.h - m_ArrowSize.h)/2 );
+		UI_DrawPic( arrowPoint, m_ArrowSize, -1, arrow );
+	}
 }
 
 void CMenuDropDownStr::UpdateEditable()
diff --git a/3rdparty/mainui/controls/Field.cpp b/3rdparty/mainui/controls/Field.cpp
index d602be0..5f2f615 100644
--- a/3rdparty/mainui/controls/Field.cpp
+++ b/3rdparty/mainui/controls/Field.cpp
@@ -54,6 +54,13 @@ CMenuField::Init
 */
 void CMenuField::VidInit( void )
 {
+	if( UI_ThemeActive( ))
+	{
+		font = uiStatic.hThemeBody;
+		charSize = THEME_BODY_TALL;
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	BaseClass::VidInit();
 
 	iCursor = strlen( szBuffer );
@@ -374,7 +381,8 @@ void CMenuField::Draw( void )
 
 	textflags |= ETF_NOSIZELIMIT;
 
-	if( szStatusText && iFlags & QMF_NOTIFY )
+	// in theme mode the framework draws one hint line in the theme font
+	if( szStatusText && iFlags & QMF_NOTIFY && !UI_ThemeActive( ))
 	{
 		int	x;
 
@@ -462,8 +470,19 @@ void CMenuField::Draw( void )
 		UI_DrawRectangle( newPos, m_scSize, uiInputFgColor );
 	}
 
-	textHeight = y - (m_scChSize * 1.5f);
-	UI_DrawString( font, m_scPos.x, textHeight, m_scSize.w, m_scChSize, szName, uiColorHelp, m_scChSize, QM_LEFT, textflags | ETF_FORCECOL );
+	if( UI_ThemeActive( ))
+	{
+		const int labelH = (int)( THEME_LABEL_TALL * uiStatic.scaleY );
+
+		textHeight = y - (int)( THEME_LABEL_TALL * 1.5f * uiStatic.scaleY );
+		UI_DrawString( uiStatic.hThemeLabel, m_scPos.x, textHeight, m_scSize.w, labelH,
+			szName, THEME_TEXT_DIM, labelH, QM_LEFT, ETF_FORCECOL | ETF_NOSIZELIMIT );
+	}
+	else
+	{
+		textHeight = y - (m_scChSize * 1.5f);
+		UI_DrawString( font, m_scPos.x, textHeight, m_scSize.w, m_scChSize, szName, uiColorHelp, m_scChSize, QM_LEFT, textflags | ETF_FORCECOL );
+	}
 
 	if( iFlags & QMF_GRAYED )
 	{
diff --git a/3rdparty/mainui/controls/Framework.cpp b/3rdparty/mainui/controls/Framework.cpp
index 12d2a29..617d03a 100644
--- a/3rdparty/mainui/controls/Framework.cpp
+++ b/3rdparty/mainui/controls/Framework.cpp
@@ -26,6 +26,156 @@ CMenuFramework::CMenuFramework( const char *name ) : BaseClass( name )
 	memset( m_apBtns, 0, sizeof( m_apBtns ) );
 	m_iBtnsNum = 0;
 	bannerAnimDirection = ANIM_NO;
+
+	bPanel = false;
+	bCloseAdded = false;
+	panelSize = Size( 560, 400 );
+	szPanelTitle = NULL;
+}
+
+void CMenuFramework::SetPanel( const char *title, int w, int h )
+{
+	bPanel = true;
+	szPanelTitle = title;
+	panelSize = Size( w, h );
+}
+
+Point CMenuFramework::PanelPos() const
+{
+	return Point(( uiStatic.width - panelSize.w ) / 2, ( 768 - panelSize.h ) / 2 );
+}
+
+Rect CMenuFramework::ContentRect() const
+{
+	Point p = PanelPos();
+
+	return Rect( p.x + THEME_PAD_X,
+		p.y + THEME_TITLE_BAND_H + THEME_PAD_TOP,
+		panelSize.w - THEME_PAD_X * 2,
+		panelSize.h - THEME_TITLE_BAND_H - THEME_PAD_TOP - THEME_PAD_BOTTOM
+			- THEME_BTNROW_H - THEME_BTNROW_GAP );
+}
+
+int CMenuFramework::ButtonRowY() const
+{
+	return PanelPos().y + panelSize.h - THEME_PAD_BOTTOM - THEME_BTNROW_H;
+}
+
+Rect CMenuFramework::ColumnRect( int index, int count ) const
+{
+	Rect c = ContentRect();
+
+	if( count < 2 )
+		return c;
+
+	int gutter = ( count == 2 ) ? THEME_GUTTER2 : THEME_GUTTER3;
+	int w = ( c.sz.w - gutter * ( count - 1 )) / count;
+
+	return Rect( c.pt.x + index * ( w + gutter ), c.pt.y, w, c.sz.h );
+}
+
+// two columns with an explicit split, e.g. a 0.62 list and a 0.38 preview
+Rect CMenuFramework::ColumnRect2( int index, float firstFrac ) const
+{
+	Rect c = ContentRect();
+	int inner = c.sz.w - THEME_GUTTER2;
+	int w0 = (int)( inner * firstFrac );
+
+	if( index == 0 )
+		return Rect( c.pt.x, c.pt.y, w0, c.sz.h );
+
+	return Rect( c.pt.x + w0 + THEME_GUTTER2, c.pt.y, inner - w0, c.sz.h );
+}
+
+Rect CMenuFramework::RowRect( int index ) const
+{
+	Rect c = ContentRect();
+
+	return Rect( c.pt.x, c.pt.y + index * THEME_ROW_PITCH, c.sz.w, THEME_ROW_H );
+}
+
+/*
+=================
+CMenuFramework::LayoutButtonRow
+
+One right-aligned row along the bottom of the panel, left to right in the order
+given, so the primary action is simply the last one. Every menu in the tree that
+calls AddButton gets this through RealignButtons; menus that own their buttons
+as members call it directly.
+=================
+*/
+void CMenuFramework::LayoutButtonRow( CMenuPicButton **btns, int count )
+{
+	int widths[MAX_FRAMEWORK_PICBUTTONS];
+	int total = 0;
+
+	if( count <= 0 || count > MAX_FRAMEWORK_PICBUTTONS )
+		return;
+
+	for( int i = 0; i < count; i++ )
+	{
+		widths[i] = UI_ThemeButtonWidth( btns[i]->szName );
+		total += widths[i] + ( i ? THEME_BTN_GAP : 0 );
+	}
+
+	Rect c = ContentRect();
+	int x = c.pt.x + c.sz.w - total;
+	int y = ButtonRowY();
+
+	for( int i = 0; i < count; i++ )
+	{
+		btns[i]->eThemeStyle = CMenuPicButton::THEME_BOX;
+		btns[i]->eTextAlignment = QM_CENTER;
+		btns[i]->font = uiStatic.hThemeButton;
+		btns[i]->charSize = THEME_BUTTON_TALL;
+		btns[i]->SetRect( x, y, widths[i], THEME_BTNROW_H );
+		btns[i]->CalcPosition();
+		btns[i]->CalcSizes();
+
+		x += widths[i] + THEME_BTN_GAP;
+	}
+}
+
+CMenuFramework::CMenuCloseButton::CMenuCloseButton()
+{
+	iFlags = QMF_MOUSEONLY;
+	eFocusAnimation = QM_HIGHLIGHTIFFOCUS;
+	szName = "";
+	SetSize( THEME_CLOSE_SIZE, THEME_CLOSE_SIZE );
+}
+
+bool CMenuFramework::CMenuCloseButton::KeyDown( int key )
+{
+	if( UI::Key::IsLeftMouse( key ) && FBitSet( iFlags, QMF_HASMOUSEFOCUS ))
+	{
+		_Event( QM_PRESSED );
+		return true;
+	}
+	return false;
+}
+
+bool CMenuFramework::CMenuCloseButton::KeyUp( int key )
+{
+	if( UI::Key::IsLeftMouse( key ) && FBitSet( iFlags, QMF_HASMOUSEFOCUS ))
+	{
+		PlayLocalSound( uiStatic.sounds[SND_LAUNCH] );
+		_Event( QM_RELEASED );
+		return true;
+	}
+	return false;
+}
+
+void CMenuFramework::CMenuCloseButton::Draw()
+{
+	const bool hot = UI_THEME_HOT( this );
+	const int inset = m_scSize.w / 4;
+
+	if( hot )
+		UI_FillRect( m_scPos, m_scSize, THEME_HOVER_FILL );
+
+	UI_ThemeDrawCross( Point( m_scPos.x + inset, m_scPos.y + inset ),
+		Size( m_scSize.w - inset * 2, m_scSize.h - inset * 2 ),
+		hot ? THEME_TEXT_HI : THEME_TEXT );
 }
 
 CMenuFramework::~CMenuFramework()
@@ -49,6 +199,9 @@ void CMenuFramework::Draw()
 	static CMenuBaseItem *lastItem;
 	CMenuBaseItem *item;
 
+	if( IsPanel( ))
+		DrawPanelChrome();
+
 	BaseClass::Draw();
 
 	item = ItemAtCursor();
@@ -64,22 +217,82 @@ void CMenuFramework::Draw()
 	// draw status text if status text isn't rendered at right side of an item
 	// ideally, framework shouldn't even know about this but since everything
 	// WON-styled goes here, let's leave it here...
-	if( item && item == lastItem && !FBitSet( item->iFlags, QMF_NOTIFY ) && item->szStatusText != NULL )
+	//
+	// In theme mode no control draws its own status text any more (they all
+	// used EngFuncs::DrawConsoleString, the engine bitmap console font, which
+	// cannot match a TTF theme), so this single hint line owns all of them.
+	const bool themeStatus = UI_ThemeActive();
+
+	if( item && item == lastItem && ( themeStatus || !FBitSet( item->iFlags, QMF_NOTIFY )) && item->szStatusText != NULL )
 	{
 		float alpha = bound( 0, ((( uiStatic.realTime - statusFadeTime ) - 100 ) * 0.01f ), 1 );
 		int r, g, b, x, len;
 
-		EngFuncs::ConsoleStringLen( item->szStatusText, &len, NULL );
+		if( themeStatus )
+		{
+			Point pt;
+			Size sz;
 
-		UnpackRGB( r, g, b, uiColorHelp );
-		EngFuncs::DrawSetTextColor( r, g, b, alpha * 255 );
-		x = ( ScreenWidth - len ) * 0.5f; // centering
+			if( IsPanel( ))
+			{
+				Rect c = ContentRect();
 
-		EngFuncs::DrawConsoleString( x, uiStatic.yOffset + 720 * uiStatic.scaleY, item->szStatusText );
+				pt = Point( c.pt.x, ButtonRowY() + ( THEME_BTNROW_H - THEME_LABEL_TALL ) / 2 ).Scale();
+				sz = Size( c.sz.w - THEME_BTN_MIN_W, THEME_LABEL_TALL ).Scale();
+			}
+			else
+			{
+				pt = Point( THEME_MENU_MARGIN, 768 - 52 ).Scale();
+				sz = Size( uiStatic.width - THEME_MENU_MARGIN * 2, THEME_LABEL_TALL ).Scale();
+			}
+
+			pt = pt + m_scPos;
+
+			UI_DrawString( uiStatic.hThemeLabel, pt, sz, item->szStatusText,
+				PackAlpha( THEME_TEXT_DIM, (unsigned int)( alpha * 255 )),
+				(int)( THEME_LABEL_TALL * uiStatic.scaleY ), QM_LEFT,
+				ETF_FORCECOL | ETF_NO_WRAP );
+		}
+		else
+		{
+			EngFuncs::ConsoleStringLen( item->szStatusText, &len, NULL );
+
+			UnpackRGB( r, g, b, uiColorHelp );
+			EngFuncs::DrawSetTextColor( r, g, b, alpha * 255 );
+			x = ( ScreenWidth - len ) * 0.5f; // centering
+
+			EngFuncs::DrawConsoleString( x, uiStatic.yOffset + 720 * uiStatic.scaleY, item->szStatusText );
+		}
 	}
 	else statusFadeTime = uiStatic.realTime;
 }
 
+/*
+=================
+CMenuFramework::DrawPanelChrome
+
+Scrim, panel, hairline border, title band with its uppercase title, and the
+hairline above the button row. Everything is axis-aligned filled rectangles:
+the library has no rounded rectangle, no gradient and no line primitive - and
+the Source dialogs this follows are square anyway.
+=================
+*/
+void CMenuFramework::DrawPanelChrome()
+{
+	Point p = ( PanelPos().Scale() ) + m_scPos;
+	Size  s = panelSize.Scale();
+
+	// dim everything behind the dialog
+	UI_FillRect( 0, 0, ScreenWidth, ScreenHeight, THEME_SCRIM );
+
+	UI_ThemeDrawDialogPanel( p, s, szPanelTitle );
+
+	// hairline above the button row
+	UI_ThemeSeparator( p.x + (int)( THEME_PAD_X * uiStatic.scaleX ),
+		(int)(( ButtonRowY() - THEME_BTNROW_GAP / 2 ) * uiStatic.scaleY ) + m_scPos.y,
+		s.w - (int)( THEME_PAD_X * 2 * uiStatic.scaleX ), THEME_SEPARATOR );
+}
+
 void CMenuFramework::Hide()
 {
 	BaseClass::Hide();
@@ -92,6 +305,15 @@ void CMenuFramework::Init()
 	pos.y = uiStatic.yOffset;
 	size.w = uiStatic.width;
 	size.h = 768;
+
+	// _Init() has run by now, so a menu that asked for panel mode is known.
+	// The close button is added last so it draws over the title band.
+	if( IsPanel() && !bCloseAdded )
+	{
+		closeBtn.onReleased = VoidCb( &CMenuFramework::Hide );
+		AddItem( closeBtn );
+		bCloseAdded = true;
+	}
 }
 
 void CMenuFramework::VidInit()
@@ -100,7 +322,23 @@ void CMenuFramework::VidInit()
 	pos.y = uiStatic.yOffset;
 	size.w = uiStatic.width;
 	size.h = 768;
+
+	if( IsPanel( ))
+	{
+		// the WON banner and its slide transition have no place in a panel
+		banner.Hide();
+
+		Point p = PanelPos();
+
+		closeBtn.SetRect( p.x + panelSize.w - THEME_CLOSE_INSET - THEME_CLOSE_SIZE,
+			p.y + ( THEME_TITLE_BAND_H - THEME_CLOSE_SIZE ) / 2,
+			THEME_CLOSE_SIZE, THEME_CLOSE_SIZE );
+	}
+
 	BaseClass::VidInit();
+
+	if( IsPanel( ))
+		RealignButtons();
 }
 
 CMenuPicButton * CMenuFramework::AddButton(const char *szName, const char *szStatus, EDefaultBtns buttonPicId, CEventCallback onReleased, int iFlags)
@@ -149,6 +387,21 @@ CMenuPicButton * CMenuFramework::AddButton( const char *szName, const char *szSt
 
 void CMenuFramework::RealignButtons( void )
 {
+	if( IsPanel( ))
+	{
+		CMenuPicButton *visible[MAX_FRAMEWORK_PICBUTTONS];
+		int n = 0;
+
+		for( int i = 0; i < m_iBtnsNum; i++ )
+		{
+			if( m_apBtns[i]->IsVisible())
+				visible[n++] = m_apBtns[i];
+		}
+
+		LayoutButtonRow( visible, n );
+		return;
+	}
+
 	for( int i = 0, j = 0; i < m_iBtnsNum; i++ )
 	{
 		if( !m_apBtns[i]->IsVisible())
@@ -179,6 +432,11 @@ bool CMenuFramework::KeyDown( int key )
 
 void CMenuFramework::PrepareBannerAnimation( EAnimation direction, CMenuPicButton *initiator )
 {
+	// the theme has no banners, and no banner slide: the only animation it
+	// keeps is the window opacity cross-fade in CMenuBaseWindow::DrawAnimation
+	if( UI_ThemeActive( ))
+		return;
+
 	// banner is not present, ignore
 	if( banner.Parent() != this )
 		return;
@@ -255,6 +513,11 @@ void CMenuFramework::CMenuBannerBitmap::Draw()
 {
 	CMenuFramework *pParent = (CMenuFramework *)Parent();
 
+	// no banners in the theme: the panel title band carries the page name, and
+	// the fallback text path here needs the two blur fonts the theme drops
+	if( UI_ThemeActive( ))
+		return;
+
 	if (pParent && (pParent->bannerAnimDirection == ANIM_OPENING || pParent->eTransitionType == ANIM_OPENING))
 		return;
 
diff --git a/3rdparty/mainui/controls/Framework.h b/3rdparty/mainui/controls/Framework.h
index f1db70e..49a2e66 100644
--- a/3rdparty/mainui/controls/Framework.h
+++ b/3rdparty/mainui/controls/Framework.h
@@ -53,6 +53,47 @@ public:
 
 	void PrepareBannerAnimation( EAnimation direction, CMenuPicButton *initiator );
 
+	// -----------------------------------------------------------------------
+	// Source-style panel mode (Theme.h)
+	//
+	// The framework stays a full-screen holder and the panel is a rectangle
+	// drawn inside it, so every child keeps screen-virtual coordinates. That
+	// matters: several menus position lists with negative coordinates meaning
+	// "from the right/bottom edge of the parent" (LoadGame, Controls,
+	// CustomGame, CryOfFear), and turning the framework itself into the panel
+	// would silently re-anchor all of them.
+	//
+	// Layout helpers return absolute virtual units, ready for SetRect.
+	// -----------------------------------------------------------------------
+	void SetPanel( const char *title, int w, int h );
+	bool IsPanel() const { return bPanel && UI_ThemeActive(); }
+	Point PanelPos() const;
+	Size  PanelSize() const { return panelSize; }
+	Rect  ContentRect() const;
+	Rect  ColumnRect( int index, int count ) const;
+	Rect  ColumnRect2( int index, float firstFrac ) const;
+	int   ButtonRowY() const;
+
+	// A row in the content area: full width, THEME_ROW_H tall, pitched by
+	// THEME_ROW_PITCH from the top of the content rect.
+	Rect  RowRect( int index ) const;
+
+	void  DrawPanelChrome();
+
+	// right-aligned button row, left to right in the order given
+	void  LayoutButtonRow( CMenuPicButton **btns, int count );
+
+	// The panel close button. Two diagonals of filled squares, because there is
+	// no line primitive and Cry of Fear ships no gfx/shell artwork.
+	class CMenuCloseButton : public CMenuBaseItem
+	{
+	public:
+		CMenuCloseButton();
+		void Draw() override;
+		bool KeyUp( int key ) override;
+		bool KeyDown( int key ) override;
+	} closeBtn;
+
 	class CMenuBannerBitmap : public CMenuBaseItem
 	{
 	public:
@@ -72,6 +113,11 @@ protected:
 
 	CMenuPicButton *m_apBtns[MAX_FRAMEWORK_PICBUTTONS];
 	int m_iBtnsNum;
+
+	bool bPanel;
+	bool bCloseAdded;
+	Size panelSize;
+	const char *szPanelTitle;
 };
 
 #endif // FRAMEWORK_H
diff --git a/3rdparty/mainui/controls/MessageBox.cpp b/3rdparty/mainui/controls/MessageBox.cpp
index b1c7b65..1f549d3 100644
--- a/3rdparty/mainui/controls/MessageBox.cpp
+++ b/3rdparty/mainui/controls/MessageBox.cpp
@@ -22,6 +22,7 @@ GNU General Public License for more details.
 CMenuMessageBox::CMenuMessageBox(const char *name) : BaseClass( name )
 {
 	iFlags |= QMF_INACTIVE;
+	szPanelTitle = "NOTICE";
 }
 
 void CMenuMessageBox::_Init()
@@ -31,11 +32,65 @@ void CMenuMessageBox::_Init()
 	dlgMessage.SetCoord( 0, 0 );
 	dlgMessage.size = size;
 
+	if( UI_ThemeActive( ))
+	{
+		// keep the message clear of the title band
+		dlgMessage.iFlags = QMF_INACTIVE;
+		dlgMessage.SetCoord( THEME_PAD_X, THEME_TITLE_BAND_H + THEME_PAD_TOP );
+		dlgMessage.size.w = size.w - THEME_PAD_X * 2;
+		dlgMessage.size.h = size.h - THEME_TITLE_BAND_H - THEME_PAD_TOP - THEME_PAD_BOTTOM;
+	}
+
 	AddItem( dlgMessage );
 }
 
+/*
+==============
+CMenuMessageBox::_VidInit
+
+_Init froze the message rect from whatever size the owner had set at that
+moment, so a later SetRect never reached the text and a long notice was
+ellipsised. In theme mode the panel is sized to the wrapped message instead,
+centred like every other dialog; the owner's rect is only used without the
+theme.
+==============
+*/
+void CMenuMessageBox::_VidInit()
+{
+	if( !UI_ThemeActive( ))
+		return;
+
+	int w = 520, h = 200;
+
+	UI_ThemeMessagePanelSize( dlgMessage.szName, &w, &h );
+
+	// no button row here, so the message keeps that space
+	h -= THEME_BTNROW_H + THEME_BTNROW_GAP;
+	if( h < THEME_TITLE_BAND_H + THEME_PAD_TOP + THEME_PAD_BOTTOM + THEME_ROW_H )
+		h = THEME_TITLE_BAND_H + THEME_PAD_TOP + THEME_PAD_BOTTOM + THEME_ROW_H;
+
+	SetRect(( uiStatic.width - w ) / 2, ( 768 - h ) / 2, w, h );
+	pos.x += uiStatic.xOffset;
+	pos.y += uiStatic.yOffset;
+
+	// QM_TOP keeps the horizontal centring (QM_CENTER is 0) but uses the whole
+	// height, unlike UI_DrawString's vertically centred path
+	dlgMessage.eTextAlignment = QM_TOP;
+	dlgMessage.SetRect( THEME_PAD_X, THEME_TITLE_BAND_H + THEME_PAD_TOP,
+		w - THEME_PAD_X * 2, h - THEME_TITLE_BAND_H - THEME_PAD_TOP - THEME_PAD_BOTTOM );
+	dlgMessage.font = uiStatic.hThemeBody;
+	dlgMessage.charSize = THEME_BODY_TALL;
+}
+
 void CMenuMessageBox::Draw()
 {
+	if( UI_ThemeActive( ))
+	{
+		UI_ThemeDrawDialogPanel( m_scPos, m_scSize, szPanelTitle );
+		BaseClass::Draw();
+		return;
+	}
+
 	EngFuncs::FillRGBA( m_scPos.x, m_scPos.y, m_scSize.w, m_scSize.h, 20, 20, 20, 235 );
 	UI_DrawRectangle( m_scPos, m_scSize, uiInputFgColor );
 	BaseClass::Draw();
diff --git a/3rdparty/mainui/controls/MessageBox.h b/3rdparty/mainui/controls/MessageBox.h
index 02aeaef..53080a4 100644
--- a/3rdparty/mainui/controls/MessageBox.h
+++ b/3rdparty/mainui/controls/MessageBox.h
@@ -26,8 +26,12 @@ public:
 	CMenuMessageBox( const char *name = "Unnamed MessageBox" );
 
 	void SetMessage( const char *sz );
+
+	// title shown in the theme's title band
+	const char *szPanelTitle;
 private:
 	void _Init() override;
+	void _VidInit() override;
 	void Draw() override;
 
 	CMenuAction dlgMessage;
diff --git a/3rdparty/mainui/controls/PicButton.cpp b/3rdparty/mainui/controls/PicButton.cpp
index ef9e268..a83fdff 100644
--- a/3rdparty/mainui/controls/PicButton.cpp
+++ b/3rdparty/mainui/controls/PicButton.cpp
@@ -118,7 +118,52 @@ CMenuPicButton::CMenuPicButton() : BaseClass()
 
 	size = uiStatic.buttons_draw_size;
 
+	eThemeStyle = THEME_PLAIN;
+	bThemeSelected = false;
+
 	SetCharSize( QM_DEFAULTFONT );
+
+	if( UI_ThemeActive( ))
+	{
+		// no drop shadow: ETF_SHADOW offsets by scaleX*3, ~4px at 1080p and
+		// 8px at 2160p, which is nothing like a Source drop shadow
+		iFlags = 0;
+		bEnableTransitions = false;
+		charSize = THEME_ITEM_TALL;
+	}
+}
+
+/*
+=================
+CMenuPicButton::VidInit
+
+The theme font handles only exist after CFontManager::VidInit, which runs after
+every menu has been constructed, so pick the handle here rather than in the
+constructor. A menu that set its own font keeps it.
+=================
+*/
+void CMenuPicButton::VidInit( void )
+{
+	if( UI_ThemeActive( ))
+	{
+		switch( eThemeStyle )
+		{
+		case THEME_BOX:
+			font = uiStatic.hThemeButton;
+			charSize = THEME_BUTTON_TALL;
+			break;
+		case THEME_ROW:
+			font = uiStatic.hThemeBody;
+			charSize = THEME_BODY_TALL;
+			break;
+		default:
+			font = uiStatic.hThemeItem;
+			charSize = THEME_ITEM_TALL;
+			break;
+		}
+	}
+
+	BaseClass::VidInit();
 }
 
 /*
@@ -219,6 +264,79 @@ void CMenuPicButton::DrawButton( int r, int g, int b, int a, wrect_t *rects, int
 	EngFuncs::PIC_DrawAdditive( m_scPos, uiStatic.buttons_draw_size.Scale(), &rects[state] );
 }
 
+/*
+=================
+CMenuPicButton::DrawTheme
+
+The Source-style path. No glow, no 512 ms decay, no colour pulse, no motion:
+the item under the cursor simply goes from THEME_TEXT to THEME_TEXT_HI, and
+rows and buttons pick up a very low-alpha fill. The status text is not drawn
+here at all - CMenuFramework draws one hint line for the whole page in the
+theme font instead of the engine bitmap console font.
+=================
+*/
+void CMenuPicButton::DrawTheme( int state )
+{
+	const bool grayed = FBitSet( iFlags, QMF_GRAYED ) != 0;
+	// Highlight follows the focus flags, never the holder's cursor index:
+	// CMenuItemsHolder::MouseMove leaves m_iCursor on the last item the pointer
+	// touched, so "this == ItemAtCursor()" keeps an item lit after the cursor
+	// has left it. QMF_HASMOUSEFOCUS is cleared for every item the cursor is
+	// not inside, and QMF_HASKEYBOARDFOCUS follows arrow-key navigation, which
+	// is exactly the state the stock pic-button path draws from.
+	const bool hot = !grayed && ( state != BUTTON_NOFOCUS );
+	unsigned int color;
+	uint textflags = ETF_FORCECOL | ETF_NO_WRAP;
+
+	if( grayed )
+		color = THEME_DISABLED;
+	else if( bThemeSelected )
+		color = THEME_ACCENT;
+	else if( hot )
+		color = THEME_TEXT_HI;
+	else
+		color = colorBase;
+
+	if( eThemeStyle == THEME_BOX )
+	{
+		UI_FillRect( m_scPos, m_scSize, m_bPressed ? THEME_PRESS_FILL :
+			( hot ? THEME_HOVER_FILL : THEME_INPUT_BG ));
+		UI_DrawHairline( m_scPos, m_scSize,
+			grayed ? THEME_DISABLED : ( hot ? THEME_ACCENT : THEME_INPUT_BORDER ));
+	}
+	else if( eThemeStyle == THEME_ROW )
+	{
+		if( bThemeSelected )
+			UI_FillRect( m_scPos, m_scSize, THEME_SELECT_FILL );
+		else if( m_bPressed )
+			UI_FillRect( m_scPos, m_scSize, THEME_PRESS_FILL );
+		else if( hot )
+			UI_FillRect( m_scPos, m_scSize, THEME_HOVER_FILL );
+	}
+	else
+	{
+		SetBits( textflags, ETF_NOSIZELIMIT );
+	}
+
+	Point pt = m_scPos;
+	Size sz = m_scSize;
+
+	if( eThemeStyle != THEME_PLAIN )
+	{
+		// vertically centre the label inside the row or the button box
+		pt.y += ( m_scSize.h - m_scChSize ) / 2;
+		sz.h = m_scChSize;
+
+		if( eThemeStyle == THEME_ROW )
+		{
+			pt.x += (int)( 10 * uiStatic.scaleX );
+			sz.w -= (int)( 20 * uiStatic.scaleX );
+		}
+	}
+
+	UI_DrawString( font, pt, sz, szName, color, m_scChSize, eTextAlignment, textflags );
+}
+
 /*
 =================
 CMenuPicButton::Draw
@@ -240,6 +358,13 @@ void CMenuPicButton::Draw( )
 	if( iOldState == BUTTON_NOFOCUS && state != BUTTON_NOFOCUS )
 		iFocusStartTime = uiStatic.realTime;
 
+	if( UI_ThemeActive( ))
+	{
+		DrawTheme( state );
+		iOldState = state;
+		return;
+	}
+
 	if( szStatusText && FBitSet( iFlags, QMF_NOTIFY ) && !FBitSet( gMenu.m_gameinfo.flags, GFL_NOSKILLS ))
 	{
 		Point coord;
diff --git a/3rdparty/mainui/controls/PicButton.h b/3rdparty/mainui/controls/PicButton.h
index abd475f..879a5f8 100644
--- a/3rdparty/mainui/controls/PicButton.h
+++ b/3rdparty/mainui/controls/PicButton.h
@@ -36,6 +36,7 @@ public:
 	CMenuPicButton();
 	bool KeyUp( int key ) override;
 	bool KeyDown( int key ) override;
+	void VidInit( void ) override;
 	void Draw( void ) override;
 	bool HotKey( int key ) override;
 
@@ -44,6 +45,18 @@ public:
 
 	bool bEnableTransitions;
 	bool bPulse;
+
+	// How the Source-style theme draws this button (Theme.h). Ignored when
+	// ui_theme is 0, which keeps the WON two-pass Gaussian glow.
+	enum EThemeStyle
+	{
+		THEME_PLAIN = 0, // bare left-aligned text: the main and pause menu list
+		THEME_ROW,       // full-width selectable row inside a panel
+		THEME_BOX        // a button in the panel's bottom-right row
+	};
+
+	EThemeStyle eThemeStyle;
+	bool bThemeSelected; // draws as the current choice: accent text + fill
 private:
 	bool bRollOver;
 
@@ -51,6 +64,7 @@ private:
 	void _Event( int ev ) override;
 
 	void DrawButton( int r, int g, int b, int a, wrect_t *rects, int state );
+	void DrawTheme( int state );
 
 	HIMAGE hPic;
 	int hotkey;
diff --git a/3rdparty/mainui/controls/Slider.cpp b/3rdparty/mainui/controls/Slider.cpp
index 1159ec9..43a2bae 100644
--- a/3rdparty/mainui/controls/Slider.cpp
+++ b/3rdparty/mainui/controls/Slider.cpp
@@ -51,6 +51,17 @@ void CMenuSlider::VidInit(  )
 	colorBase.SetDefault( uiColorWhite );
 	colorFocus.SetDefault( uiColorWhite );
 
+	if( UI_ThemeActive( ))
+	{
+		// a 6-unit track in a 26-unit hit box, instead of 2 + 6*2 = 14
+		if( size.h == 2 + m_iSliderOutlineWidth * 2 )
+			size.h = THEME_SLIDER_H;
+
+		font = uiStatic.hThemeLabel;
+		charSize = THEME_LABEL_TALL;
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	BaseClass::VidInit();
 
 	// scale the center box
@@ -153,7 +164,8 @@ void CMenuSlider::Draw( void )
 	int	textHeight, sliderX;
 	uint textflags = ( iFlags & QMF_DROPSHADOW ) ? ETF_SHADOW : 0;
 
-	if( szStatusText && iFlags & QMF_NOTIFY )
+	// in theme mode the framework draws one hint line in the theme font
+	if( szStatusText && iFlags & QMF_NOTIFY && !UI_ThemeActive( ))
 	{
 		Point coord;
 
@@ -175,9 +187,23 @@ void CMenuSlider::Draw( void )
 		{
 			int	dist, numSteps;
 
-			// move slider follow the holded mouse button
-			dist = uiStatic.cursorX - m_scPos.x - m_iSliderOutlineWidth - (m_scCenterBox.w/2);
-			numSteps = floor(dist / m_flDrawStep);
+			if( UI_ThemeActive( ))
+			{
+				// the theme knob is 10 virtual units wide and the track spans
+				// the whole control, so map the cursor straight onto it
+				const int knobW = (int)( 10 * uiStatic.scaleX );
+				float frac = (float)( uiStatic.cursorX - m_scPos.x - knobW / 2 ) /
+					(float)( m_scSize.w - knobW );
+
+				frac = bound( 0.0f, frac, 1.0f );
+				numSteps = (int)(( frac * ( m_flMaxValue - m_flMinValue )) / m_flRange + 0.5f );
+			}
+			else
+			{
+				// move slider follow the holded mouse button
+				dist = uiStatic.cursorX - m_scPos.x - m_iSliderOutlineWidth - (m_scCenterBox.w/2);
+				numSteps = floor(dist / m_flDrawStep);
+			}
 			m_flCurValue = bound( m_flMinValue, numSteps * m_flRange + m_flMinValue, m_flMaxValue );
 
 			// tell menu about changes
@@ -195,6 +221,28 @@ void CMenuSlider::Draw( void )
 		* ( m_scSize.w - m_iSliderOutlineWidth - (m_scCenterBox.w) );
 
 
+	if( UI_ThemeActive( ))
+	{
+		// UI_SLIDER_MAIN is gfx/shell artwork this game does not ship, so the
+		// knob is a filled rectangle and the track a filled bar.
+		const bool hot = UI_THEME_HOT( this );
+		const int trackH = (int)( THEME_SLIDER_TRACK * uiStatic.scaleY );
+		const int trackY = m_scPos.y + ( m_scSize.h - trackH ) / 2;
+		const int knobW = (int)( 10 * uiStatic.scaleX );
+		int knobX = m_scPos.x + ( ( m_flCurValue - m_flMinValue ) / ( m_flMaxValue - m_flMinValue ) )
+			* ( m_scSize.w - knobW );
+
+		UI_FillRect( m_scPos.x, trackY, m_scSize.w, trackH, THEME_TRACK );
+		UI_FillRect( m_scPos.x, trackY, knobX - m_scPos.x + knobW / 2, trackH,
+			PackAlpha( THEME_ACCENT, 128 ));
+		UI_FillRect( knobX, m_scPos.y, knobW, m_scSize.h, hot ? THEME_TEXT_HI : THEME_ACCENT );
+
+		textHeight = m_scPos.y - (m_scChSize * 1.5f);
+		UI_DrawString( font, m_scPos.x, textHeight, m_scSize.w, m_scChSize, szName,
+			THEME_TEXT_DIM, m_scChSize, eTextAlignment, ETF_FORCECOL );
+		return;
+	}
+
 	UI_DrawRectangleExt( m_scPos.x + m_iSliderOutlineWidth / 2, m_scPos.y + m_iSliderOutlineWidth, m_scSize.w - m_iSliderOutlineWidth, m_scCenterBox.h, uiInputBgColor, m_iSliderOutlineWidth );
 	if( eFocusAnimation == QM_HIGHLIGHTIFFOCUS && this == m_pParent->ItemAtCursor())
 		UI_DrawPic( sliderX, m_scPos.y, m_scCenterBox.w, m_scSize.h, uiColorHelp, imgSlider );
diff --git a/3rdparty/mainui/controls/SpinControl.cpp b/3rdparty/mainui/controls/SpinControl.cpp
index 79bd960..dbf4097 100644
--- a/3rdparty/mainui/controls/SpinControl.cpp
+++ b/3rdparty/mainui/controls/SpinControl.cpp
@@ -44,11 +44,65 @@ CMenuSpinControl::Init
 */
 void CMenuSpinControl::VidInit( void )
 {
+	if( UI_ThemeActive( ))
+	{
+		colorBase.SetDefault( THEME_TEXT );
+		font = uiStatic.hThemeBody;
+		charSize = THEME_BODY_TALL;
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	colorBase.SetDefault( uiColorHelp );
 
 	BaseClass::VidInit();
 }
 
+/*
+=================
+CMenuSpinControl::DrawTheme
+
+The arrows are gfx/shell artwork this game does not ship. Draw them as solid
+triangles inside the control's own frame instead, so the whole widget is one
+box with a value in the middle - which is also what the Source references do.
+=================
+*/
+void CMenuSpinControl::DrawTheme( void )
+{
+	const bool grayed = FBitSet( iFlags, QMF_GRAYED ) != 0;
+	const bool focus = !grayed && UI_THEME_HOT( this );
+	Size arrow( m_scSize.h, m_scSize.h );
+	Point left( m_scPos.x, m_scPos.y );
+	Point right( m_scPos.x + m_scSize.w - arrow.w, m_scPos.y );
+	Point valuePos( m_scPos.x + arrow.w, m_scPos.y );
+	Size valueSize( m_scSize.w - arrow.w * 2, m_scSize.h );
+	const int inset = arrow.w / 3;
+	Size tri( arrow.w - inset * 2, arrow.h - inset * 2 );
+
+	// label above the control, the existing idiom
+	UI_DrawString( uiStatic.hThemeLabel, m_scPos.x, m_scPos.y - (int)( THEME_LABEL_TALL * 1.5f * uiStatic.scaleY ),
+		m_scSize.w, (int)( THEME_LABEL_TALL * uiStatic.scaleY ), szName, THEME_TEXT_DIM,
+		(int)( THEME_LABEL_TALL * uiStatic.scaleY ), QM_LEFT, ETF_FORCECOL | ETF_NOSIZELIMIT );
+
+	UI_FillRect( m_scPos, m_scSize, THEME_INPUT_BG );
+	UI_DrawHairline( m_scPos, m_scSize,
+		grayed ? THEME_DISABLED : ( focus ? THEME_ACCENT : THEME_INPUT_BORDER ));
+
+	const bool leftHot = !grayed && UI_CursorInRect( left, arrow );
+	const bool rightHot = !grayed && UI_CursorInRect( right, arrow );
+
+	UI_ThemeDrawTriangle( Point( left.x + inset, left.y + inset ), tri, 0,
+		grayed ? THEME_DISABLED : ( leftHot ? THEME_ACCENT : THEME_TEXT ));
+	UI_ThemeDrawTriangle( Point( right.x + inset, right.y + inset ), tri, 1,
+		grayed ? THEME_DISABLED : ( rightHot ? THEME_ACCENT : THEME_TEXT ));
+
+	UI::Scissor::PushScissor( valuePos, valueSize );
+	UI_DrawString( font, Point( valuePos.x, valuePos.y + ( valueSize.h - m_scChSize ) / 2 ),
+		Size( valueSize.w, m_scChSize ), m_szDisplay,
+		grayed ? THEME_DISABLED : ( focus ? THEME_TEXT_HI : colorBase ),
+		m_scChSize, eTextAlignment, ETF_FORCECOL | ETF_NO_WRAP );
+	UI::Scissor::PopScissor();
+}
+
 bool CMenuSpinControl::KeyDown( int key )
 {
 	const char *sound = 0;
@@ -127,7 +181,8 @@ void CMenuSpinControl::Draw( void )
 	Size scCenterBox;
 	uint textflags = ( iFlags & QMF_DROPSHADOW ) ? ETF_SHADOW : 0;
 
-	if( szStatusText && iFlags & QMF_NOTIFY )
+	// in theme mode the framework draws one hint line in the theme font
+	if( szStatusText && iFlags & QMF_NOTIFY && !UI_ThemeActive( ))
 	{
 		Point coord;
 
@@ -141,6 +196,12 @@ void CMenuSpinControl::Draw( void )
 		EngFuncs::DrawConsoleString( coord, szStatusText );
 	}
 
+	if( UI_ThemeActive( ))
+	{
+		DrawTheme();
+		return;
+	}
+
 	int textHeight = m_scPos.y - (m_scChSize * 1.5f);
 	UI_DrawString( font, m_scPos.x - UI_OUTLINE_WIDTH, textHeight, m_scSize.w + UI_OUTLINE_WIDTH * 2, m_scChSize, szName, uiColorHelp, m_scChSize, QM_LEFT, textflags | ETF_FORCECOL | ETF_NOSIZELIMIT );
 
diff --git a/3rdparty/mainui/controls/SpinControl.h b/3rdparty/mainui/controls/SpinControl.h
index 1829837..623c69c 100644
--- a/3rdparty/mainui/controls/SpinControl.h
+++ b/3rdparty/mainui/controls/SpinControl.h
@@ -47,6 +47,7 @@ public:
 	void ForceDisplayString( const char *display );
 
 private:
+	void DrawTheme( void );
 	const char *MoveLeft();
 	const char *MoveRight();
 	void Display();
diff --git a/3rdparty/mainui/controls/Switch.cpp b/3rdparty/mainui/controls/Switch.cpp
index e3e0063..364fe70 100644
--- a/3rdparty/mainui/controls/Switch.cpp
+++ b/3rdparty/mainui/controls/Switch.cpp
@@ -87,6 +87,20 @@ int CMenuSwitch::IsNewStateByMouseClick()
 
 void CMenuSwitch::VidInit()
 {
+	if( UI_ThemeActive( ))
+	{
+		// a Source-style segmented control: the selected segment carries the
+		// accent fill and accent text, the rest is the input background
+		iSelectColor.SetDefault( THEME_SELECT_FILL );
+		iBackgroundColor.SetDefault( THEME_INPUT_BG );
+		iFgTextColor.SetDefault( THEME_ACCENT );
+		iBgTextColor.SetDefault( THEME_TEXT );
+		colorFocus.SetDefault( THEME_HOVER_FILL );
+		font = uiStatic.hThemeBody;
+		charSize = THEME_BODY_TALL;
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	iSelectColor.SetDefault( uiPromptTextColor );
 	iBackgroundColor.SetDefault( uiColorBlack );
 	iFgTextColor.SetDefault( uiInputFgColor );
@@ -207,7 +221,8 @@ void CMenuSwitch::Draw( void )
 	uint selectColor = iSelectColor;
 	UI_DrawString( font, m_scTextPos, m_scTextSize, szName, uiColorHelp, m_scChSize, eTextAlignment, textflags | ETF_FORCECOL );
 
-	if( szStatusText && iFlags & QMF_NOTIFY )
+	// in theme mode the framework draws one hint line in the theme font
+	if( szStatusText && iFlags & QMF_NOTIFY && !UI_ThemeActive( ) )
 	{
 		Point coord;
 
diff --git a/3rdparty/mainui/controls/Table.cpp b/3rdparty/mainui/controls/Table.cpp
index 902da98..1acf14e 100644
--- a/3rdparty/mainui/controls/Table.cpp
+++ b/3rdparty/mainui/controls/Table.cpp
@@ -45,6 +45,13 @@ CMenuTable::CMenuTable() : BaseClass(),
 
 void CMenuTable::VidInit()
 {
+	if( UI_ThemeActive( ))
+	{
+		font = uiStatic.hThemeBody;
+		charSize = THEME_BODY_TALL;
+		iFlags &= ~QMF_DROPSHADOW;
+	}
+
 	BaseClass::VidInit();
 
 	iBackgroundColor.SetDefault( uiColorBlack );
@@ -202,25 +209,58 @@ bool CMenuTable::MoveView(int delta )
 
 bool CMenuTable::MoveCursor(int delta)
 {
+	const int step = delta < 0 ? -1 : 1;
+
 	iCurItem += delta;
 
+	// step over section captions: they are boundaries, not rows
+	while( iCurItem >= 0 && iCurItem < m_pModel->GetRows() &&
+		m_pModel->IsLineSectionHeader( iCurItem ))
+	{
+		iCurItem += step;
+	}
+
 	if( iCurItem < 0 )
 	{
 		iCurItem = 0;
+		SkipSectionHeaders( 1 );
 		return false;
 	}
 	else if( iCurItem > m_pModel->GetRows() - 1 )
 	{
 		iCurItem = m_pModel->GetRows() - 1;
+		SkipSectionHeaders( -1 );
 		return false;
 	}
 	return true;
 }
 
+/*
+=================
+CMenuTable::SkipSectionHeaders
+
+Move the cursor off a section caption in the given direction, without running
+off either end of the list.
+=================
+*/
+void CMenuTable::SkipSectionHeaders( int dir )
+{
+	const int rows = m_pModel->GetRows();
+
+	while( iCurItem >= 0 && iCurItem < rows && m_pModel->IsLineSectionHeader( iCurItem ))
+		iCurItem += dir;
+
+	iCurItem = bound( 0, iCurItem, rows > 0 ? rows - 1 : 0 );
+}
+
 void CMenuTable::SetCurrentIndex( int idx )
 {
 	iCurItem = bound( 0, idx, m_pModel->GetRows() );
 
+	// never land on a section caption
+	if( m_pModel->GetRows() > 0 && m_pModel->IsLineSectionHeader( iCurItem ))
+		SkipSectionHeaders( 1 );
+
 	if( iCurItem < iTopItem )
 		iTopItem = iCurItem;
 	if( iNumRows ) // check if already vidinit
@@ -282,7 +322,7 @@ bool CMenuTable::KeyUp( int key )
 					int offsety = uiStatic.cursorY - starty;
 					int newCur = iTopItem + offsety / m_scChSize;
 
-					if( newCur < m_pModel->GetRows() )
+					if( newCur < m_pModel->GetRows() && !m_pModel->IsLineSectionHeader( newCur ))
 					{
 						if( newCur == iCurItem )
 						{
@@ -517,6 +557,11 @@ void CMenuTable::DrawLine( Point p, int line, uint textColor, bool forceCol, uin
 
 		textflags |= iFlags & QMF_DROPSHADOW ? ETF_SHADOW : 0;
 
+		// ^N colour codes live inside the cell text itself (Cry of Fear's own
+		// gfx/shell/kb_act.lst is full of them); the theme wants one row colour
+		if( forceCol )
+			textflags |= ETF_FORCECOL;
+
 		if( columns[i].fStaticWidth )
 			sz.w = columns[i].flWidth * uiStatic.scaleX;
 		else
@@ -605,7 +650,11 @@ void CMenuTable::DrawLine( Point p, int line, uint textColor, bool forceCol, uin
 void CMenuTable::Draw()
 {
 	int i, y;
-	int selColor = PackRGB( 80, 56, 24 );
+	// the WON selection and hover colours were hardcoded here and unreachable
+	// from anywhere; the theme owns them now
+	const bool theme = UI_ThemeActive();
+	int selColor = theme ? THEME_SELECT_FILL : PackRGB( 80, 56, 24 );
+	int hoverColor = theme ? THEME_HOVER_FILL : 0x80383838;
 	int upFocus, downFocus, scrollbarFocus;
 
 	// HACKHACK: recalc iNumRows, to be not greater than iNumItems
@@ -685,7 +734,7 @@ void CMenuTable::Draw()
 
 	// draw the arrows base
 	UI_FillRect( upArrow.x, upArrow.y + arrow.h,
-		arrow.w, downArrow.y - upArrow.y - arrow.h, uiInputFgColor );
+		arrow.w, downArrow.y - upArrow.y - arrow.h, theme ? THEME_INPUT_BG : uiInputFgColor );
 
 	// ADAMIX
 	if( iScrollBarSliding )
@@ -696,7 +745,27 @@ void CMenuTable::Draw()
 
 	// ADAMIX END
 	// draw the arrows
-	if( iFlags & QMF_GRAYED )
+	if( theme )
+	{
+		// gfx/shell/uparrowd and friends are artwork this game does not ship
+		const bool grayed = FBitSet( iFlags, QMF_GRAYED ) != 0;
+		const int inset = arrow.w / 3;
+		Size tri( arrow.w - inset * 2, arrow.h - inset * 2 );
+
+		scrollbarFocus = UI_CursorInRect( sbarPos, sbarSize ) || iScrollBarSliding;
+
+		UI_FillRect( sbarPos, sbarSize, grayed ? THEME_DISABLED :
+			( scrollbarFocus ? THEME_ACCENT : THEME_INPUT_BORDER ));
+
+		upFocus = !grayed && UI_CursorInRect( upArrow, arrow );
+		downFocus = !grayed && UI_CursorInRect( downArrow, arrow );
+
+		UI_ThemeDrawTriangle( Point( upArrow.x + inset, upArrow.y + inset ), tri, 2,
+			grayed ? THEME_DISABLED : ( upFocus ? THEME_ACCENT : THEME_TEXT ));
+		UI_ThemeDrawTriangle( Point( downArrow.x + inset, downArrow.y + inset ), tri, 3,
+			grayed ? THEME_DISABLED : ( downFocus ? THEME_ACCENT : THEME_TEXT ));
+	}
+	else if( iFlags & QMF_GRAYED )
 	{
 		UI_DrawPic( upArrow, arrow, uiColorDkGrey, szUpArrow );
 		UI_DrawPic( downArrow, arrow, uiColorDkGrey, szDownArrow );
@@ -747,9 +816,33 @@ void CMenuTable::Draw()
 	for( i = iTopItem; i < m_pModel->GetRows() && i < iNumRows + iTopItem; i++, y += m_scChSize )
 	{
 		int color = colorBase; // predict state
-		bool forceCol = false;
+		// the theme owns the row colour: ^N colour codes inside a model's text
+		// (Cry of Fear's own gfx/shell/kb_act.lst is full of them) would
+		// otherwise paint the list in WON amber and cyan
+		bool forceCol = theme;
 		int fillColor = 0;
 
+		// a section boundary, not a row: a small uppercase caption with a
+		// hairline under it instead of the list file's row of '=' characters
+		if( theme && m_pModel->IsLineSectionHeader( i ))
+		{
+			const int capH = (int)( THEME_LABEL_TALL * uiStatic.scaleY );
+			const char *caption = m_pModel->GetCellText( i, 0 );
+
+			if( caption && caption[0] )
+			{
+				UI_DrawString( uiStatic.hThemeLabel,
+					Point( boxPos.x + (int)( 4 * uiStatic.scaleX ), y + ( m_scChSize - capH ) / 2 ),
+					Size( boxSize.w - (int)( 8 * uiStatic.scaleX ), capH ),
+					caption, THEME_TEXT_DIM, capH, QM_LEFT,
+					ETF_FORCECOL | ETF_NO_WRAP | ETF_NOSIZELIMIT );
+			}
+
+			UI_ThemeSeparator( boxPos.x, y + m_scChSize - UI_ThemeHairline(),
+				boxSize.w, THEME_SEPARATOR );
+			continue;
+		}
+
 		if( iFlags & QMF_GRAYED )
 		{
 			color = uiColorDkGrey;
@@ -768,7 +861,7 @@ void CMenuTable::Draw()
 			}
 			else if( i == iHighlight )
 			{
-				fillColor = 0x80383838;
+				fillColor = hoverColor;
 			}
 		}
 
diff --git a/3rdparty/mainui/controls/Table.h b/3rdparty/mainui/controls/Table.h
index e38f09d..244cc68 100644
--- a/3rdparty/mainui/controls/Table.h
+++ b/3rdparty/mainui/controls/Table.h
@@ -57,6 +57,7 @@ public:
 	bool MouseMove( int x, int y ) override;
 	bool MoveView( int delta );
 	bool MoveCursor( int delta );
+	void SkipSectionHeaders( int dir );
 	int GetCurrentIndex() { return iCurItem; }
 	void SetCurrentIndex( int idx );
 	int GetSortingColumn( void ) { return m_iSortingColumn; }
diff --git a/3rdparty/mainui/controls/YesNoMessageBox.cpp b/3rdparty/mainui/controls/YesNoMessageBox.cpp
index 9d78f05..587d09e 100644
--- a/3rdparty/mainui/controls/YesNoMessageBox.cpp
+++ b/3rdparty/mainui/controls/YesNoMessageBox.cpp
@@ -27,6 +27,7 @@ CMenuYesNoMessageBox::CMenuYesNoMessageBox( bool alert ) : BaseClass( "YesNoMess
 	bAutoHide = true;
 	m_bIsAlert = alert;
 	iFlags |= QMF_DIALOG;
+	szPanelTitle = alert ? "NOTICE" : "CONFIRM";
 
 	dlgMessage1.iFlags = QMF_INACTIVE|QMF_DROPSHADOW;
 	dlgMessage1.eTextAlignment = QM_TOP;
@@ -105,6 +106,52 @@ CMenuYesNoMessageBox::VidInit
 */
 void CMenuYesNoMessageBox::_VidInit()
 {
+	if( UI_ThemeActive( ))
+	{
+		// a centred Source panel: title band, message in the content rect,
+		// button row bottom-right with the primary action on the right.
+		// The panel is sized to the wrapped message (UI_ThemeMessagePanelSize),
+		// not fixed, so a two-sentence warning no longer runs out of lines and
+		// ellipsises itself into the button row.
+		int w = 520, h = 200;
+
+		UI_ThemeMessagePanelSize( dlgMessage1.szName, &w, &h );
+
+		SetRect(( uiStatic.width - w ) / 2, ( 768 - h ) / 2, w, h );
+		pos.x += uiStatic.xOffset;
+		pos.y += uiStatic.yOffset;
+		CalcPosition();
+		CalcSizes();
+
+		// QM_TOP, not QM_CENTER: UI_DrawString's vertically centred path starts
+		// halfway down the rect and still stops a line short of its bottom, so
+		// it fits only about half as many lines as the rect has room for - which
+		// is why a wrapped message ran into the button row. QM_TOP keeps the
+		// horizontal centring (QM_CENTER is 0) and uses the whole height.
+		dlgMessage1.eTextAlignment = QM_TOP;
+		dlgMessage1.SetRect( THEME_PAD_X, THEME_TITLE_BAND_H + THEME_PAD_TOP,
+			w - THEME_PAD_X * 2,
+			h - THEME_TITLE_BAND_H - THEME_PAD_TOP - THEME_PAD_BOTTOM
+				- THEME_BTNROW_H - THEME_BTNROW_GAP );
+		dlgMessage1.font = uiStatic.hThemeBody;
+		dlgMessage1.charSize = THEME_BODY_TALL;
+		dlgMessage1.iFlags &= ~QMF_DROPSHADOW;
+
+		const int wYes = UI_ThemeButtonWidth( yes.szName );
+		const int wNo  = UI_ThemeButtonWidth( no.szName );
+		const int right = w - THEME_PAD_X;
+		const int rowY = h - THEME_PAD_BOTTOM - THEME_BTNROW_H;
+
+		yes.eThemeStyle = CMenuPicButton::THEME_BOX;
+		yes.eTextAlignment = QM_CENTER;
+		yes.SetRect( right - wYes, rowY, wYes, THEME_BTNROW_H );
+
+		no.eThemeStyle = CMenuPicButton::THEME_BOX;
+		no.eTextAlignment = QM_CENTER;
+		no.SetRect( right - wYes - THEME_BTN_GAP - wNo, rowY, wNo, THEME_BTNROW_H );
+		return;
+	}
+
 	SetRect( DLG_X + 192, 256, 640, 256 );
 	pos.x += uiStatic.xOffset;
 	pos.y += uiStatic.yOffset;
@@ -112,6 +159,23 @@ void CMenuYesNoMessageBox::_VidInit()
 	CalcSizes();
 }
 
+/*
+==============
+CMenuYesNoMessageBox::Show
+
+The message is set immediately before the box is shown (the quit dialog alone
+has three different texts), and the panel is sized to the wrapped message, so
+the layout has to be redone here rather than only on a resolution change.
+==============
+*/
+void CMenuYesNoMessageBox::Show()
+{
+	BaseClass::Show();
+
+	if( UI_ThemeActive( ) && WasInit( ))
+		VidInit();
+}
+
 /*
 ==============
 CMenuYesNoMessageBox::Draw
@@ -119,6 +183,18 @@ CMenuYesNoMessageBox::Draw
 */
 void CMenuYesNoMessageBox::Draw()
 {
+	if( UI_ThemeActive( ))
+	{
+		UI_FillRect( 0, 0, gpGlobals->scrWidth, gpGlobals->scrHeight, THEME_SCRIM );
+		UI_ThemeDrawDialogPanel( m_scPos, m_scSize, szPanelTitle );
+		UI_ThemeSeparator( m_scPos.x + (int)( THEME_PAD_X * uiStatic.scaleX ),
+			m_scPos.y + m_scSize.h - (int)(( THEME_PAD_BOTTOM + THEME_BTNROW_H + THEME_BTNROW_GAP / 2 ) * uiStatic.scaleY ),
+			m_scSize.w - (int)( THEME_PAD_X * 2 * uiStatic.scaleX ), THEME_SEPARATOR );
+
+		CMenuBaseWindow::Draw();
+		return;
+	}
+
 	UI_FillRect( 0, 0, gpGlobals->scrWidth, gpGlobals->scrHeight, 0x40000000 );
 
 	EngFuncs::FillRGBA( m_scPos.x, m_scPos.y, m_scSize.w, m_scSize.h, 20, 20, 20, 235 );
diff --git a/3rdparty/mainui/controls/YesNoMessageBox.h b/3rdparty/mainui/controls/YesNoMessageBox.h
index eb94222..94df055 100644
--- a/3rdparty/mainui/controls/YesNoMessageBox.h
+++ b/3rdparty/mainui/controls/YesNoMessageBox.h
@@ -29,6 +29,7 @@ public:
 
 	void _Init() override;
 	void _VidInit() override;
+	void Show() override;
 	void Draw() override;
 	bool KeyDown( int key ) override;
 	void SetMessage( const char *msg );
@@ -49,6 +50,10 @@ public:
 	CEventCallback onNegative;
 
 	bool bAutoHide;
+
+	// title shown in the theme's title band
+	const char *szPanelTitle;
+
 	CMenuAction		dlgMessage1;
 	CMenuPicButton	yes;
 	CMenuPicButton	no;
diff --git a/3rdparty/mainui/font/FontManager.cpp b/3rdparty/mainui/font/FontManager.cpp
index e319369..da79b71 100644
--- a/3rdparty/mainui/font/FontManager.cpp
+++ b/3rdparty/mainui/font/FontManager.cpp
@@ -21,9 +21,13 @@ GNU General Public License for more details.
 
 #if defined(MAINUI_USE_FREETYPE)
 #include "FreeTypeFont.h"
-#elif defined(MAINUI_USE_STB)
+#endif
+#if defined(MAINUI_USE_STB)
 #include "StbFont.h"
-#elif defined(_WIN32)
+#endif
+#if defined(_WIN32) && defined(MAINUI_USE_CUSTOM_FONT_RENDER)
+// kept available alongside stb_truetype so a missing shipped TTF can still fall
+// back to an installed system family instead of the bitmap font
 #include "WinAPIFont.h"
 #endif
 
@@ -34,6 +38,43 @@ GNU General Public License for more details.
 #define DEFAULT_CONFONT  "Tahoma"
 #define DEFAULT_WEIGHT   500
 
+// the three shipped Inter statics, picked by the requested weight
+#define THEME_FONT_WEIGHT_REGULAR  400
+#define THEME_FONT_WEIGHT_MEDIUM   500
+#define THEME_FONT_WEIGHT_SEMIBOLD 600
+
+/*
+=================
+AllocPrimaryFont / AllocSystemFont
+
+The primary backend is whatever the build selected (stb_truetype for this
+build). The system backend only exists on Windows and is used as a fallback so
+a missing or unreadable shipped TTF can never drop the whole menu to the
+bitmap font.
+=================
+*/
+static CBaseFont *AllocPrimaryFont( void )
+{
+#if defined(MAINUI_USE_FREETYPE)
+	return new CFreeTypeFont();
+#elif defined(MAINUI_USE_STB)
+	return new CStbFont();
+#elif defined(_WIN32) && defined(MAINUI_USE_CUSTOM_FONT_RENDER)
+	return new CWinAPIFont();
+#else
+	return new CBitmapFont();
+#endif
+}
+
+static CBaseFont *AllocSystemFont( void )
+{
+#if defined(_WIN32) && defined(MAINUI_USE_CUSTOM_FONT_RENDER)
+	return new CWinAPIFont();
+#else
+	return NULL;
+#endif
+}
+
 CFontManager *g_FontMgr;
 
 CFontManager::CFontManager()
@@ -74,27 +115,39 @@ void CFontManager::VidInit( void )
 #endif
 	)
 	{
+		const bool theme = UI_ThemeActive();
+		const char *menufont = theme ? UI_ThemeFontFamily() : DEFAULT_MENUFONT;
+
 		DeleteAllFonts();
-		uiStatic.hDefaultFont = CFontBuilder( DEFAULT_MENUFONT, UI_MED_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
+		uiStatic.hDefaultFont = CFontBuilder( menufont, UI_MED_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
 			.SetHandleNum( QM_DEFAULTFONT )
 			.Create();
-		uiStatic.hSmallFont   = CFontBuilder( DEFAULT_MENUFONT, UI_SMALL_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
+		uiStatic.hSmallFont   = CFontBuilder( menufont, UI_SMALL_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
 			.SetHandleNum( QM_SMALLFONT )
 			.Create();
-		uiStatic.hBigFont     = CFontBuilder( DEFAULT_MENUFONT, UI_BIG_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
+		uiStatic.hBigFont     = CFontBuilder( menufont, UI_BIG_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
 			.SetHandleNum( QM_BIGFONT )
 			.Create();
-		uiStatic.hBoldFont = CFontBuilder( DEFAULT_MENUFONT, UI_MED_CHAR_HEIGHT * scale, 1000 )
+		uiStatic.hBoldFont = CFontBuilder( menufont, UI_MED_CHAR_HEIGHT * scale, 1000 )
 			.SetHandleNum( QM_BOLDFONT )
 			.Create();
 
-		if( !uiStatic.lowmemory )
+		if( theme )
 		{
-			uiStatic.hLightBlur = CFontBuilder( DEFAULT_MENUFONT, UI_MED_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
+			// The two Gaussian blur passes only exist for the WON pic-button
+			// glow, which the theme drops. Not building them also takes the two
+			// 8*scale rasterisations out of every resolution change. Point the
+			// handles at the default font so nothing can draw with an invalid
+			// one if an untouched code path still reaches for them.
+			uiStatic.hLightBlur = uiStatic.hHeavyBlur = uiStatic.hDefaultFont;
+		}
+		else if( !uiStatic.lowmemory )
+		{
+			uiStatic.hLightBlur = CFontBuilder( menufont, UI_MED_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
 				.SetBlurParams( 2 * scale, 1.25f )
 				.Create();
 
-			uiStatic.hHeavyBlur = CFontBuilder( DEFAULT_MENUFONT, UI_MED_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
+			uiStatic.hHeavyBlur = CFontBuilder( menufont, UI_MED_CHAR_HEIGHT * scale, DEFAULT_WEIGHT )
 				.SetBlurParams( 8 * scale, 2.0f )
 				.Create();
 		}
@@ -102,6 +155,26 @@ void CFontManager::VidInit( void )
 		uiStatic.hConsoleFont = CFontBuilder( DEFAULT_CONFONT, UI_CONSOLE_CHAR_HEIGHT * scale, 500 )
 			.SetOutlineSize()
 			.Create();
+
+		if( theme )
+		{
+			// Regular for body and labels, Medium for the menu items, SemiBold
+			// for the title band, the buttons and the wordmark
+			uiStatic.hThemeTitle   = CFontBuilder( menufont, THEME_TITLE_TALL   * scale, THEME_FONT_WEIGHT_SEMIBOLD ).Create();
+			uiStatic.hThemeLabel   = CFontBuilder( menufont, THEME_LABEL_TALL   * scale, THEME_FONT_WEIGHT_REGULAR ).Create();
+			uiStatic.hThemeBody    = CFontBuilder( menufont, THEME_BODY_TALL    * scale, THEME_FONT_WEIGHT_REGULAR ).Create();
+			uiStatic.hThemeButton  = CFontBuilder( menufont, THEME_BUTTON_TALL  * scale, THEME_FONT_WEIGHT_SEMIBOLD ).Create();
+			uiStatic.hThemeItem    = CFontBuilder( menufont, THEME_ITEM_TALL    * scale, THEME_FONT_WEIGHT_MEDIUM ).Create();
+			uiStatic.hThemeLogo    = CFontBuilder( menufont, THEME_LOGO_TALL    * scale, THEME_FONT_WEIGHT_SEMIBOLD ).Create();
+			uiStatic.hThemeLogoSub = CFontBuilder( menufont, THEME_LOGOSUB_TALL * scale, THEME_FONT_WEIGHT_MEDIUM ).Create();
+		}
+		else
+		{
+			uiStatic.hThemeTitle = uiStatic.hThemeLabel = uiStatic.hThemeBody =
+			uiStatic.hThemeButton = uiStatic.hThemeItem = uiStatic.hThemeLogo =
+			uiStatic.hThemeLogoSub = uiStatic.hDefaultFont;
+		}
+
 		prevScale = scale;
 	}
 }
@@ -487,30 +560,58 @@ HFont CFontBuilder::Create()
 		}
 	}
 
-#if defined(MAINUI_USE_FREETYPE)
-	font = new CFreeTypeFont();
-#elif defined(MAINUI_USE_STB)
-	font = new CStbFont();
-#elif defined(_WIN32) && defined(MAINUI_USE_CUSTOM_FONT_RENDER)
-	font = new CWinAPIFont();
-#else
-	font = new CBitmapFont();
-#endif
+	font = AllocPrimaryFont();
 
 	double starttime = EngFuncs::DoubleTime();
 
 	if( !font->Create( m_szName, m_iTall, m_iWeight, m_iBlur, m_fBrighten, m_iOutlineSize, m_iScanlineOffset, m_fScanlineScale, m_iFlags ) )
 	{
-		delete font;
+		// The shipped TTF may be missing (stb_truetype), or the family may not
+		// be installed (CWinAPIFont::Create -> EnumFontFamiliesExA). Dropping
+		// straight to the bitmap font is a visible catastrophe, so walk a chain
+		// of families through the system backend before giving up.
+		bool created = false;
 
-		// fallback to bitmap font
-		font = new CBitmapFont();
+		for( int i = 0; !created; i++ )
+		{
+			const char *name = UI_ThemeFontFallback( i );
+			CBaseFont *next;
+
+			if( !name )
+				break;
+
+			next = AllocSystemFont();
+			if( !next )
+			{
+				// no second backend: retrying the same family is pointless
+				if( !stricmp( name, m_szName ))
+					continue;
+				next = AllocPrimaryFont();
+			}
+
+			delete font;
+			font = next;
 
-		// should never fail
-		if( !font->Create( "Bitmap Font", m_iTall, m_iWeight, m_iBlur, m_fBrighten, m_iOutlineSize, m_iScanlineOffset, m_fScanlineScale, m_iFlags ) )
+			created = font->Create( name, m_iTall, m_iWeight, m_iBlur, m_fBrighten,
+				m_iOutlineSize, m_iScanlineOffset, m_fScanlineScale, m_iFlags );
+
+			if( created )
+				Con_Printf( "Font \"%s\" is unavailable, using \"%s\"\n", m_szName, name );
+		}
+
+		if( !created )
 		{
 			delete font;
-			return -1;
+
+			// fallback to bitmap font
+			font = new CBitmapFont();
+
+			// should never fail
+			if( !font->Create( "Bitmap Font", m_iTall, m_iWeight, m_iBlur, m_fBrighten, m_iOutlineSize, m_iScanlineOffset, m_fScanlineScale, m_iFlags ) )
+			{
+				delete font;
+				return -1;
+			}
 		}
 	}
 
@@ -534,6 +635,20 @@ HFont CFontBuilder::Create()
 
 bool CFontManager::FindFontDataFile( const char *name, int tall, int weight, int flags, char *dataFile, size_t dataFileChars )
 {
+	// Inter (SIL Open Font License 1.1) is shipped with the game data as three
+	// static faces; stb_truetype cannot synthesise a weight, so pick the face
+	// from the requested weight.
+	if( !stricmp( name, "Inter" ))
+	{
+		if( weight >= THEME_FONT_WEIGHT_SEMIBOLD )
+			Q_strncpy( dataFile, "gfx/fonts/Inter-SemiBold.ttf", dataFileChars );
+		else if( weight >= THEME_FONT_WEIGHT_MEDIUM )
+			Q_strncpy( dataFile, "gfx/fonts/Inter-Medium.ttf", dataFileChars );
+		else
+			Q_strncpy( dataFile, "gfx/fonts/Inter-Regular.ttf", dataFileChars );
+		return true;
+	}
+
 	if( !strcmp( name, "Trebuchet MS" ))
 	{
 		Q_strncpy( dataFile, "gfx/fonts/FiraSans-Regular.ttf", dataFileChars );
diff --git a/3rdparty/mainui/font/WinAPIFont.cpp b/3rdparty/mainui/font/WinAPIFont.cpp
index 7073e5c..6d03c4b 100644
--- a/3rdparty/mainui/font/WinAPIFont.cpp
+++ b/3rdparty/mainui/font/WinAPIFont.cpp
@@ -13,7 +13,10 @@ MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
 GNU General Public License for more details.
 */
 
-#if defined _WIN32 && defined(MAINUI_USE_CUSTOM_FONT_RENDER) && !defined(MAINUI_USE_STB)
+// Built even when stb_truetype is the primary backend: CFontBuilder::Create
+// falls back to this one when a shipped TTF is missing, so the menu can never
+// drop to the bitmap font.
+#if defined _WIN32 && defined(MAINUI_USE_CUSTOM_FONT_RENDER)
 #include <stdarg.h>
 #include <malloc.h>
 
diff --git a/3rdparty/mainui/menus/AdvancedControls.cpp b/3rdparty/mainui/menus/AdvancedControls.cpp
index 3424ce3..c39071a 100644
--- a/3rdparty/mainui/menus/AdvancedControls.cpp
+++ b/3rdparty/mainui/menus/AdvancedControls.cpp
@@ -18,6 +18,17 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 
 */
 
+/*
+------------------------------------------------------------------------------
+
+In theme mode this page is the top-level "Game" options page, the first entry
+of Options. It carries what used to be hidden behind Controls > Advanced (the
+advanced control options), plus the Cry of Fear pause-menu-saves switch that
+used to live in the Save/Load menu and the subtitle language selector. Without
+the theme it is still the stock Advanced Controls dialog reached from Controls,
+so the WON path is unchanged.
+*/
+
 #include "q_client.h"
 #include "Framework.h"
 #include "MenuStrings.h"
@@ -25,14 +36,55 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 #include "PicButton.h"
 #include "CheckBox.h"
 #include "Slider.h"
+#include "SpinControl.h"
+#include "StringArrayModel.h"
+#include "CryOfFear.h"
 
 #define ART_BANNER			"gfx/shell/head_advanced"
 
+// the subtitle languages, wrapped as a spin-control model
+class CMenuCoFLanguageModel : public CMenuBaseArrayModel
+{
+public:
+	void Update() override { }
+	int GetRows() const override { return UI_CoFSubtitleLanguageCount(); }
+	const char *GetText( int line ) override { return UI_CoFSubtitleLanguageName( line ); }
+};
+
+// The in-game UI scale, as a spin control over the two engine cvars that drive
+// it (see engine/client/cl_main.c and docs/cof-ui-m4-scaling.md):
+//
+//   Auto   cof_ui_scale 0    the automatic law - the UI keeps the fraction of
+//                            the screen it has at 1080p, at every resolution
+//   100 %  cof_ui_scale 100  no automatic scaling, the client's native pixels
+//   125,
+//   150,
+//   200 %  cof_ui_scale 0 with cof_ui_scale_user as the multiplier on top
+//
+// so all five entries mean something different. The multiplier also drives the
+// engine HUD text height, so the bars, the panels and the text grow together.
+static const struct { const char *label; float scale; float user; } g_cofHudScales[] =
+{
+	{ "Auto",  0.0f,   1.00f },
+	{ "100 %", 100.0f, 1.00f },
+	{ "125 %", 0.0f,   1.25f },
+	{ "150 %", 0.0f,   1.50f },
+	{ "200 %", 0.0f,   2.00f },
+};
+
+class CMenuCoFHudScaleModel : public CMenuBaseArrayModel
+{
+public:
+	void Update() override { }
+	int GetRows() const override { return (int)( sizeof( g_cofHudScales ) / sizeof( g_cofHudScales[0] )); }
+	const char *GetText( int line ) override { return g_cofHudScales[line].label; }
+};
+
 class CAdvancedControls : public CMenuFramework
 {
 public:
 	typedef CMenuFramework BaseClass;
-	CAdvancedControls() : CMenuFramework("CAdvancedControls") { }
+	CAdvancedControls() : CMenuFramework("CAdvancedControls") { m_bCoF = false; }
 
 	void ToggleLookCheckboxes( bool write );
 
@@ -43,6 +95,10 @@ private:
 
 	void GetConfig( void );
 	void PitchInvert( void );
+	void MenuSavesChanged( void );
+	void ConsoleEnableChanged( void );
+	void LanguageChanged( void );
+	void HudScaleChanged( void );
 
 	CMenuPicButton done, inputDev;
 
@@ -55,6 +111,23 @@ private:
 	CMenuCheckBox	autoaim;
 	CMenuCheckBox	rawinput;
 	CMenuSlider	sensitivity;
+
+	// "Enable console". Bound to the engine cvar con_enable, which this
+	// project's engine patch adds: stock FWGS has no cvar for the console at
+	// all, only the "-console"/"-dev" launch switches and the one-way,
+	// restricted "ui_allowconsole" command that upstream's multiplayer-only
+	// Game Options page exposes as a push button. con_enable is archived and
+	// the engine mirrors it onto host.allow_console, so this is a real
+	// two-way, persistent setting. See docs/cof-ui-death-flow.md.
+	CMenuCheckBox	conEnable;
+
+	// Cry of Fear game options
+	CMenuCheckBox	menuSaves;
+	CMenuSpinControl	language;
+	CMenuSpinControl	hudScale;
+	CMenuCoFLanguageModel	languageModel;
+	CMenuCoFHudScaleModel	hudScaleModel;
+	bool		m_bCoF;
 };
 
 /*
@@ -92,9 +165,94 @@ void CAdvancedControls::GetConfig( )
 	rawinput.LinkCvar( "m_rawinput" );
 	sensitivity.LinkCvar( "sensitivity" );
 
+	if( UI_ThemeActive( ))
+	{
+		conEnable.LinkCvar( "con_enable" );
+		conEnable.UpdateEditable();
+	}
+
+	if( m_bCoF )
+	{
+		menuSaves.LinkCvar( "cof_pause_menu_saves" );
+		menuSaves.UpdateEditable();
+		language.SetCurrentValue( (float)( UI_CoFGetSubtitleLanguage() - 1 ));
+
+		// pick the row the two cvars currently describe; anything hand-edited
+		// that matches no row shows as Auto without being written back
+		{
+			float scale = EngFuncs::GetCvarFloat( "cof_ui_scale" );
+			float user = EngFuncs::GetCvarFloat( "cof_ui_scale_user" );
+			int row = 0;
+
+			for( int i = 0; i < hudScaleModel.GetRows( ); i++ )
+			{
+				if( fabs( g_cofHudScales[i].scale - scale ) < 0.5f &&
+					fabs( g_cofHudScales[i].user - user ) < 0.005f )
+				{
+					row = i;
+					break;
+				}
+			}
+
+			hudScale.SetCurrentValue( (float)row );
+		}
+	}
+
 	ToggleLookCheckboxes( false );
 }
 
+/*
+=================
+CAdvancedControls::MenuSavesChanged
+
+Opt-in feature: make the change durable as soon as it is selected, exactly as
+the Save/Load menu did before this switch moved here.
+=================
+*/
+void CAdvancedControls::MenuSavesChanged()
+{
+	menuSaves.WriteCvar();
+}
+
+/*
+=================
+CAdvancedControls::ConsoleEnableChanged
+
+Write it immediately, like the pause-saves switch: the engine picks the new
+value up on its next frame (Con_ApplyEnable, engine/client/console.c) and the
+archived cvar is what makes the choice survive a restart.
+=================
+*/
+void CAdvancedControls::ConsoleEnableChanged()
+{
+	conEnable.WriteCvar();
+}
+
+void CAdvancedControls::LanguageChanged()
+{
+	UI_CoFSetSubtitleLanguage( (int)language.GetCurrentValue() + 1 );
+}
+
+/*
+=================
+CAdvancedControls::HudScaleChanged
+
+Both cvars are archived and both are live - the engine re-derives the transform
+and reloads the HUD font on the next frame (CL_CoF_CheckUIScaleChanged), so
+there is nothing to restart and nothing to warn about.
+=================
+*/
+void CAdvancedControls::HudScaleChanged()
+{
+	int row = (int)hudScale.GetCurrentValue();
+
+	if( row < 0 || row >= hudScaleModel.GetRows( ))
+		return;
+
+	EngFuncs::CvarSetValue( "cof_ui_scale", g_cofHudScales[row].scale );
+	EngFuncs::CvarSetValue( "cof_ui_scale_user", g_cofHudScales[row].user );
+}
+
 void CAdvancedControls::PitchInvert()
 {
 	bool invert = invertMouse.bChecked;
@@ -131,6 +289,8 @@ void CAdvancedControls::SaveAndPopMenu()
 	autoaim.WriteCvar();
 	rawinput.WriteCvar();
 	sensitivity.WriteCvar();
+	if( UI_ThemeActive( ))
+		conEnable.WriteCvar();
 
 	ToggleLookCheckboxes( true );
 
@@ -146,6 +306,11 @@ void CAdvancedControls::_Init( void )
 {
 	banner.SetPicture( ART_BANNER );
 
+	m_bCoF = UI_ThemeActive() && UI_IsCryOfFear();
+
+	if( UI_ThemeActive( ))
+		SetPanel( "GAME", 760, 520 );
+
 	done.szName = L( "Done" );
 	done.SetPicture( PC_DONE );
 	done.onReleased = VoidCb( &CAdvancedControls::SaveAndPopMenu );
@@ -197,7 +362,32 @@ void CAdvancedControls::_Init( void )
 		inputDev.SetGrayed( true );
 	inputDev.SetCoord( 72, 210 );
 
-	AddItem( banner );
+	conEnable.SetNameAndStatus( L( "Enable console" ),
+		L( "Allow the developer console without the -console or -dev command line switch" ));
+	conEnable.iFlags |= QMF_NOTIFY;
+	conEnable.onChanged = VoidCb( &CAdvancedControls::ConsoleEnableChanged );
+
+	menuSaves.SetNameAndStatus( L( "Pause menu saves" ),
+		L( "Allow saving from the pause menu into the five shared Cry of Fear slots" ));
+	menuSaves.iFlags |= QMF_NOTIFY;
+	menuSaves.onChanged = VoidCb( &CAdvancedControls::MenuSavesChanged );
+
+	language.szName = L( "Subtitle language" );
+	language.Setup( &languageModel );
+	language.SetCharSize( QM_SMALLFONT );
+	language.iFlags |= QMF_NOTIFY;
+	language.szStatusText = L( "Language of the subtitles and the in-game documents" );
+	language.onChanged = VoidCb( &CAdvancedControls::LanguageChanged );
+
+	hudScale.szName = L( "HUD scale" );
+	hudScale.Setup( &hudScaleModel );
+	hudScale.SetCharSize( QM_SMALLFONT );
+	hudScale.iFlags |= QMF_NOTIFY;
+	hudScale.szStatusText = L( "Size of the in-game HUD, inventory and subtitles. Auto keeps them the same fraction of the screen at every resolution" );
+	hudScale.onChanged = VoidCb( &CAdvancedControls::HudScaleChanged );
+
+	if( !UI_ThemeActive( ))
+		AddItem( banner );
 	AddItem( done );
 	AddItem( inputDev );
 	AddItem( crosshair );
@@ -209,11 +399,74 @@ void CAdvancedControls::_Init( void )
 	AddItem( autoaim );
 	AddItem( rawinput );
 	AddItem( sensitivity );
+
+	if( UI_ThemeActive( ))
+		AddItem( conEnable );
+
+	if( m_bCoF )
+	{
+		AddItem( menuSaves );
+		AddItem( language );
+		AddItem( hudScale );
+	}
 }
 
 
 void CAdvancedControls::_VidInit()
 {
+	if( IsPanel( ))
+	{
+		Rect left = ColumnRect( 0, 2 );
+		Rect right = ColumnRect( 1, 2 );
+		CMenuCheckBox *col1[] = { &crosshair, &invertMouse, &mouseLook, &lookSpring };
+		CMenuCheckBox *col2[] = { &lookStrafe, &lookFilter, &autoaim, &rawinput };
+		int y = left.pt.y;
+
+		for( int i = 0; i < 4; i++ )
+		{
+			col1[i]->SetCoord( left.pt.x, y );
+			col2[i]->SetCoord( right.pt.x, y );
+			y += THEME_ROW_PITCH;
+		}
+
+		// the extra game switches keep the two checkbox columns going
+		CMenuCheckBox *extra[2];
+		int n = 0;
+
+		extra[n++] = &conEnable;
+		if( m_bCoF )
+			extra[n++] = &menuSaves;
+
+		for( int i = 0; i < n; i++ )
+		{
+			extra[i]->SetCoord(( i & 1 ) ? right.pt.x : left.pt.x, y );
+			if( i & 1 )
+				y += THEME_ROW_PITCH;
+		}
+
+		if( n & 1 )
+			y += THEME_ROW_PITCH;
+
+		// the slider draws its own caption 1.5 char heights above its track, so
+		// the next row has to clear the checkbox block by more than a label
+		y += THEME_LABEL_TALL + 40;
+		sensitivity.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+		inputDev.SetRect( right.pt.x, y, right.sz.w, THEME_ROW_H );
+		inputDev.eThemeStyle = CMenuPicButton::THEME_ROW;
+		inputDev.eTextAlignment = QM_LEFT;
+
+		if( m_bCoF )
+		{
+			y += THEME_CTRL_PITCH;
+			language.SetRect( left.pt.x, y, left.sz.w, THEME_CTRL_H );
+			hudScale.SetRect( right.pt.x, y, right.sz.w, THEME_CTRL_H );
+		}
+
+		done.szName = L( "Close" );
+		CMenuPicButton *btns[] = { &done };
+		LayoutButtonRow( btns, 1 );
+	}
+
 	GetConfig();
 }
 
diff --git a/3rdparty/mainui/menus/Audio.cpp b/3rdparty/mainui/menus/Audio.cpp
index 152fb47..5962e14 100644
--- a/3rdparty/mainui/menus/Audio.cpp
+++ b/3rdparty/mainui/menus/Audio.cpp
@@ -25,6 +25,7 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 #include "CheckBox.h"
 #include "SpinControl.h"
 #include "StringArrayModel.h"
+#include "CryOfFear.h"
 
 #define ART_BANNER			"gfx/shell/head_audio"
 
@@ -44,6 +45,7 @@ private:
 
 	CMenuSlider	soundVolume;
 	CMenuSlider	musicVolume;
+	CMenuSlider	menuVolume;
 	CMenuSlider	suitVolume;
 	CMenuSlider	vibration;
 	CMenuCheckBox noDSP;
@@ -63,6 +65,9 @@ void CMenuAudio::GetConfig( void )
 {
 	soundVolume.LinkCvar( "volume" );
 	musicVolume.LinkCvar( "MP3Volume" );
+	// the engine cvar that scales the menu library's own sounds and nothing
+	// else (engine/client/dll_int/cl_gameui.c, pfnPlaySound)
+	menuVolume.LinkCvar( "ui_sound_volume" );
 	suitVolume.LinkCvar( "suitvolume" );
 	vibration.LinkCvar( "vibration_length" );
 
@@ -98,6 +103,8 @@ void CMenuAudio::SaveAndPopMenu()
 {
 	soundVolume.WriteCvar();
 	musicVolume.WriteCvar();
+	if( menuVolume.IsVisible( ))
+		menuVolume.WriteCvar();
 	suitVolume.WriteCvar();
 	vibration.WriteCvar();
 	noDSP.WriteCvar();
@@ -117,6 +124,9 @@ void CMenuAudio::_Init( void )
 {
 	banner.SetPicture(ART_BANNER);
 
+	if( UI_ThemeActive( ))
+		SetPanel( "AUDIO", 640, 460 );
+
 	soundVolume.szName = L( "GameUI_SoundEffectVolume" );
 	soundVolume.Setup( 0.0, 1.0, 0.05f );
 	soundVolume.onChanged = CMenuEditable::WriteCvarCb;
@@ -129,6 +139,17 @@ void CMenuAudio::_Init( void )
 	musicVolume.SetCoord( 320, 340 );
 	musicVolume.size.w = 300;
 
+	// Menu sounds. Only in theme mode: the WON layout is a grid of fixed
+	// coordinates and adding a row to it would move controls the ui_theme 0
+	// path promises to leave exactly as they were.
+	menuVolume.szName = L( "Menu sounds" );
+	menuVolume.szStatusText = L( "Volume of the menu's own button and rollover sounds" );
+	menuVolume.Setup( 0.0, 1.0, 0.05f );
+	menuVolume.onChanged = CMenuEditable::WriteCvarCb;
+	menuVolume.SetCoord( 320, 400 );
+	menuVolume.size.w = 300;
+	menuVolume.SetVisibility( UI_ThemeActive( ));
+
 	suitVolume.szName = L( "GameUI_HEVSuitVolume" );
 	suitVolume.Setup( 0.0, 1.0, 0.05f );
 	suitVolume.onChanged = CMenuEditable::WriteCvarCb;
@@ -159,10 +180,13 @@ void CMenuAudio::_Init( void )
 	vibration.onChanged = VoidCb( &CMenuAudio::VibrateChanged );
 	vibration.SetCoord( 700, 560 );
 
-	AddItem( banner );
-	AddButton( L( "Done" ), nullptr, PC_DONE, VoidCb( &CMenuAudio::SaveAndPopMenu ));
+	if( !UI_ThemeActive( ))
+		AddItem( banner );
+	AddButton( UI_ThemeActive() ? L( "Close" ) : L( "Done" ), nullptr, PC_DONE,
+		VoidCb( &CMenuAudio::SaveAndPopMenu ));
 	AddItem( soundVolume );
 	AddItem( musicVolume );
+	AddItem( menuVolume );
 	AddItem( suitVolume );
 	AddItem( noDSP );
 	AddItem( useAlphaDSP );
@@ -173,6 +197,44 @@ void CMenuAudio::_Init( void )
 
 void CMenuAudio::_VidInit( )
 {
+	if( IsPanel( ))
+	{
+		// left column: the volume sliders
+		// right column: the DSP and focus checkboxes
+		// the two vibration items are mobile-only and are hidden on desktop
+		Rect left = ColumnRect( 0, 2 );
+		Rect right = ColumnRect( 1, 2 );
+		int y = left.pt.y + THEME_LABEL_TALL + 8;
+
+		// Cry of Fear has no HEV suit, so it has no suit voice to set a volume
+		// for; "suitvolume" only drives Half-Life's suit speech.
+		suitVolume.SetVisibility( !UI_IsCryOfFear( ));
+
+		soundVolume.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+		y += THEME_CTRL_PITCH;
+		musicVolume.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+		y += THEME_CTRL_PITCH;
+		menuVolume.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+
+		if( suitVolume.IsVisible( ))
+		{
+			y += THEME_CTRL_PITCH;
+			suitVolume.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+		}
+
+		y = right.pt.y + THEME_LABEL_TALL + 8;
+		noDSP.SetCoord( right.pt.x, y );
+		y += THEME_ROW_PITCH;
+		useAlphaDSP.SetCoord( right.pt.x, y );
+		y += THEME_ROW_PITCH;
+		muteFocusLost.SetCoord( right.pt.x, y );
+
+		vibrationEnable.SetVisibility( false );
+		vibration.SetVisibility( false );
+
+		RealignButtons();
+	}
+
 	GetConfig();
 }
 
diff --git a/3rdparty/mainui/menus/Configuration.cpp b/3rdparty/mainui/menus/Configuration.cpp
index 6e0a981..78ee08d 100644
--- a/3rdparty/mainui/menus/Configuration.cpp
+++ b/3rdparty/mainui/menus/Configuration.cpp
@@ -31,6 +31,10 @@ class CMenuOptions: public CMenuFramework
 {
 private:
 	void _Init( void ) override;
+	void _VidInit( void ) override;
+
+	CMenuPicButton row[4];
+	CMenuPicButton close;
 
 public:
 	typedef CMenuFramework BaseClass;
@@ -54,6 +58,46 @@ void CMenuOptions::_Init( void )
 
 	msgBox.Link( this );
 
+	if( UI_ThemeActive( ))
+	{
+		// Touch, Gamepad and Update have no place in this build's Options:
+		// touch is inert, the gamepad page is not part of Cry of Fear's own
+		// menu, and the update check points at upstream FWGS.
+		//
+		// Game is first and is the page the advanced control options, the
+		// pause-menu-saves switch and the subtitle language moved onto; it is
+		// menus/AdvancedControls.cpp, which is no longer reachable from
+		// Controls.
+		static const char *names[4]  = { "Game", "Controls", "GameUI_Audio", "GameUI_Video" };
+		static const char *hints[4]  =
+		{
+			"Aiming, mouse look, saves and language",
+			"Change keyboard and mouse settings",
+			"Change sound volume and quality",
+			"Change screen size, video mode and gamma"
+		};
+		void (*handlers[4])( void ) = { UI_AdvControls_Menu, UI_Controls_Menu, UI_Audio_Menu, UI_Video_Menu };
+
+		SetPanel( "OPTIONS", 420, 340 );
+
+		for( int i = 0; i < 4; i++ )
+		{
+			row[i].SetNameAndStatus( L( names[i] ), L( hints[i] ));
+			row[i].iFlags |= QMF_NOTIFY;
+			row[i].eThemeStyle = CMenuPicButton::THEME_ROW;
+			row[i].eTextAlignment = QM_LEFT;
+			row[i].onReleased = handlers[i];
+			AddItem( row[i] );
+		}
+
+		close.szName = L( "Close" );
+		close.SetPicture( PC_DONE );
+		close.iFlags |= QMF_NOTIFY;
+		close.onReleased = VoidCb( &CMenuOptions::Hide );
+		AddItem( close );
+		return;
+	}
+
 	AddItem( banner );
 	AddButton( L( "Controls" ), L( "Change keyboard and mouse settings" ),
 		PC_CONTROLS, UI_Controls_Menu, QMF_NOTIFY );
@@ -71,4 +115,20 @@ void CMenuOptions::_Init( void )
 		PC_DONE, VoidCb( &CMenuOptions::Hide ), QMF_NOTIFY );
 }
 
+void CMenuOptions::_VidInit( void )
+{
+	if( !IsPanel( ))
+		return;
+
+	for( int i = 0; i < 4; i++ )
+	{
+		Rect r = RowRect( i );
+
+		row[i].SetRect( r.pt.x, r.pt.y, r.sz.w, r.sz.h );
+	}
+
+	CMenuPicButton *btns[] = { &close };
+	LayoutButtonRow( btns, 1 );
+}
+
 ADD_MENU( menu_options, CMenuOptions, UI_Options_Menu );
diff --git a/3rdparty/mainui/menus/Controls.cpp b/3rdparty/mainui/menus/Controls.cpp
index b4f1aac..fe26190 100644
--- a/3rdparty/mainui/menus/Controls.cpp
+++ b/3rdparty/mainui/menus/Controls.cpp
@@ -230,6 +230,9 @@ void CMenuControls::_Init( void )
 {
 	banner.SetPicture( ART_BANNER );
 
+	if( UI_ThemeActive( ))
+		SetPanel( "CONTROLS", 900, 600 );
+
 	keysList.SetRect( 360, 230, -20, 465 );
 	keysList.SetModel( &keysListModel );
 	keysList.SetupColumn( 0, L( "GameUI_Action" ), 0.50f );
@@ -243,21 +246,55 @@ void CMenuControls::_Init( void )
 	msgBox2.onPositive = VoidCb( &CMenuControls::ResetKeysList );
 	msgBox2.Link( this );
 
-	AddItem( banner );
+	if( !UI_ThemeActive( ))
+		AddItem( banner );
+
 	AddButton( L( "GameUI_UseDefaults" ), nullptr, PC_USE_DEFAULTS, msgBox2.MakeOpenEvent( ));
-	AddButton( L( "Adv. Controls" ), nullptr, PC_ADV_CONTROLS, UI_AdvControls_Menu );
-	AddButton( L( "GameUI_OK" ), nullptr, PC_OK, VoidCb( &CMenuControls::SaveAndPopMenu ));
-	AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuControls::Cancel ));
+
+	// The advanced controls now live on the top-level Game options page
+	// (menus/AdvancedControls.cpp, "GAME"), which Options lists first, so the
+	// button that hid them behind Controls is gone from the themed page.
+	if( !UI_ThemeActive( ))
+		AddButton( L( "Adv. Controls" ), nullptr, PC_ADV_CONTROLS, UI_AdvControls_Menu );
+
+	// secondary first, so the button row reads ... Cancel then OK
+	if( UI_ThemeActive( ))
+	{
+		AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuControls::Cancel ));
+		AddButton( L( "GameUI_OK" ), nullptr, PC_OK, VoidCb( &CMenuControls::SaveAndPopMenu ));
+	}
+	else
+	{
+		AddButton( L( "GameUI_OK" ), nullptr, PC_OK, VoidCb( &CMenuControls::SaveAndPopMenu ));
+		AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuControls::Cancel ));
+	}
+
 	AddItem( keysList );
 }
 
 void CMenuControls::_VidInit()
 {
-	msgBox1.SetRect( DLG_X + 192, 256, 640, 128 );
-	msgBox1.pos.x += uiStatic.xOffset;
-	msgBox1.pos.y += uiStatic.yOffset;
+	if( IsPanel( ))
+	{
+		Rect c = ContentRect();
+
+		keysList.SetRect( c.pt.x, c.pt.y, c.sz.w, c.sz.h );
+		msgBox1.szPanelTitle = "PRESS A KEY";
+	}
+	else
+	{
+		// in theme mode CMenuMessageBox sizes and centres itself around the
+		// wrapped message, so do not stamp the WON rect over it
+		msgBox1.SetRect( DLG_X + 192, 256, 640, 128 );
+		msgBox1.pos.x += uiStatic.xOffset;
+		msgBox1.pos.y += uiStatic.yOffset;
+	}
 
 	keysListModel.Update();
+
+	// the first row of the list is a section caption in theme mode, so start
+	// the cursor on the first real binding instead
+	keysList.SetCurrentIndex( 0 );
 }
 
 ADD_MENU( menu_controls, CMenuControls, UI_Controls_Menu );
diff --git a/3rdparty/mainui/menus/CreateGame.cpp b/3rdparty/mainui/menus/CreateGame.cpp
index 00deec5..a745ba5 100644
--- a/3rdparty/mainui/menus/CreateGame.cpp
+++ b/3rdparty/mainui/menus/CreateGame.cpp
@@ -222,13 +222,28 @@ void CMenuCreateGame::_Init( void )
 	nat.bChecked = false;
 	nat.LinkCvar( "sv_nat" );
 
+	if( UI_ThemeActive( ))
+		SetPanel( "HOST SERVER", 900, 560 );
+	else
+		AddItem( banner );
+
 	// add them here, so "done" button can be used by mapsListModel::Update
-	AddItem( banner );
 	CMenuPicButton *advOpt = AddButton( L( "Adv. Options" ), nullptr, PC_ADV_OPT, UI_AdvServerOptions_Menu );
 	advOpt->SetGrayed( !UI_AdvServerOptions_IsAvailable() );
 
-	done = AddButton( L( "GameUI_OK" ), nullptr, PC_OK, Begin );
-	done->onReleasedClActive = msgBox.MakeOpenEvent();
+	if( UI_ThemeActive( ))
+	{
+		// secondary first: Adv. Options, Cancel, Start
+		AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuCreateGame::Hide ) );
+		done = AddButton( L( "Start" ), nullptr, PC_OK, Begin );
+		done->onReleasedClActive = msgBox.MakeOpenEvent();
+		msgBox.szPanelTitle = "START SERVER";
+	}
+	else
+	{
+		done = AddButton( L( "GameUI_OK" ), nullptr, PC_OK, Begin );
+		done->onReleasedClActive = msgBox.MakeOpenEvent();
+	}
 
 	mapsList.SetCharSize( QM_SMALLFONT );
 	mapsList.SetupColumn( 0, L( "GameUI_Map" ), 0.5f );
@@ -288,7 +303,8 @@ void CMenuCreateGame::_Init( void )
 	msgBox.SetMessage( L( "Starting a new game will exit any current game, OK to exit?" ) );
 	msgBox.Link( this );
 
-	AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuCreateGame::Hide ) );
+	if( !UI_ThemeActive( ))
+		AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuCreateGame::Hide ) );
 	AddItem( hostName );
 	AddItem( maxClients );
 	AddItem( password );
@@ -298,6 +314,25 @@ void CMenuCreateGame::_Init( void )
 
 void CMenuCreateGame::_VidInit()
 {
+	if( IsPanel( ))
+	{
+		Rect left = ColumnRect2( 0, 0.42f );
+		Rect right = ColumnRect2( 1, 0.42f );
+		int y = left.pt.y + THEME_LABEL_TALL + 8;
+
+		hostName.SetRect( left.pt.x, y, left.sz.w, THEME_CTRL_H );
+		y += THEME_CTRL_PITCH;
+		maxClients.SetRect( left.pt.x, y, 140, THEME_CTRL_H );
+		y += THEME_CTRL_PITCH;
+		password.SetRect( left.pt.x, y, left.sz.w, THEME_CTRL_H );
+		y += THEME_CTRL_PITCH;
+		nat.SetCoord( left.pt.x, y );
+		nat.Hide();
+
+		mapsList.SetRect( right.pt.x, right.pt.y, right.sz.w, right.sz.h );
+		return;
+	}
+
 	nat.SetCoord( 72, 685 );
 	nat.Hide();
 	// if( !EngFuncs::GetCvarFloat("public") )
diff --git a/3rdparty/mainui/menus/CryOfFear.cpp b/3rdparty/mainui/menus/CryOfFear.cpp
index 21f6b98..36ee0e2 100644
--- a/3rdparty/mainui/menus/CryOfFear.cpp
+++ b/3rdparty/mainui/menus/CryOfFear.cpp
@@ -21,10 +21,12 @@ so the game DLL (cl_dlls/hl.dll) keeps doing the work:
   custom campaign cmd campaign <firstmap>  sent before the skillset, exactly as
                                            the original CCampaignSlider does
   language        cmd subtitleset 1..7     (English + the six txtfiles/languages)
-  unlockables     unlockablescmd           the client's own entry command; it
-                                           loads the c_unlockables gallery map,
-                                           whose state is anti-tamper obfuscated
-                                           and cannot be drawn from here
+  unlockables     (no command)             an engine-menu page that reads the
+                                           real state out of
+                                           cryoffear/scriptsettings.dat;
+                                           "Open original gallery" still runs
+                                           the client's own unlockablescmd,
+                                           which loads c_unlockables
   extras          ShellExecute(<url>)      the original used the Steam overlay,
                                            which never appears under FWGS
 
@@ -43,6 +45,7 @@ Nothing here is reachable unless the running game directory is "cryoffear".
 #include "Utils.h"
 #include "utlvector.h"
 #include "CryOfFear.h"
+#include "FontManager.h"
 
 // ---------------------------------------------------------------------------
 // game detection
@@ -92,12 +95,57 @@ static bool UI_CoFMapNameIsSane( const char *name )
 
 #define COF_NUM_SKILLSETS 4
 
+/*
+=================
+Skip prologue
+
+c_intro is a ~3-minute non-interactive title, credits and car-crash sequence.
+Measured from the entity lumps of the shipped maps, read-only:
+
+* c_intro has exactly one trigger_changelevel, "*141", "map" "c_nightmare",
+  landmark "landakukej" (info_landmark at 212 -408 -2523); the player is
+  teleported into it by cof_teleport "kunitei2" at t+133, itself fired through
+  multiintroduct -> multikattej;
+* every one of c_intro's ten trigger_camera entities carries spawnflags 116,
+  which includes SF_CAMERA_PLAYER_TAKECONTROL, so the player never has control
+  on that map at all;
+* c_intro grants and sets NOTHING: no game_player_equip, no weapon_*, no
+  item_*, no cof_giveitems, no cof_clearitems, no env_global, no cof_chapter,
+  no cof_begingame, no trigger_auto. Its only effect outside itself is the
+  level change;
+* c_nightmare's worldspawn has "newunit" "1", so the engine wipes the save
+  transition directory on entry - it is a clean unit boundary by design - and
+  its first info_player_start (211 -412 -2545) is exactly the landmark arrival
+  point, inside the trigger_once that starts its own opening sequence;
+* c_nightmare carries the campaign's only cof_begingame ("begin", fired at t=0
+  of its nightmulti), and it grants weapon_camera through cof_giveitems
+  "kakkaej". Starting one map later, at c_start, would skip both.
+
+So c_nightmare is the first map of the game proper, and arriving there directly
+is indistinguishable from arriving there through the prologue.
+
+Difficulty survives the map change by itself: "difficulty" is an engine cvar
+registered by the game DLL with FCVAR_ARCHIVE|FCVAR_SERVER (hl.dll cvar_t at
+10214750, default "2"), skillset writes it synchronously with pfnCVarSetFloat
+(VA 10019E09 / 10019E47), and RefreshSkillData (VA 100AF810) re-reads it from
+the cvar on every level load through CWorld::Precache -> InstallGameRules. The
+skillset handler also arms a five-second player think (VA 100DFC10) that issues
+"map <campaign>" or, with no campaign set, "map c_intro" - which is why the
+skip is spelled as the game's own "campaign" override rather than as our own
+"map" command on top of it: otherwise that think would load c_intro five
+seconds after we loaded c_nightmare.
+=================
+*/
+#define COF_FIRST_MAP          "c_intro"
+#define COF_SKIP_PROLOGUE_MAP  "c_nightmare"
+#define COF_SKIP_PROLOGUE_CVAR "cof_skip_prologue"
+
 // The four rows of gfx/vgui/640_difficulty.tga, in the order the original
 // panel draws them. The fourth is replaced by "LOCKED" in
 // gfx/vgui/640_difficultylocked.tga until the Nightmare mode unlockable is
-// earned, which is a client-side flag inside the obfuscated
-// cryoffear/scriptsettings.dat and is not readable from the menu library;
-// cof_nightmare_unlocked mirrors it here.
+// earned. That flag is line 83 of cryoffear/scriptsettings.dat and is read
+// here through UI_CoFNightmareStatus (see the unlockables section below); the
+// milestone-2 stand-in cvar cof_nightmare_unlocked is gone.
 static const char *s_cofSkillName[COF_NUM_SKILLSETS] =
 {
 	"Easy", "Medium", "Difficult", "Nightmare"
@@ -122,6 +170,7 @@ public:
 	CMenuCoFDifficulty() : CMenuFramework( "CMenuCoFDifficulty" )
 	{
 		memset( skillBtn, 0, sizeof( skillBtn ));
+		m_iSelected = 0;
 		m_szCampaign[0] = m_szTitle[0] = m_szLabel[0] = m_szCommentaryLabel[0] = 0;
 	}
 
@@ -135,12 +184,21 @@ private:
 	static void StartGameCb( CMenuBaseItem *pSelf, void *pExtra );
 	void StartGame( int skillset );
 	void DevCommentaryChanged();
+	void CommentaryLabel();
 	void Refresh();
 
 	CEventCallback  skillCallback[COF_NUM_SKILLSETS];
 	CMenuPicButton *skillBtn[COF_NUM_SKILLSETS];
+	CMenuPicButton  skillRow[COF_NUM_SKILLSETS];
 	CMenuAction     campaignLabel;
+	CMenuAction     lockedNote;
 	CMenuCheckBox   devCommentary;
+	CMenuCheckBox   skipPrologue;
+
+	int  m_iSelected;
+	void SelectRow( int index );
+	void StartSelected();
+	static void SelectRowCb( CMenuBaseItem *pSelf, void *pExtra );
 
 	char m_szCampaign[64];
 	char m_szTitle[128];
@@ -148,6 +206,29 @@ private:
 	char m_szCommentaryLabel[CS_SIZE];
 };
 
+void CMenuCoFDifficulty::SelectRow( int index )
+{
+	if( index < 0 || index >= COF_NUM_SKILLSETS )
+		return;
+
+	m_iSelected = index;
+
+	for( int i = 0; i < COF_NUM_SKILLSETS; i++ )
+		skillRow[i].bThemeSelected = ( i == index );
+}
+
+void CMenuCoFDifficulty::SelectRowCb( CMenuBaseItem *pSelf, void *pExtra )
+{
+	CMenuCoFDifficulty *ui = (CMenuCoFDifficulty *)pSelf->Parent();
+
+	ui->SelectRow( (int)(size_t)pExtra - 1 );
+}
+
+void CMenuCoFDifficulty::StartSelected()
+{
+	StartGame( m_iSelected + 1 );
+}
+
 void CMenuCoFDifficulty::SetCampaign( const char *firstmap, const char *title )
 {
 	if( firstmap && firstmap[0] )
@@ -187,7 +268,8 @@ void CMenuCoFDifficulty::StartGame( int skillset )
 	if( skillset < 1 || skillset > COF_NUM_SKILLSETS )
 		return;
 
-	if( skillset == COF_NUM_SKILLSETS && EngFuncs::GetCvarFloat( "cof_nightmare_unlocked" ) == 0.0f )
+	// Nightmare needs the real flag, not a menu guess: unknown is not unlocked
+	if( skillset == COF_NUM_SKILLSETS && UI_CoFNightmareStatus() != 1 )
 		return;
 
 	// Never emit a level change with an empty map name: the engine's own
@@ -206,16 +288,43 @@ void CMenuCoFDifficulty::StartGame( int skillset )
 	EngFuncs::CvarSetValue( "coop", 0.0f );
 	EngFuncs::PlayBackgroundTrack( NULL, NULL );
 
+	// The original difficulty panel stopped the client's MP3 player before it
+	// changed level. "stopmp3" is the client's own console command (no
+	// arguments, unconditional stop) and is a different player from the
+	// engine's PlayBackgroundTrack above; without it the background map's music
+	// carries into the new game.
+	if( UI_IsCryOfFear( ))
+		EngFuncs::ClientCmd( false, "stopmp3\n" );
+
+	// Skip prologue: start the main campaign at the map the prologue leads to
+	// instead of at c_intro. Main campaign only - a custom campaign has its own
+	// first map and the checkbox is not shown for one. See COF_SKIP_PROLOGUE_MAP
+	// for why that map is c_nightmare and why it is safe.
+	const char *firstmap = m_szCampaign[0] ? m_szCampaign : COF_FIRST_MAP;
+
+	if( !m_szCampaign[0] && EngFuncs::GetCvarFloat( COF_SKIP_PROLOGUE_CVAR ) != 0.0f )
+	{
+		firstmap = COF_SKIP_PROLOGUE_MAP;
+		Con_Printf( "Cry of Fear: skip prologue is on, starting at \"%s\" instead of \"%s\"\n",
+			COF_SKIP_PROLOGUE_MAP, COF_FIRST_MAP );
+	}
+
 	if( EngFuncs::GetCvarFloat( "host_serverstate" ) != 0.0f )
 	{
-		if( m_szCampaign[0] )
-			EngFuncs::ClientCmdF( false, "cmd campaign %s\n", m_szCampaign );
+		// "campaign <map>" is the game DLL's own override for the map its
+		// skillset think loads (hl.dll ClientCommand at VA 10019C91 stores the
+		// name, the think at VA 100DFC10 formats "map %s\n" with it and falls
+		// back to "map c_intro\n"), so the skip rides the stock rails: the
+		// authentic white fade, inventory/game_start.wav and the difficulty
+		// write all still happen, only the destination differs.
+		if( strcmp( firstmap, COF_FIRST_MAP ))
+			EngFuncs::ClientCmdF( false, "cmd campaign %s\n", firstmap );
 		EngFuncs::ClientCmdF( false, "cmd skillset %d\n", skillset );
 	}
 	else
 	{
 		EngFuncs::CvarSetValue( "difficulty", (float)skillset );
-		EngFuncs::ClientCmdF( false, "map %s\n", m_szCampaign[0] ? m_szCampaign : "c_intro" );
+		EngFuncs::ClientCmdF( false, "map %s\n", firstmap );
 	}
 
 	UI_CloseMenu();
@@ -228,12 +337,30 @@ void CMenuCoFDifficulty::StartGameCb( CMenuBaseItem *pSelf, void *pExtra )
 	ui->StartGame( (int)(size_t)pExtra );
 }
 
+/*
+=================
+CMenuCoFDifficulty::CommentaryLabel
+
+The [ON ]/[OFF] text marker existed only because Cry of Fear ships no
+gfx/shell/cb_* artwork and the stock checkbox therefore drew nothing at all.
+The theme draws the box from primitives, so the marker is not needed.
+=================
+*/
+void CMenuCoFDifficulty::CommentaryLabel()
+{
+	if( UI_ThemeActive( ))
+		Q_strncpy( m_szCommentaryLabel, L( "Enable developer commentary" ), sizeof( m_szCommentaryLabel ));
+	else
+		snprintf( m_szCommentaryLabel, sizeof( m_szCommentaryLabel ), "%s %s",
+			devCommentary.bChecked ? "[ON ]" : "[OFF]", L( "Enable developer commentary" ));
+
+	devCommentary.szName = m_szCommentaryLabel;
+}
+
 void CMenuCoFDifficulty::DevCommentaryChanged()
 {
 	devCommentary.WriteCvar();
-	snprintf( m_szCommentaryLabel, sizeof( m_szCommentaryLabel ), "%s %s",
-		devCommentary.bChecked ? "[ON ]" : "[OFF]", L( "Enable developer commentary" ));
-	devCommentary.szName = m_szCommentaryLabel;
+	CommentaryLabel();
 }
 
 void CMenuCoFDifficulty::Refresh()
@@ -251,20 +378,43 @@ void CMenuCoFDifficulty::Refresh()
 		snprintf( m_szLabel, sizeof( m_szLabel ), "%s", L( "Cry of Fear" ));
 	campaignLabel.szName = m_szLabel;
 
-	const bool nightmare = EngFuncs::GetCvarFloat( "cof_nightmare_unlocked" ) != 0.0f;
+	const int  nightmareState = UI_CoFNightmareStatus();
+	const bool nightmare = ( nightmareState == 1 );
+	const char *nightmareWhy = ( nightmareState == 0 ) ?
+		"Nightmare has to be unlocked in game (scriptsettings.dat line 83)." :
+		"Nightmare state is unknown: cryoffear/scriptsettings.dat is missing or line 83 is unreadable.";
+
 	skillBtn[COF_NUM_SKILLSETS - 1]->szName = nightmare ? s_cofSkillName[COF_NUM_SKILLSETS - 1] : "Locked";
-	skillBtn[COF_NUM_SKILLSETS - 1]->szStatusText = nightmare ? s_cofSkillStatus[COF_NUM_SKILLSETS - 1] :
-		"Nightmare mode has to be unlocked in game (cof_nightmare_unlocked)";
+	skillBtn[COF_NUM_SKILLSETS - 1]->szStatusText = nightmare ? s_cofSkillStatus[COF_NUM_SKILLSETS - 1] : nightmareWhy;
 	skillBtn[COF_NUM_SKILLSETS - 1]->SetGrayed( !nightmare );
 
+	if( UI_ThemeActive( ))
+	{
+		// the locked row's explanation is a dim caption under the list instead
+		// of a status string only visible on hover
+		lockedNote.szName = nightmare ? "" : nightmareWhy;
+		lockedNote.SetVisibility( !nightmare );
+
+		if( m_iSelected == COF_NUM_SKILLSETS - 1 && !nightmare )
+			m_iSelected = 0;
+		SelectRow( m_iSelected );
+	}
+
 	devCommentary.UpdateEditable();
-	snprintf( m_szCommentaryLabel, sizeof( m_szCommentaryLabel ), "%s %s",
-		devCommentary.bChecked ? "[ON ]" : "[OFF]", L( "Enable developer commentary" ));
-	devCommentary.szName = m_szCommentaryLabel;
+	CommentaryLabel();
+
+	// Skip prologue is about the main campaign's intro map only; a custom
+	// campaign starts at its own first map and has no prologue to skip
+	skipPrologue.SetVisibility( m_szCampaign[0] == 0 );
+	skipPrologue.UpdateEditable();
 }
 
 void CMenuCoFDifficulty::Show()
 {
+	// the Nightmare row is the one thing on this page that depends on the
+	// player's progress, so re-read scriptsettings.dat before it is drawn
+	UI_CoFUnlockablesReload();
+
 	// base Show() runs Init() the first time, so the items only exist after it
 	CMenuFramework::Show();
 	Refresh();
@@ -272,18 +422,41 @@ void CMenuCoFDifficulty::Show()
 
 void CMenuCoFDifficulty::_Init( void )
 {
+	const bool theme = UI_ThemeActive();
+
 	banner.szName = L( "Choose a difficulty" );
 
+	if( theme )
+		// 440, not 400: the page grew a "Skip prologue" row in the
+		// 2026-09-22 round and at 400 the framework's status line at the
+		// bottom of the panel ran under the Cancel/Start row.
+		SetPanel( "NEW GAME", 560, 440 );
+
 	for( int i = 0; i < COF_NUM_SKILLSETS; i++ )
 	{
 		skillCallback[i] = StartGameCb;
 		skillCallback[i].pExtra = (void *)(size_t)( i + 1 );
-		skillBtn[i] = AddButton( s_cofSkillName[i], s_cofSkillStatus[i],
-			s_cofSkillPic[i], skillCallback[i], QMF_NOTIFY );
-	}
 
-	AddButton( L( "GameUI_Cancel" ), L( "Go back to the Main menu" ), PC_CANCEL,
-		VoidCb( &CMenuCoFDifficulty::Hide ), QMF_NOTIFY );
+		if( theme )
+		{
+			// the four difficulties are full-width selectable rows, not four
+			// WON pic buttons in a column
+			skillRow[i].SetNameAndStatus( s_cofSkillName[i], s_cofSkillStatus[i] );
+			skillRow[i].iFlags |= QMF_NOTIFY;
+			skillRow[i].eThemeStyle = CMenuPicButton::THEME_ROW;
+			skillRow[i].eTextAlignment = QM_LEFT;
+			skillRow[i].onReleased = skillCallback[i];
+			skillRow[i].onGotFocus = SelectRowCb;
+			skillRow[i].onGotFocus.pExtra = (void *)(size_t)( i + 1 );
+			AddItem( skillRow[i] );
+			skillBtn[i] = &skillRow[i];
+		}
+		else
+		{
+			skillBtn[i] = AddButton( s_cofSkillName[i], s_cofSkillStatus[i],
+				s_cofSkillPic[i], skillCallback[i], QMF_NOTIFY );
+		}
+	}
 
 	campaignLabel.iFlags = QMF_INACTIVE;
 	campaignLabel.colorBase = uiColorHelp;
@@ -291,21 +464,78 @@ void CMenuCoFDifficulty::_Init( void )
 	campaignLabel.szName = m_szLabel;
 	campaignLabel.SetCoord( 72, 200 );
 
+	lockedNote.iFlags = QMF_INACTIVE;
+	lockedNote.colorBase = uiColorHelp;
+	lockedNote.SetCharSize( QM_SMALLFONT );
+	lockedNote.szName = "";
+	lockedNote.SetVisibility( false );
+
 	devCommentary.SetCoord( 72, 230 + ( COF_NUM_SKILLSETS + 1 ) * 50 );
 	devCommentary.LinkCvar( "dev_commentary" );
 	devCommentary.UpdateEditable();
 	devCommentary.szStatusText = L( "Play with the developer commentary track" );
 	devCommentary.onChanged = VoidCb( &CMenuCoFDifficulty::DevCommentaryChanged );
 
-	AddItem( banner );
+	skipPrologue.szName = L( "Skip prologue" );
+	skipPrologue.szStatusText = L( "Start at c_nightmare, past the three-minute intro sequence. The intro grants nothing, so nothing is lost" );
+	skipPrologue.SetCoord( 72, 230 + ( COF_NUM_SKILLSETS + 2 ) * 50 );
+	skipPrologue.LinkCvar( COF_SKIP_PROLOGUE_CVAR );
+	skipPrologue.UpdateEditable();
+	skipPrologue.onChanged = CMenuEditable::WriteCvarCb;
+
+	if( !theme )
+		AddItem( banner );
 	AddItem( campaignLabel );
 	AddItem( devCommentary );
+	AddItem( skipPrologue );
+
+	if( theme )
+		AddItem( lockedNote );
+
+	// button row, secondary first: Cancel then Start
+	AddButton( L( "GameUI_Cancel" ), L( "Go back to the Main menu" ), PC_CANCEL,
+		VoidCb( &CMenuCoFDifficulty::Hide ), QMF_NOTIFY );
+
+	if( theme )
+		AddButton( L( "Start" ), L( "Start a new game on the selected difficulty" ),
+			PC_NEW_GAME, VoidCb( &CMenuCoFDifficulty::StartSelected ), QMF_NOTIFY );
 
 	Refresh();
 }
 
 void CMenuCoFDifficulty::_VidInit( void )
 {
+	if( IsPanel( ))
+	{
+		Rect c = ContentRect();
+
+		campaignLabel.SetRect( c.pt.x, c.pt.y, c.sz.w, THEME_LABEL_TALL );
+
+		int y = c.pt.y + THEME_LABEL_TALL + 14;
+
+		for( int i = 0; i < COF_NUM_SKILLSETS; i++ )
+		{
+			skillRow[i].SetRect( c.pt.x, y, c.sz.w, THEME_ROW_H );
+			y += THEME_ROW_PITCH;
+		}
+
+		y += 6;
+		lockedNote.SetRect( c.pt.x, y, c.sz.w, THEME_LABEL_TALL * 2 );
+
+		// the two checkboxes sit at the bottom of the panel, Skip prologue
+		// under the commentary box; when Skip prologue is hidden (a custom
+		// campaign) the commentary box keeps the bottom line to itself
+		if( skipPrologue.IsVisible( ))
+		{
+			devCommentary.SetCoord( c.pt.x, c.pt.y + c.sz.h - THEME_CHECK_SIZE - THEME_ROW_PITCH );
+			skipPrologue.SetCoord( c.pt.x, c.pt.y + c.sz.h - THEME_CHECK_SIZE );
+		}
+		else
+		{
+			devCommentary.SetCoord( c.pt.x, c.pt.y + c.sz.h - THEME_CHECK_SIZE );
+		}
+	}
+
 	Refresh();
 }
 
@@ -316,8 +546,11 @@ static CMenuCoFDifficulty *menu_cofdifficulty = NULL;
 static void UI_CoFDifficulty_Precache( void )
 {
 	menu_cofdifficulty = new CMenuCoFDifficulty();
-	EngFuncs::CvarRegister( "cof_nightmare_unlocked", "0", FCVAR_ARCHIVE );
+	// cof_nightmare_unlocked is gone: the flag is read from
+	// cryoffear/scriptsettings.dat line 83 (UI_CoFNightmareStatus)
 	EngFuncs::CvarRegister( "dev_commentary", "0", FCVAR_ARCHIVE );
+	// archived, default off; read in CMenuCoFDifficulty::StartGame
+	EngFuncs::CvarRegister( COF_SKIP_PROLOGUE_CVAR, "0", FCVAR_ARCHIVE );
 }
 
 static void UI_CoFDifficulty_Shutdown( void )
@@ -388,18 +621,23 @@ public:
 		// base Show() runs Init(), which is what creates play/list
 		CMenuFramework::Show();
 		model.Update();
+		UpdateDetail();
 	}
 
 	void Play( int line );
 
 private:
 	void _Init() override;
+	void _VidInit() override;
 	void PlaySelected();
+	void UpdateDetail();
 
 	CMenuPicButton play;
 	CMenuPicButton cancel;
 	CMenuTable     list;
 	CMenuAction    hint;
+	CMenuAction    detail;
+	char           m_szDetail[384];
 	CMenuCoFCampaignModel model;
 
 	friend class CMenuCoFCampaignModel;
@@ -505,13 +743,12 @@ void CMenuCoFCampaign::PlaySelected()
 
 void CMenuCoFCampaign::_Init( void )
 {
+	const bool theme = UI_ThemeActive();
+
 	banner.szName = L( "Custom Campaign" );
 
-	play.szName = L( "Play" );
-	play.SetPicture( PC_NEW_GAME );
-	play.iFlags |= QMF_NOTIFY;
-	play.onReleased = VoidCb( &CMenuCoFCampaign::PlaySelected );
-	play.SetCoord( 72, 230 );
+	if( theme )
+		SetPanel( "CUSTOM CAMPAIGN", 860, 520 );
 
 	cancel.szName = L( "GameUI_Cancel" );
 	cancel.SetPicture( PC_CANCEL );
@@ -519,12 +756,24 @@ void CMenuCoFCampaign::_Init( void )
 	cancel.onReleased = VoidCb( &CMenuCoFCampaign::Hide );
 	cancel.SetCoord( 72, 280 );
 
+	play.szName = L( "Play" );
+	play.SetPicture( PC_NEW_GAME );
+	play.iFlags |= QMF_NOTIFY;
+	play.onReleased = VoidCb( &CMenuCoFCampaign::PlaySelected );
+	play.SetCoord( 72, 230 );
+
 	hint.iFlags = QMF_INACTIVE;
 	hint.colorBase = uiColorHelp;
 	hint.SetCharSize( QM_SMALLFONT );
 	hint.szName = L( "From maps/*.custom" );
 	hint.SetCoord( 72, 340 );
 
+	detail.iFlags = QMF_INACTIVE;
+	detail.colorBase = uiColorHelp;
+	detail.SetCharSize( QM_SMALLFONT );
+	detail.szName = m_szDetail;
+	m_szDetail[0] = 0;
+
 	list.SetupColumn( 0, L( "Campaign" ), 0.45f );
 	list.SetupColumn( 1, L( "Author" ), 0.35f );
 	list.SetupColumn( 2, L( "First map" ), 0.20f );
@@ -532,6 +781,18 @@ void CMenuCoFCampaign::_Init( void )
 	list.SetCharSize( QM_SMALLFONT );
 	list.SetRect( 360, 230, -20, 465 );
 
+	if( theme )
+	{
+		list.onChanged = VoidCb( &CMenuCoFCampaign::UpdateDetail );
+
+		AddItem( list );
+		AddItem( hint );
+		AddItem( detail );
+		AddItem( cancel );
+		AddItem( play );
+		return;
+	}
+
 	AddItem( banner );
 	AddItem( play );
 	AddItem( cancel );
@@ -539,6 +800,44 @@ void CMenuCoFCampaign::_Init( void )
 	AddItem( list );
 }
 
+void CMenuCoFCampaign::_VidInit( void )
+{
+	if( IsPanel( ))
+	{
+		Rect left = ColumnRect2( 0, 0.70f );
+		Rect right = ColumnRect2( 1, 0.70f );
+
+		list.SetRect( left.pt.x, left.pt.y, left.sz.w, left.sz.h - THEME_LABEL_TALL - 10 );
+		hint.SetRect( left.pt.x, left.pt.y + left.sz.h - THEME_LABEL_TALL, left.sz.w, THEME_LABEL_TALL );
+		detail.SetRect( right.pt.x, right.pt.y, right.sz.w, right.sz.h );
+
+		CMenuPicButton *row[] = { &cancel, &play };
+		LayoutButtonRow( row, 2 );
+	}
+}
+
+/*
+=================
+CMenuCoFCampaign::UpdateDetail
+
+The panel's right-hand column repeats the selected campaign's description,
+author and first map as running text.
+=================
+*/
+void CMenuCoFCampaign::UpdateDetail( void )
+{
+	int line = list.GetCurrentIndex();
+
+	if( !model.IsValidIndex( line ))
+	{
+		m_szDetail[0] = 0;
+		return;
+	}
+
+	snprintf( m_szDetail, sizeof( m_szDetail ), "%s\n\n%s\n\nFirst map: %s",
+		model[line].description, model[line].author, model[line].firstmap );
+}
+
 ADD_MENU( menu_cofcampaign, CMenuCoFCampaign, UI_CoFCampaign_Menu );
 
 // ---------------------------------------------------------------------------
@@ -555,6 +854,48 @@ static const char *s_cofLanguage[COF_NUM_LANGUAGES] =
 	"English", "Dutch", "French", "German", "Norwegian", "Spanish", "Swedish"
 };
 
+// ---------------------------------------------------------------------------
+// Subtitle language, shared with the Game options page
+//
+// The original panel sends the bare game-DLL command "subtitleset N"; the
+// client answers it by writing the archived cvar cof_subtitlelanguage, which is
+// how the choice survives a restart. Without a server there is nothing to
+// forward to, so write the cvar directly - the same split the Language page
+// itself uses.
+// ---------------------------------------------------------------------------
+
+int UI_CoFSubtitleLanguageCount( void )
+{
+	return COF_NUM_LANGUAGES;
+}
+
+const char *UI_CoFSubtitleLanguageName( int index )
+{
+	if( index < 0 || index >= COF_NUM_LANGUAGES )
+		return "";
+	return s_cofLanguage[index];
+}
+
+int UI_CoFGetSubtitleLanguage( void )
+{
+	int index = (int)EngFuncs::GetCvarFloat( "cof_subtitlelanguage" );
+
+	if( index < 1 || index > COF_NUM_LANGUAGES )
+		index = 1;
+	return index;
+}
+
+void UI_CoFSetSubtitleLanguage( int index )
+{
+	if( index < 1 || index > COF_NUM_LANGUAGES )
+		return;
+
+	if( EngFuncs::GetCvarFloat( "host_serverstate" ) != 0.0f )
+		EngFuncs::ClientCmdF( false, "cmd subtitleset %d\n", index );
+	else
+		EngFuncs::CvarSetValue( "cof_subtitlelanguage", (float)index );
+}
+
 class CMenuCoFLanguage : public CMenuFramework
 {
 public:
@@ -569,7 +910,22 @@ public:
 
 private:
 	void _Init() override;
-	void _VidInit() override { Refresh(); }
+	void _VidInit() override
+	{
+		if( IsPanel( ))
+		{
+			for( int i = 0; i < COF_NUM_LANGUAGES; i++ )
+			{
+				Rect r = RowRect( i );
+
+				langRow[i].SetRect( r.pt.x, r.pt.y, r.sz.w, r.sz.h );
+			}
+
+			CMenuPicButton *row[] = { &close };
+			LayoutButtonRow( row, 1 );
+		}
+		Refresh();
+	}
 	void Refresh();
 
 	static void SetLanguageCb( CMenuBaseItem *pSelf, void *pExtra );
@@ -577,6 +933,8 @@ private:
 
 	CEventCallback  langCallback[COF_NUM_LANGUAGES];
 	CMenuPicButton *langBtn[COF_NUM_LANGUAGES];
+	CMenuPicButton  langRow[COF_NUM_LANGUAGES];
+	CMenuPicButton  close;
 	CMenuAction     current;
 	char            m_szCurrent[128];
 };
@@ -593,11 +951,7 @@ choice survives a restart (it is saved in cryoffear/config.cfg). Measured:
 */
 void CMenuCoFLanguage::SetLanguage( int index )
 {
-	if( EngFuncs::GetCvarFloat( "host_serverstate" ) != 0.0f )
-		EngFuncs::ClientCmdF( false, "cmd subtitleset %d\n", index );
-	else
-		EngFuncs::CvarSetValue( "cof_subtitlelanguage", (float)index );
-
+	UI_CoFSetSubtitleLanguage( index );
 	Refresh();
 }
 
@@ -610,29 +964,50 @@ void CMenuCoFLanguage::SetLanguageCb( CMenuBaseItem *pSelf, void *pExtra )
 
 void CMenuCoFLanguage::Refresh()
 {
-	int index = (int)EngFuncs::GetCvarFloat( "cof_subtitlelanguage" );
-
-	if( index < 1 || index > COF_NUM_LANGUAGES )
-		index = 1;
+	int index = UI_CoFGetSubtitleLanguage();
 
 	snprintf( m_szCurrent, sizeof( m_szCurrent ), "Subtitle language: %s", s_cofLanguage[index - 1] );
 	current.szName = m_szCurrent;
+
+	// the theme marks the current choice on the row itself, so the caption is
+	// redundant and is not shown
+	if( UI_ThemeActive( ))
+	{
+		for( int i = 0; i < COF_NUM_LANGUAGES; i++ )
+			langRow[i].bThemeSelected = ( i == index - 1 );
+	}
 }
 
 void CMenuCoFLanguage::_Init( void )
 {
+	const bool theme = UI_ThemeActive();
+
 	banner.szName = L( "Language" );
 
+	if( theme )
+		SetPanel( "LANGUAGE", 420, 420 );
+
 	for( int i = 0; i < COF_NUM_LANGUAGES; i++ )
 	{
 		langCallback[i] = SetLanguageCb;
 		langCallback[i].pExtra = (void *)(size_t)( i + 1 );
-		langBtn[i] = AddButton( s_cofLanguage[i], L( "Set the subtitle and document language" ),
-			PC_CUSTOMIZE, langCallback[i], QMF_NOTIFY );
-	}
 
-	AddButton( L( "Done" ), L( "Go back to the Main menu" ), PC_DONE,
-		VoidCb( &CMenuCoFLanguage::Hide ), QMF_NOTIFY );
+		if( theme )
+		{
+			langRow[i].SetNameAndStatus( s_cofLanguage[i], L( "Set the subtitle and document language" ));
+			langRow[i].iFlags |= QMF_NOTIFY;
+			langRow[i].eThemeStyle = CMenuPicButton::THEME_ROW;
+			langRow[i].eTextAlignment = QM_LEFT;
+			langRow[i].onReleased = langCallback[i];
+			AddItem( langRow[i] );
+			langBtn[i] = &langRow[i];
+		}
+		else
+		{
+			langBtn[i] = AddButton( s_cofLanguage[i], L( "Set the subtitle and document language" ),
+				PC_CUSTOMIZE, langCallback[i], QMF_NOTIFY );
+		}
+	}
 
 	current.iFlags = QMF_INACTIVE;
 	current.colorBase = uiColorHelp;
@@ -640,8 +1015,21 @@ void CMenuCoFLanguage::_Init( void )
 	current.szName = m_szCurrent;
 	current.SetCoord( 72, 200 );
 
-	AddItem( banner );
-	AddItem( current );
+	if( theme )
+	{
+		close.szName = L( "Close" );
+		close.SetPicture( PC_DONE );
+		close.iFlags |= QMF_NOTIFY;
+		close.onReleased = VoidCb( &CMenuCoFLanguage::Hide );
+		AddItem( close );
+	}
+	else
+	{
+		AddButton( L( "Done" ), L( "Go back to the Main menu" ), PC_DONE,
+			VoidCb( &CMenuCoFLanguage::Hide ), QMF_NOTIFY );
+		AddItem( banner );
+		AddItem( current );
+	}
 
 	Refresh();
 }
@@ -652,14 +1040,16 @@ ADD_MENU( menu_coflanguage, CMenuCoFLanguage, UI_CoFLanguage_Menu );
 // extras
 // ---------------------------------------------------------------------------
 
-#define COF_NUM_EXTRAS 6
+#define COF_NUM_EXTRAS 7
 
-// The six links of the original Extras panel, in its own order. The original
-// opened them with SteamFriends()->ActivateGameOverlayToWebPage and had no
-// fallback, so under FWGS every one of them was a silent no-op; ShellExecute
-// hands them to the system browser instead.
+// The six links of the original Extras panel, in its own order, with this
+// project's own page added first. The original opened them with
+// SteamFriends()->ActivateGameOverlayToWebPage and had no fallback, so under
+// FWGS every one of them was a silent no-op; ShellExecute hands them to the
+// system browser instead.
 static const char *s_cofExtraName[COF_NUM_EXTRAS] =
 {
+	"Cry of Fear: Enhanced",
 	"Donate",
 	"Soundtrack 1",
 	"Soundtrack 2",
@@ -670,6 +1060,7 @@ static const char *s_cofExtraName[COF_NUM_EXTRAS] =
 
 static const char *s_cofExtraUrl[COF_NUM_EXTRAS] =
 {
+	"https://cofenhanced.haej.pl",
 	"http://www.cry-of-fear.com/donate.php",
 	"http://www.cdbaby.com/cd/andreasronnberg",
 	"http://www.cdbaby.com/cd/andreasronnberg2",
@@ -685,10 +1076,14 @@ public:
 
 private:
 	void _Init() override;
+	void _VidInit() override;
 
 	static void OpenCb( CMenuBaseItem *pSelf, void *pExtra );
 
 	CEventCallback urlCallback[COF_NUM_EXTRAS];
+	CMenuPicButton extraRow[COF_NUM_EXTRAS];
+	CMenuAction    extraUrl[COF_NUM_EXTRAS];
+	CMenuPicButton close;
 };
 
 void CMenuCoFExtras::OpenCb( CMenuBaseItem *pSelf, void *pExtra )
@@ -703,14 +1098,50 @@ void CMenuCoFExtras::OpenCb( CMenuBaseItem *pSelf, void *pExtra )
 
 void CMenuCoFExtras::_Init( void )
 {
+	const bool theme = UI_ThemeActive();
+
 	banner.szName = L( "Extras" );
 
+	if( theme )
+		SetPanel( "EXTRAS", 560, 440 );
+
 	for( int i = 0; i < COF_NUM_EXTRAS; i++ )
 	{
 		urlCallback[i] = OpenCb;
 		urlCallback[i].pExtra = (void *)(size_t)i;
-		AddButton( s_cofExtraName[i], s_cofExtraUrl[i], PC_VISIT_WEB_SITE,
-			urlCallback[i], QMF_NOTIFY );
+
+		if( theme )
+		{
+			extraRow[i].SetNameAndStatus( s_cofExtraName[i], s_cofExtraUrl[i] );
+			extraRow[i].iFlags |= QMF_NOTIFY;
+			extraRow[i].eThemeStyle = CMenuPicButton::THEME_ROW;
+			extraRow[i].eTextAlignment = QM_LEFT;
+			extraRow[i].onReleased = urlCallback[i];
+			AddItem( extraRow[i] );
+
+			// the URL was only a hover status string; show it as a dim second
+			// line under each row instead
+			extraUrl[i].iFlags = QMF_INACTIVE;
+			extraUrl[i].colorBase = uiColorHelp;
+			extraUrl[i].SetCharSize( QM_SMALLFONT );
+			extraUrl[i].szName = s_cofExtraUrl[i];
+			AddItem( extraUrl[i] );
+		}
+		else
+		{
+			AddButton( s_cofExtraName[i], s_cofExtraUrl[i], PC_VISIT_WEB_SITE,
+				urlCallback[i], QMF_NOTIFY );
+		}
+	}
+
+	if( theme )
+	{
+		close.szName = L( "Close" );
+		close.SetPicture( PC_DONE );
+		close.iFlags |= QMF_NOTIFY;
+		close.onReleased = VoidCb( &CMenuCoFExtras::Hide );
+		AddItem( close );
+		return;
 	}
 
 	AddButton( L( "Done" ), L( "Go back to the Main menu" ), PC_DONE,
@@ -719,8 +1150,249 @@ void CMenuCoFExtras::_Init( void )
 	AddItem( banner );
 }
 
+void CMenuCoFExtras::_VidInit( void )
+{
+	if( !IsPanel( ))
+		return;
+
+	Rect c = ContentRect();
+	const int pitch = c.sz.h / COF_NUM_EXTRAS;
+	int y = c.pt.y;
+
+	for( int i = 0; i < COF_NUM_EXTRAS; i++ )
+	{
+		extraRow[i].SetRect( c.pt.x, y, c.sz.w, THEME_BODY_TALL + 6 );
+		extraUrl[i].SetRect( c.pt.x + 10, y + THEME_BODY_TALL + 6, c.sz.w - 10, THEME_LABEL_TALL );
+		y += pitch;
+	}
+
+	CMenuPicButton *row[] = { &close };
+	LayoutButtonRow( row, 1 );
+}
+
 ADD_MENU( menu_cofextras, CMenuCoFExtras, UI_CoFExtras_Menu );
 
+// ---------------------------------------------------------------------------
+// credits
+// ---------------------------------------------------------------------------
+
+/*
+=================
+CMenuCoFCredits
+
+Three sections, each a small uppercase section label, one or more body lines,
+and a link button that hands a URL to the system browser through
+EngFuncs::ShellExecute - the same route the Links page uses, because the
+original panel's SteamFriends()->ActivateGameOverlayToWebPage is a silent no-op
+under FWGS.
+
+The Polish section and the first name carry non-ASCII letters (l-stroke,
+o-diaeresis). The literals below are UTF-8 and the theme's primary font backend
+is stb_truetype over the shipped Inter faces (--enable-stbtt, see the Type
+section of docs/cof-ui-m3-theme.md), which rasterises by code point, so they
+render as themselves.
+
+One column, sized to fit 1280x720 without scrolling: a 640x470 panel in the
+1024x768 virtual space is 61 percent of the height, and the section pitch is
+derived from the content rect rather than hard-coded, so it follows the panel
+at every resolution.
+=================
+*/
+#define COF_NUM_CREDIT_SECTIONS 3
+#define COF_MAX_CREDIT_LINES    4
+
+struct cofCreditSection_t
+{
+	const char *label;
+	const char *lines[COF_MAX_CREDIT_LINES];
+	int         numLines;
+	int         dimFrom;   // first line index drawn dim; >= numLines for none
+	const char *linkName;
+	const char *linkUrl;
+};
+
+static const cofCreditSection_t s_cofCredits[COF_NUM_CREDIT_SECTIONS] =
+{
+	{
+		"TEAM PSYKSKALLAR",
+		{ "Andreas \"ruMpel\" Rönnberg and James \"Minuit\" Marchant",
+		  "Creators of Cry of Fear", NULL, NULL },
+		2, 1,
+		"YouTube", "https://www.youtube.com/@TeamPsykskallar"
+	},
+	{
+		"CRY OF FEAR: ENHANCED",
+		{ "haej", "Community-driven", NULL, NULL },
+		2, 1,
+		"Project site", "https://cofenhanced.haej.pl"
+	},
+	{
+		"CRY OF FEAR SPOLSZCZENIE (POLISH LOCALISATION)",
+		{ "Avioo - Tłumaczenie, grafiki, testowanie",
+		  "Mixdedemon - Tłumaczenie",
+		  "hexag0n - Technikalia, testowanie",
+		  "Izonka - Testowanie" },
+		4, 4,
+		"Steam Workshop", "https://steamcommunity.com/sharedfiles/filedetails/?id=3164091802"
+	}
+};
+
+class CMenuCoFCredits : public CMenuFramework
+{
+public:
+	CMenuCoFCredits() : CMenuFramework( "CMenuCoFCredits" ) { }
+
+private:
+	void _Init() override;
+	void _VidInit() override;
+
+	static void OpenCb( CMenuBaseItem *pSelf, void *pExtra );
+
+	CMenuAction    sectionLabel[COF_NUM_CREDIT_SECTIONS];
+	CMenuAction    bodyLine[COF_NUM_CREDIT_SECTIONS][COF_MAX_CREDIT_LINES];
+	CEventCallback linkCallback[COF_NUM_CREDIT_SECTIONS];
+	CMenuPicButton linkBtn[COF_NUM_CREDIT_SECTIONS];
+	CMenuPicButton close;
+};
+
+void CMenuCoFCredits::OpenCb( CMenuBaseItem *pSelf, void *pExtra )
+{
+	const int index = (int)(size_t)pExtra;
+
+	if( index < 0 || index >= COF_NUM_CREDIT_SECTIONS )
+		return;
+
+	EngFuncs::ShellExecute( s_cofCredits[index].linkUrl, NULL, false );
+}
+
+void CMenuCoFCredits::_Init( void )
+{
+	const bool theme = UI_ThemeActive();
+
+	banner.szName = L( "Credits" );
+
+	if( theme )
+		SetPanel( "CREDITS", 640, 470 );
+
+	for( int s = 0; s < COF_NUM_CREDIT_SECTIONS; s++ )
+	{
+		const cofCreditSection_t *sec = &s_cofCredits[s];
+
+		sectionLabel[s].iFlags = QMF_INACTIVE;
+		sectionLabel[s].colorBase = uiColorHelp;
+		sectionLabel[s].SetCharSize( QM_SMALLFONT );
+		sectionLabel[s].szName = sec->label;
+		AddItem( sectionLabel[s] );
+
+		for( int i = 0; i < sec->numLines; i++ )
+		{
+			bodyLine[s][i].iFlags = QMF_INACTIVE;
+			bodyLine[s][i].colorBase = ( i >= sec->dimFrom ) ? uiColorHelp : uiColorWhite;
+			bodyLine[s][i].SetCharSize( QM_SMALLFONT );
+			bodyLine[s][i].szName = sec->lines[i];
+			AddItem( bodyLine[s][i] );
+		}
+
+		linkCallback[s] = OpenCb;
+		linkCallback[s].pExtra = (void *)(size_t)s;
+
+		if( theme )
+		{
+			linkBtn[s].SetNameAndStatus( sec->linkName, sec->linkUrl );
+			linkBtn[s].iFlags |= QMF_NOTIFY;
+			linkBtn[s].eThemeStyle = CMenuPicButton::THEME_ROW;
+			linkBtn[s].eTextAlignment = QM_CENTER;
+			linkBtn[s].onReleased = linkCallback[s];
+			AddItem( linkBtn[s] );
+		}
+		else
+		{
+			AddButton( sec->linkName, sec->linkUrl, PC_VISIT_WEB_SITE,
+				linkCallback[s], QMF_NOTIFY );
+		}
+	}
+
+	if( theme )
+	{
+		close.szName = L( "Close" );
+		close.SetPicture( PC_DONE );
+		close.iFlags |= QMF_NOTIFY;
+		close.onReleased = VoidCb( &CMenuCoFCredits::Hide );
+		AddItem( close );
+		return;
+	}
+
+	AddButton( L( "Done" ), L( "Go back to the Main menu" ), PC_DONE,
+		VoidCb( &CMenuCoFCredits::Hide ), QMF_NOTIFY );
+
+	AddItem( banner );
+}
+
+void CMenuCoFCredits::_VidInit( void )
+{
+	if( !IsPanel( ))
+		return;
+
+	Rect c = ContentRect();
+
+	// every section gets the same slice of the content rect, so the spacing
+	// stays generous and nothing is pinned to one resolution
+	const int pitch = c.sz.h / COF_NUM_CREDIT_SECTIONS;
+	int y = c.pt.y;
+
+	// one width for all three link buttons, measured from the longest label so
+	// "Steam Workshop" is not clipped to "Steam ...". THEME_BTN_MIN_W is 110
+	// virtual units and two of the three labels are wider than that.
+	int btnW = THEME_BTN_MIN_W;
+
+	if( uiStatic.scaleX > 0.0f )
+	{
+		const int chSize = (int)( THEME_BODY_TALL * uiStatic.scaleY );
+
+		for( int s = 0; s < COF_NUM_CREDIT_SECTIONS; s++ )
+		{
+			int w = (int)( g_FontMgr->GetTextWideScaled( uiStatic.hThemeBody,
+				s_cofCredits[s].linkName, chSize ) / uiStatic.scaleX ) + 24;
+
+			if( w > btnW )
+				btnW = w;
+		}
+	}
+
+	for( int s = 0; s < COF_NUM_CREDIT_SECTIONS; s++ )
+	{
+		const cofCreditSection_t *sec = &s_cofCredits[s];
+		int ly = y;
+
+		sectionLabel[s].SetRect( c.pt.x, ly, c.sz.w, THEME_LABEL_TALL );
+		ly += THEME_LABEL_TALL + 8;
+
+		for( int i = 0; i < sec->numLines; i++ )
+		{
+			// only the line the link button sits beside has to make room for
+			// it; the others get the full content width, otherwise the long
+			// first line of the first section is clipped
+			const bool beside = ( i == sec->numLines - 1 );
+
+			bodyLine[s][i].SetRect( c.pt.x, ly,
+				beside ? c.sz.w - btnW - THEME_BTN_GAP : c.sz.w, THEME_LABEL_TALL );
+			ly += THEME_LABEL_TALL + 4;
+		}
+
+		// the link button is right-aligned, level with the section's last line
+		linkBtn[s].SetRect( c.pt.x + c.sz.w - btnW,
+			ly - ( THEME_LABEL_TALL + 4 ) - ( THEME_ROW_H - THEME_LABEL_TALL ) / 2,
+			btnW, THEME_ROW_H );
+
+		y += pitch;
+	}
+
+	CMenuPicButton *row[] = { &close };
+	LayoutButtonRow( row, 1 );
+}
+
+ADD_MENU( menu_cofcredits, CMenuCoFCredits, UI_CoFCredits_Menu );
+
 // ---------------------------------------------------------------------------
 // unlockables
 // ---------------------------------------------------------------------------
@@ -734,6 +1406,11 @@ obfuscated tokens compared against a table compiled into client.dll, so the
 menu library cannot render the gallery itself. Run the client's own entry
 command instead, which is what the original main menu did; it loads
 maps/c_unlockables.bsp, where the gallery entity draws the real state.
+
+This is now the "Open original gallery" button of the Unlockables page rather
+than the main menu's own route: the gallery panel the client draws on that map
+is mispositioned at high resolutions, and its MAIN MENU button returns to the
+client's old menu.
 =================
 */
 void UI_CoFUnlockables( void )
@@ -746,3 +1423,732 @@ void UI_CoFUnlockables( void )
 	EngFuncs::ClientCmd( false, "unlockablescmd\n" );
 	UI_CloseMenu();
 }
+
+// ---------------------------------------------------------------------------
+// unlockables page
+//
+// The unlock state is real, and this is how it is read.
+//
+// MEASURED, cryoffear/scriptsettings.dat (canonical copy, 2222 bytes, SHA-256
+// 194E7144C398A7176D7BCA8543FFD4547949AC998E6F38FA38982DC7D6295F1E): 101 lines
+// of exactly 20 bytes separated by CRLF, with a trailing CRLF after the last
+// one (101 * 20 + 101 * 2 = 2222). Line bytes are printable ASCII plus the
+// single extended byte 0xA3 - the character the Steam guide renders as "£".
+// There is no header, no magic and no length prefix.
+//
+// MEASURED, client.dll .rdata: immediately after the "/alt.dat" literal
+// (VA 1014118C) the client carries 34 pairs of 20-byte tokens, each stored as
+// <20 bytes>'\n' padded to 24, running from VA 10141198 to VA 101417F7 and
+// followed by the unrelated "Ending seen: %d" strings. The two members of a
+// pair differ in exactly one character. That is the whole "encryption": a flag
+// is set by writing one of its two tokens into its own line of the file, and
+// checked by comparing the line to both. (The '\n' terminator in the binary is
+// why the file is CRLF: the client writes the token through a text-mode
+// stream.)
+//
+// MEASURED, this session: every one of the 34 pairs matches exactly one line
+// of the canonical scriptsettings.dat, and the 34 lines are all distinct, so
+// the pair -> line mapping below is derived from the file itself, not guessed.
+// All 27 named entries additionally match the Steam guide's table byte for
+// byte, including the 0xA3 bytes - which is what pins the names to the lines.
+// The first-listed member of each pair is the locked token (measured for the
+// 27 named entries against the guide; assumed for the other 7, and consistent
+// with a fresh install holding the first member on all 34 lines).
+//
+// The unlock *conditions* are still almost entirely untextual: a full sweep of
+// the shipped data (txtfiles, notes, resource, scripts, the gallery map's
+// entity lump) and of both game DLLs' string tables found only the
+// "Unlocked: ..." announcements, the gallery art names, and five hint-page
+// titles in hl.dll ("How To Unlock Book Pages", "Ranking System Explained",
+// "How To Unlock Hoodies", "How To Unlock Secret Items", "How To Unlock Secret
+// Weapons") - whose order matches the guide's five hint-page lines 76..80
+// exactly. The pages themselves are rasterised textures inside
+// models/bookmenu.mdl (page, page1..page5), so no string form exists. The
+// "how" column is therefore blank wherever nothing is measured; nothing here
+// is invented.
+// ---------------------------------------------------------------------------
+
+#define COF_NUM_UNLOCKABLES  34
+#define COF_TOKEN_LEN        20
+#define COF_UNLOCK_NIGHTMARE 33  // index of scriptsettings.dat line 83, below
+
+typedef struct
+{
+	int         line;     // 1-based line number in cryoffear/scriptsettings.dat
+	int         pair;     // index of the pair in client.dll's .rdata table
+	const char *name;
+	const char *how;      // "" when no condition is measured - never invented
+	const char *locked;   // the client's own bytes, 0xA3 written as \243
+	const char *unlocked;
+} cofUnlockable_t;
+
+static const cofUnlockable_t s_cofUnlockables[COF_NUM_UNLOCKABLES] =
+{
+	{  3,  4, "Secret room, empty hint book, MP5",
+		"One flag for all three; MP5 via Steam group",
+		"z3,4lj)4b!9g7vgi4x5a",
+		"z3,4rj)4b!9g7vgi4x5a" },
+	{  7,  1, "Unidentified flag (line 7)",
+		"",
+		"ye&ole1^0(fcanb3v5t4",
+		"ye&0le1^0(fcanb3v5t4" },
+	{ 15,  0, "Unidentified flag (line 15)",
+		"",
+		"v9agjak+p)(,o3qb)aq4",
+		"v9agjak+p)(,03qb)aq4" },
+	{ 22,  3, "Unidentified flag (line 22)",
+		"",
+		"2quwo6z3y4w\243ld)y!i35",
+		"2quvo6z3y4w\243ld)y!i35" },
+	{ 39,  5, "FAMAS with infinite ammo",
+		"",
+		"r1&,iji4cjtvf,44w919",
+		"r1&,iji4cjtvf.44w919" },
+	{ 45,  2, "Unidentified flag (line 45)",
+		"",
+		"1r6fd46ob7u2yj9q83yx",
+		"1r6fd46ob7u2yj9r83yx" },
+	{ 47, 32, "Unidentified flag (line 47)",
+		"",
+		"g_t\2430j^zrqhli09jb5rc",
+		"c_t\2430j^zrqhli09jb5rc" },
+	{ 48, 33, "Unidentified flag (line 48)",
+		"",
+		"kk78kewvx9!%n218dkss",
+		"ck78kewvx9!%n218dkss" },
+	{ 51,  6, "David Leatherhoff suit",
+		"",
+		"olt0\243s^\243rn(aki!9eqkh",
+		"olt0\243s^\243rn(aki!9eqkj" },
+	{ 52,  7, "ModDB hoodie",
+		"",
+		"h*wchks\243n&%!\243bf%jtew",
+		"h*wchks\243n&%!\243bf%jtei" },
+	{ 53,  8, "Hello Kitty suit",
+		"",
+		"%xn%i)jhxmfnbgjsr^if",
+		"\243xn%i)jhxmfnbgjsr^if" },
+	{ 54,  9, "Afraid of Monsters suit",
+		"",
+		"fy^xix&&vkz)vcdn!wtv",
+		"fy^xix&&vkz)vcrn!wtv" },
+	{ 55, 10, "Camouflage hoodie",
+		"",
+		"jwsyv**yb&(qtqlg)moj",
+		"jwtyv**yb&(qtqlg)moj" },
+	{ 56, 11, "Half Life Creations hoodie",
+		"",
+		"henp(cm$at!n%\243f(c$jp",
+		"henp(cm$at!m%\243f(c$jp" },
+	{ 57, 12, "Black Metal suit",
+		"",
+		"yef!xkyzw*^vd*tj)zkc",
+		"yef!&kyzw*^vd*tj)zkc" },
+	{ 58, 13, "Team Psykskallar hoodie",
+		"",
+		"z(*mbwy(talqlrqf$lmw",
+		"z(*mbwy(tolqlrqf$lmw" },
+	{ 59, 14, "Awesome suit",
+		"",
+		"zgpwg\243vyqlzensuauwat",
+		"zwpwg\243vyqlzensuauwat" },
+	{ 60, 15, "Sick Simon suit",
+		"",
+		"dzpy)*iiy$oj!akyfbn%",
+		"dzpy)*iiy$0j!akyfbn%" },
+	{ 61, 16, "AoM Twitcher suit",
+		"",
+		"pe%%*\243\243rxa(%(bog)l^j",
+		"pe%%*\243\243rxr(%(bog)l^j" },
+	{ 62, 17, "Custom hoodies",
+		"",
+		"efq%)x)mk\243*odxsdocb)",
+		"efq%)u)mk\243*odxsdocb)" },
+	{ 70, 18, "David Leatherhoff's axe",
+		"",
+		"pzakmx&p^lbk^kw($(fo",
+		"pzakhx&p^lbk^kw($(fo" },
+	{ 71, 19, "Digital camera",
+		"",
+		"g*q$cceqhz&vcdp(au%!",
+		"g*q$cceqhz&vcdp(av%!" },
+	{ 72, 20, "Simon's book",
+		"",
+		"aaa^$i)whenzprdh!vus",
+		"aba^$i)whenzprdh!vus" },
+	{ 73, 21, "Developer commentary",
+		"Enable it on the New Game page once unlocked",
+		"mwu\243g$&ztmkfwej(bg^a",
+		"mwu\243g$&stmkfwej(bg^a" },
+	{ 74, 22, "Hidden package",
+		"Tied to the secret ending",
+		"tzi&aoxfbkb^n&ikaxj%",
+		"tzi&a0xfbkb^n&ikaxj%" },
+	{ 75, 23, "Gasmask (night vision)",
+		"",
+		"wph!uhhgrws)*erdb*rg",
+		"wph!uhmgrws)*erdb*rg" },
+	{ 76, 24, "First hint page",
+		"In-game hint page \"How To Unlock Book Pages\"",
+		"&x&w%dvcr&$yoqs%c%ar",
+		"&x&v%dvcr&$yoqs%c%ar" },
+	{ 77, 25, "Second hint page",
+		"In-game hint page \"Ranking System Explained\"",
+		"feyvvuxq^i)brfentm$&",
+		"foyvvuxq^i)brfentm$&" },
+	{ 78, 26, "Third hint page",
+		"In-game hint page \"How To Unlock Hoodies\"",
+		"*j%gb*^noyresqjihoez",
+		"*j%gb*^noyresqjihoes" },
+	{ 79, 27, "Fourth hint page",
+		"In-game hint page \"How To Unlock Secret Items\"",
+		"nz(xdxj*w(\243!&uh\243qn*j",
+		"nz(xdxj*w)\243!&uh\243qn*j" },
+	{ 80, 28, "Fifth hint page",
+		"In-game hint page \"How To Unlock Secret Weapons\"",
+		"lvdw&bhwjqsjm$pfwnp)",
+		"rvdw&bhwjqsjm$pfwnp)" },
+	{ 81, 29, "Unidentified flag (line 81)",
+		"",
+		"zqv%n(nqquvh)dlmtl!x",
+		"zqv%n(noquvh)dlmtl!x" },
+	{ 82, 30, "Doctor mode",
+		"",
+		")cfhc$vle$^hn\243padlvd",
+		")cfhc$vle$^hn\243podlvd" },
+	{ 83, 31, "Nightmare difficulty",
+		"",
+		"&fx!hd$bw^ayk%dfmcbg",
+		"&fx!hd$bw!ayk%dfmcbg" },
+};
+
+// -1 unknown, 0 locked, 1 unlocked, one entry per row of the table above.
+static signed char s_cofUnlockState[COF_NUM_UNLOCKABLES];
+static bool        s_cofUnlockRead = false;
+
+/*
+=================
+UI_CoFDatLine
+
+The 1-based line lineNo of a scriptsettings.dat buffer, without its line
+terminator. Splitting on '\n' and dropping a trailing '\r' rather than striding
+by a fixed 22 bytes, so a file written with LF only still reads correctly.
+Returns false when the file is too short.
+=================
+*/
+static bool UI_CoFDatLine( const char *buf, int len, int lineNo, const char **out, int *outLen )
+{
+	int at = 1;
+	int start = 0;
+	int i;
+
+	for( i = 0; i <= len; i++ )
+	{
+		if( i == len || buf[i] == '\n' )
+		{
+			if( at == lineNo )
+			{
+				int end = i;
+
+				if( end > start && buf[end - 1] == '\r' )
+					end--;
+
+				// a trailing CRLF makes the last split empty; that is not a line
+				if( i == len && end == start )
+					return false;
+
+				*out = buf + start;
+				*outLen = end - start;
+				return true;
+			}
+			at++;
+			start = i + 1;
+		}
+	}
+	return false;
+}
+
+/*
+=================
+UI_CoFUnlockablesReload
+
+Read cryoffear/scriptsettings.dat once and resolve all 34 flags from it. The
+file is 2 KiB, so this is cheap enough to redo whenever a page that shows the
+state opens; nothing is cached across a page visit on purpose, because the
+player can unlock something and come straight back to the menu.
+
+Every flag whose line is missing, short, or equal to neither of its two tokens
+stays unknown. A missing file leaves all 34 unknown rather than pretending
+everything is locked - an empty or replaced file is not the same fact as a
+locked flag.
+=================
+*/
+void UI_CoFUnlockablesReload( void )
+{
+	char *buf;
+	int   len = 0;
+	int   i;
+
+	for( i = 0; i < COF_NUM_UNLOCKABLES; i++ )
+		s_cofUnlockState[i] = -1;
+
+	s_cofUnlockRead = true;
+
+	if( !UI_IsCryOfFear( ))
+		return;
+
+	buf = (char *)EngFuncs::COM_LoadFile( "scriptsettings.dat", &len );
+	if( !buf )
+	{
+		Con_DPrintf( "Cry of Fear: scriptsettings.dat not found, unlock state unknown\n" );
+		return;
+	}
+
+	for( i = 0; i < COF_NUM_UNLOCKABLES; i++ )
+	{
+		const char *line;
+		int lineLen = 0;
+
+		if( !UI_CoFDatLine( buf, len, s_cofUnlockables[i].line, &line, &lineLen ))
+			continue;
+		if( lineLen != COF_TOKEN_LEN )
+			continue;
+
+		if( !memcmp( line, s_cofUnlockables[i].unlocked, COF_TOKEN_LEN ))
+			s_cofUnlockState[i] = 1;
+		else if( !memcmp( line, s_cofUnlockables[i].locked, COF_TOKEN_LEN ))
+			s_cofUnlockState[i] = 0;
+	}
+
+	EngFuncs::COM_FreeFile( (byte *)buf );
+}
+
+/*
+=================
+UI_CoFUnlockableStatus
+
+-1 unknown, 0 locked, 1 unlocked. Reads the file lazily the first time, so a
+caller that forgets to reload still gets a real answer.
+=================
+*/
+int UI_CoFUnlockableStatus( int index )
+{
+	if( index < 0 || index >= COF_NUM_UNLOCKABLES )
+		return -1;
+
+	if( !s_cofUnlockRead )
+		UI_CoFUnlockablesReload();
+
+	return s_cofUnlockState[index];
+}
+
+int UI_CoFNightmareStatus( void )
+{
+	return UI_CoFUnlockableStatus( COF_UNLOCK_NIGHTMARE );
+}
+
+class CMenuCoFUnlockables;
+
+class CMenuCoFUnlockablesModel : public CMenuBaseModel
+{
+public:
+	void Update() override { }
+	int GetColumns() const override { return 3; }
+	int GetRows() const override { return COF_NUM_UNLOCKABLES; }
+	const char *GetCellText( int line, int column ) override
+	{
+		if( line < 0 || line >= COF_NUM_UNLOCKABLES )
+			return NULL;
+
+		switch( column )
+		{
+		case 0: return s_cofUnlockables[line].name;
+		case 1: return s_cofUnlockables[line].how;
+		case 2:
+			switch( UI_CoFUnlockableStatus( line ))
+			{
+			case 1:  return "Unlocked";
+			case 0:  return "Locked";
+			default: return "?";
+			}
+		}
+		return NULL;
+	}
+
+	// The status column carries the theme's one accent when a flag is set and
+	// the dim body colour when it is not, so the page reads at a glance. Every
+	// other column keeps the table's own row colour.
+	bool GetCellColors( int line, int column, unsigned int &textColor, bool &force ) const override
+	{
+		if( column != 2 || !UI_ThemeActive( ) ||
+			line < 0 || line >= COF_NUM_UNLOCKABLES )
+			return false;
+
+		force = true;
+
+		switch( s_cofUnlockRead ? s_cofUnlockState[line] : -1 )
+		{
+		case 1:  textColor = THEME_ACCENT; break;
+		case 0:  textColor = THEME_TEXT_DIM; break;
+		default: textColor = THEME_DISABLED; break;
+		}
+		return true;
+	}
+};
+
+class CMenuCoFUnlockables : public CMenuFramework
+{
+public:
+	CMenuCoFUnlockables() : CMenuFramework( "CMenuCoFUnlockables" ) { }
+
+	// the page is the one place the state is shown, so re-read the file every
+	// time it opens rather than trusting whatever the last visit saw
+	void Show() override
+	{
+		UI_CoFUnlockablesReload();
+		CMenuFramework::Show();
+	}
+
+private:
+	void _Init() override;
+	void _VidInit() override;
+
+	CMenuTable     list;
+	CMenuAction    hint;
+	CMenuPicButton gallery;
+	CMenuPicButton close;
+	CMenuCoFUnlockablesModel model;
+};
+
+void CMenuCoFUnlockables::_Init( void )
+{
+	const bool theme = UI_ThemeActive();
+
+	banner.szName = L( "Unlockables" );
+
+	if( theme )
+		SetPanel( "UNLOCKABLES", 940, 600 );
+
+	list.SetupColumn( 0, L( "Unlockable" ), 0.42f );
+	list.SetupColumn( 1, L( "How to unlock" ), 0.42f );
+	list.SetupColumn( 2, L( "Status" ), 0.16f );
+	list.SetModel( &model );
+	list.SetCharSize( QM_SMALLFONT );
+	list.SetRect( 360, 230, -20, 465 );
+
+	hint.iFlags = QMF_INACTIVE;
+	hint.colorBase = uiColorHelp;
+	hint.SetCharSize( QM_SMALLFONT );
+	hint.szName = "Status comes from cryoffear/scriptsettings.dat: each flag is one 20-byte line, compared to the two tokens client.dll carries for it. \"?\" means the file is missing or the line matches neither. Seven flags have no known name.";
+
+	gallery.szName = L( "Open original gallery" );
+	gallery.SetPicture( PC_VIEW_GAME_INFO );
+	gallery.iFlags |= QMF_NOTIFY;
+	gallery.szStatusText = "Load the c_unlockables gallery map, the client's own screen";
+	gallery.onReleased = UI_CoFUnlockables;
+	gallery.SetCoord( 72, 230 );
+
+	close.szName = L( "Close" );
+	close.SetPicture( PC_DONE );
+	close.iFlags |= QMF_NOTIFY;
+	close.onReleased = VoidCb( &CMenuCoFUnlockables::Hide );
+	close.SetCoord( 72, 280 );
+
+	if( !theme )
+		AddItem( banner );
+
+	AddItem( list );
+	AddItem( hint );
+	AddItem( gallery );
+	AddItem( close );
+}
+
+void CMenuCoFUnlockables::_VidInit( void )
+{
+	if( !IsPanel( ))
+		return;
+
+	Rect c = ContentRect();
+	const int hintH = THEME_LABEL_TALL * 2 + 4;
+
+	list.SetRect( c.pt.x, c.pt.y, c.sz.w, c.sz.h - hintH - 10 );
+	hint.SetRect( c.pt.x, c.pt.y + c.sz.h - hintH, c.sz.w, hintH );
+
+	CMenuPicButton *row[] = { &gallery, &close };
+	LayoutButtonRow( row, 2 );
+}
+
+ADD_MENU( menu_cofunlockables, CMenuCoFUnlockables, UI_CoFUnlockables_Menu );
+
+// ---------------------------------------------------------------------------
+// death page (command menu_cofdeath, opened by the engine)
+//
+// Measured, in the shipped binaries and in the engine fixture
+// (docs/cof-ui-death-flow.md):
+//
+//   * dying sends one user message, "VGUIMenu" with a first byte of 35;
+//     client.dll's CClientViewport::ShowVGUIMenu maps index 35 onto CGameOver
+//     (ctor at VA 10040620, the panel that loads gfx/vgui/game_over*.tga and
+//     plays game_over.mp3), and nothing else in the game sends index 35;
+//   * the panel is "GAME OVER" over the still-running scene with two text
+//     links, LOAD GAME and EXIT, each on its own faint dark strip. No panel,
+//     no scrim, and the world keeps rendering;
+//   * LOAD GAME (command id 1, CGameOverHandler_Command::actionPerformed at
+//     VA 10040B40) runs no console command in single player: it hides itself
+//     and shows the client's own save/load panel (gViewport+0x1464). In coop
+//     (the [105431DC] arm) it is a RESTART instead and issues "map <level>";
+//   * EXIT (command id 2) hides itself and issues, through pfnClientCmd,
+//     "map c_game_menu1" (the push at VA 10040B74) - the very command
+//     cof_ui_menu_map_redirect already turns into disconnect + menu_main +
+//     map_background;
+//   * nothing else follows. No stufftext arrives and the game never returns to
+//     the menu map by itself, so there is no auto-return to fight.
+//
+// This page is that screen in our theme: the same structure, the same two
+// choices, the same end states.
+// ---------------------------------------------------------------------------
+
+/*
+=================
+CMenuCoFDeathTitle
+
+"GAME OVER" in the main menu's wordmark handle, centred, on its own backing
+strip. Tracked like the wordmark, because UI_DrawString cannot add advance.
+=================
+*/
+class CMenuCoFDeathTitle : public CMenuBaseItem
+{
+public:
+	CMenuCoFDeathTitle()
+	{
+		iFlags = QMF_INACTIVE;
+		szName = "GAME OVER";
+	}
+
+	void Draw() override;
+
+	static int Tall() { return THEME_LOGO_TALL; }
+};
+
+void CMenuCoFDeathTitle::Draw()
+{
+	const int tall  = (int)( THEME_LOGO_TALL * uiStatic.scaleY );
+	const int track = (int)( THEME_LOGO_TRACKING * uiStatic.scaleX );
+	const int w = UI_ThemeTrackedWidth( uiStatic.hThemeLogo, szName, tall, track );
+	const int x = m_scPos.x + ( m_scSize.w - w ) / 2;
+
+	UI_ThemeDeathBacking( x, m_scPos.y, w, tall );
+	UI_ThemeDrawTracked( uiStatic.hThemeLogo, x, m_scPos.y, szName, THEME_TEXT_HI, tall, track );
+}
+
+/*
+=================
+CMenuCoFDeathItem
+
+A main-menu style item with the original's backing strip behind its own text.
+Everything else - hover, keyboard focus, the click - is the stock pic button.
+=================
+*/
+class CMenuCoFDeathItem : public CMenuPicButton
+{
+public:
+	void Draw() override
+	{
+		if( UI_ThemeActive( ))
+		{
+			int w = g_FontMgr->GetTextWideScaled( font, szName, m_scChSize );
+			int x = m_scPos.x + ( m_scSize.w - w ) / 2;
+
+			UI_ThemeDeathBacking( x, m_scPos.y + ( m_scSize.h - m_scChSize ) / 2, w, m_scChSize );
+		}
+
+		CMenuPicButton::Draw();
+	}
+};
+
+class CMenuCoFDeath : public CMenuFramework
+{
+public:
+	CMenuCoFDeath() : CMenuFramework( "CMenuCoFDeath" ) { }
+
+	void Show() override;
+	void Hide() override;
+	void Think() override;
+
+	// Escape has nowhere to go from here: the session is over and the only
+	// window on the stack is this one. Swallow it rather than drop the player
+	// back onto the client's own panel with no way to reach either choice.
+	bool KeyDown( int key ) override
+	{
+		if( UI::Key::IsEscape( key ))
+			return true;
+
+		return CMenuFramework::KeyDown( key );
+	}
+
+private:
+	void _Init() override;
+	void _VidInit() override;
+
+	void LoadCb();
+	void ExitCb();
+
+	CMenuCoFDeathTitle title;
+	CMenuCoFDeathItem  load;
+	CMenuCoFDeathItem  exit;
+};
+
+/*
+=================
+CMenuCoFDeath::LoadCb
+
+The original's LOAD GAME opened the client's own save/load panel, which is
+gated off with the rest of the client layer while our menu has focus. Our Load
+Game page is the replacement, pushed over this one so its Cancel comes back
+here - which is what the original did too (the client re-shows CGameOver from
+its save/load panel's back button while the player is still dead, client.dll
+VA 100315B0).
+=================
+*/
+void CMenuCoFDeath::LoadCb()
+{
+	UI_LoadGame_Menu();
+}
+
+/*
+=================
+CMenuCoFDeath::ExitCb
+
+The original's EXIT issued "map c_game_menu1" through pfnClientCmd, which the
+engine's cof_ui_menu_map_redirect turns into disconnect + menu_main +
+map_background. Emit exactly that, so this button and the original link take
+the same path and reach the same state; with the redirect switched off, ask the
+engine for the return sequence directly instead of loading the old menu map.
+=================
+*/
+void CMenuCoFDeath::ExitCb()
+{
+	if( EngFuncs::GetCvarFloat( "cof_ui_menu_map_redirect" ) != 0.0f )
+		EngFuncs::ClientCmd( false, "map c_game_menu1\n" );
+	else
+		EngFuncs::ClientCmd( false, "cof_ui_menu_return\n" );
+}
+
+/*
+=================
+CMenuCoFDeath::Show
+
+REVERSED 2026-09-22, by the user's own play feedback. This page used to issue
+the client's "stopmp3" here and once more on its first Think frame, to silence
+game_over.mp3 - measured ordering, docs/cof-ui-death-flow.md section 1.1: the
+client's CGameOver::setVisible (client.dll VA 10040E60) starts that track while
+it handles the same "VGUIMenu" message the engine latched, so a stop issued from
+Show() always landed after it.
+
+That was the wrong call. The original death screen is a panel over a still
+running game: the ambience, the rain, a chainsaw that was already running and
+the death music all keep playing while the player reads the screen. Silencing
+the music made our page feel like a freeze-frame, which is exactly what the
+user reported. Both calls are gone, and the engine half of the same fix
+(cof_ui_death_keep_running) stops the engine pausing the world and muting the
+mixer while this page owns input - see docs/cof-ui-death-flow.md section 6.
+
+Nothing replaces them: the page now leaves the client's MP3 player alone. The
+Load Game and New Game pages still issue their own "stopmp3", which is a
+different question (music bleeding across a transition, cof_mp3_stop_on_map).
+=================
+*/
+void CMenuCoFDeath::Show()
+{
+	// no scrim, no panel: the scene stays exactly as the player left it
+	UI_ThemeSetSceneUnveiled( true );
+
+	CMenuFramework::Show();
+}
+
+void CMenuCoFDeath::Hide()
+{
+	UI_ThemeSetSceneUnveiled( false );
+	CMenuFramework::Hide();
+}
+
+/*
+=================
+CMenuCoFDeath::Think
+
+Close as soon as the session this page belongs to is gone. Loading a save goes
+through UI_CloseMenu, which cleans the whole stack, but Exit only queues a
+disconnect: the redirect's own "menu_main" then becomes the current window and
+this one would sit behind it forever, still holding the scrim suppression.
+=================
+*/
+void CMenuCoFDeath::Think()
+{
+	if( !EngFuncs::ClientInGame( ) || EngFuncs::GetCvarFloat( "cl_background" ) != 0.0f )
+	{
+		UI_ThemeSetSceneUnveiled( false );
+
+		if( uiStatic.menu.Count() > 1 )
+			Hide();
+
+		return;
+	}
+
+	CMenuFramework::Think();
+}
+
+void CMenuCoFDeath::_Init( void )
+{
+	banner.szName = L( "Game over" );
+
+	load.szName = L( "Load Game" );
+	load.SetPicture( PC_LOAD_GAME );
+	load.iFlags |= QMF_NOTIFY;
+	load.onReleased = VoidCb( &CMenuCoFDeath::LoadCb );
+
+	exit.szName = L( "Exit" );
+	exit.SetPicture( PC_QUIT );
+	exit.iFlags |= QMF_NOTIFY;
+	exit.onReleased = VoidCb( &CMenuCoFDeath::ExitCb );
+
+	if( !UI_ThemeActive( ))
+		AddItem( banner );
+	else
+		AddItem( title );
+
+	AddItem( load );
+	AddItem( exit );
+}
+
+void CMenuCoFDeath::_VidInit( void )
+{
+	if( !UI_ThemeActive( ))
+	{
+		load.SetCoord( 72, 230 );
+		exit.SetCoord( 72, 280 );
+		return;
+	}
+
+	const int chSize = (int)( THEME_ITEM_TALL * uiStatic.scaleY );
+	CMenuCoFDeathItem *items[] = { &load, &exit };
+	int y = ( 768 * THEME_DEATH_ITEM_PCT ) / 100;
+
+	title.SetRect( 0, ( 768 * THEME_DEATH_TITLE_PCT ) / 100,
+		uiStatic.width, CMenuCoFDeathTitle::Tall( ));
+
+	for( int i = 0; i < 2; i++ )
+	{
+		int w = THEME_BTN_MIN_W;
+
+		if( uiStatic.scaleX > 0.0f )
+			w = (int)( g_FontMgr->GetTextWideScaled( uiStatic.hThemeItem,
+				items[i]->szName, chSize ) / uiStatic.scaleX ) + 12;
+
+		items[i]->eThemeStyle = CMenuPicButton::THEME_PLAIN;
+		items[i]->eTextAlignment = QM_CENTER;
+		items[i]->SetRect(( uiStatic.width - w ) / 2, y, w, THEME_DEATH_PITCH );
+		y += THEME_DEATH_PITCH;
+	}
+}
+
+ADD_MENU( menu_cofdeath, CMenuCoFDeath, UI_CoFDeath_Menu );
diff --git a/3rdparty/mainui/menus/CryOfFear.h b/3rdparty/mainui/menus/CryOfFear.h
index 4cead8f..b7c0eb6 100644
--- a/3rdparty/mainui/menus/CryOfFear.h
+++ b/3rdparty/mainui/menus/CryOfFear.h
@@ -29,8 +29,45 @@ void UI_CoFCampaign_Menu( void );
 void UI_CoFLanguage_Menu( void );
 void UI_CoFExtras_Menu( void );
 
-// Unlockables is not a page: the gallery is a map with anti-tamper state the
-// menu cannot read, so this only runs the client's own entry command.
+// The Credits page: who made Cry of Fear, who makes this project, and who
+// made the Polish localisation, each section with a link to its own page.
+void UI_CoFCredits_Menu( void );
+
+// The subtitle language, shared by the Language page and the Game options
+// page. Index is 1-based, matching the game DLL's "subtitleset N".
+int         UI_CoFSubtitleLanguageCount( void );
+const char *UI_CoFSubtitleLanguageName( int index ); // 0-based, for a list model
+int         UI_CoFGetSubtitleLanguage( void );
+void        UI_CoFSetSubtitleLanguage( int index );
+
+// The Unlockables page: the 34 flags Cry of Fear keeps in
+// cryoffear/scriptsettings.dat, with a real status column. Each flag is one
+// 20-byte line of that file, and client.dll carries the two possible tokens
+// for it as a compiled-in pair; comparing the line to the pair is the whole
+// decode. 27 of the 34 have a known name, the other 7 are listed unnamed.
+// See docs/cof-ui-m3-theme.md and
+// stage1/ui-data-research-20260921/unlockables-token-table.md.
+void UI_CoFUnlockables_Menu( void );
+int  UI_CoFUnlockableStatus( int index ); // -1 unknown, 0 locked, 1 unlocked
+
+// Re-read cryoffear/scriptsettings.dat. Cheap (2 KiB) and done whenever a page
+// that shows unlock state opens, so the menu never shows a stale answer after
+// the player unlocks something and returns to the menu.
+void UI_CoFUnlockablesReload( void );
+
+// Line 83 of scriptsettings.dat, the Nightmare difficulty flag. Returns the
+// same tri-state as UI_CoFUnlockableStatus; the difficulty page treats
+// anything other than 1 as locked.
+int  UI_CoFNightmareStatus( void );
+
+// The old route: run the client's own entry command, which loads the
+// c_unlockables gallery map. Kept as the page's "Open original gallery".
 void UI_CoFUnlockables( void );
 
+// The death page. Opened by the engine (command "menu_cofdeath") when the game
+// DLL sends the GAME OVER user message - "VGUIMenu" with a first byte of 35 -
+// which is the only signal Cry of Fear produces on death. See
+// docs/cof-ui-death-flow.md.
+void UI_CoFDeath_Menu( void );
+
 #endif // MENU_CRYOFFEAR_H
diff --git a/3rdparty/mainui/menus/LoadGame.cpp b/3rdparty/mainui/menus/LoadGame.cpp
index 53138ba..4580d0f 100644
--- a/3rdparty/mainui/menus/LoadGame.cpp
+++ b/3rdparty/mainui/menus/LoadGame.cpp
@@ -185,7 +185,13 @@ private:
 class CMenuLoadGame : public CMenuFramework
 {
 public:
-	CMenuLoadGame() : CMenuFramework( "CMenuLoadGame" ), savesListModel( this ) { }
+	// m_fSaveMode decides the panel title, which of the two primary buttons is
+	// laid out into the bottom row and which of them is visible at all, and
+	// _VidInit() runs from Show() - so it must never be read before it has
+	// been written. It used to be left indeterminate here, which is half of
+	// the "first Load Game opens as SAVE GAME" bug; the other half is the call
+	// order in UI_LoadSaveGame_Menu() below.
+	CMenuLoadGame() : CMenuFramework( "CMenuLoadGame" ), m_fSaveMode( false ), savesListModel( this ) { }
 
 	// true to turn this menu into save mode, false to turn into load mode
 	void SetSaveMode( bool saveMode );
@@ -194,6 +200,7 @@ public:
 
 private:
 	void _Init( void );
+	void _VidInit( void );
 
 	void LoadGame();
 	void SaveGame();
@@ -484,13 +491,51 @@ void CMenuLoadGame::_Init( void )
 
 	levelShot.SetRect( LEVELSHOT_X, LEVELSHOT_Y, LEVELSHOT_W, LEVELSHOT_H );
 
-	AddItem( banner );
-	AddItem( load );
-	AddItem( save );
+	if( UI_ThemeActive( ))
+	{
+		SetPanel( "LOAD GAME", 860, 560 );
+		msgBox.szPanelTitle = "DELETE SAVE";
+		overwriteBox.szPanelTitle = "OVERWRITE SAVE";
+	}
+	else
+	{
+		AddItem( banner );
+	}
+
+	AddItem( savesList );
+	AddItem( levelShot );
 	AddItem( remove );
 	AddItem( cancel );
-	AddItem( levelShot );
-	AddItem( savesList );
+	AddItem( load );
+	AddItem( save );
+}
+
+void CMenuLoadGame::_VidInit( void )
+{
+	if( !IsPanel( ))
+		return;
+
+	szPanelTitle = m_fSaveMode ? "SAVE GAME" : "LOAD GAME";
+
+	Rect left = ColumnRect2( 0, 0.62f );
+	Rect right = ColumnRect2( 1, 0.62f );
+
+	savesList.SetRect( left.pt.x, left.pt.y, left.sz.w, left.sz.h );
+	levelShot.SetRect( right.pt.x, right.pt.y, right.sz.w, right.sz.w * 9 / 16 );
+
+	CMenuPicButton *btns[3];
+	int n = 0;
+
+	if( remove.IsVisible( ))
+		btns[n++] = &remove;
+	btns[n++] = &cancel;
+
+	CMenuPicButton *primary = m_fSaveMode ? &save : &load;
+
+	if( primary->IsVisible( ))
+		btns[n++] = primary;
+
+	LayoutButtonRow( btns, n );
 }
 
 void CMenuLoadGame::LoadGame()
@@ -503,6 +548,15 @@ void CMenuLoadGame::LoadGame()
 
 	snprintf( cmd, sizeof( cmd ), "load \"%s\"\n", name );
 	EngFuncs::StopBackgroundTrack( );
+
+	// Cry of Fear's own Load Game panel stopped the client's MP3 player before
+	// loading; the engine menu never did, so the background map's track kept
+	// playing into a save whose map starts no track of its own. "stopmp3" is
+	// the client's own console command (no arguments, unconditional stop) and
+	// is a different player from the engine's StopBackgroundTrack above.
+	if( UI_IsCoFRootSaveGame( ))
+		EngFuncs::ClientCmd( false, "stopmp3\n" );
+
 	EngFuncs::ClientCmd( false, cmd );
 	UI_CloseMenu();
 }
@@ -656,8 +710,18 @@ void UI_LoadSaveGame_Menu( bool saveMode )
 
 	if( !EngFuncs::CheckGameDll( )) return;
 
-	menu_loadgame->Show();
+	// The mode has to be applied BEFORE Show(), because Show() is
+	// Init() + VidInit() (controls/BaseWindow.cpp) and _VidInit() is where the
+	// panel title is chosen and where LayoutButtonRow() places whichever of
+	// Load / Save is the primary button for this mode. With the old order
+	// _VidInit() read the PREVIOUS mode: the first Load Game of a session came
+	// up titled SAVE GAME with Save laid out into the row, and then
+	// SetSaveMode() hid Save and showed Load - which nothing had positioned, so
+	// it kept its WON-layout coordinate (72, 230) and drew as a bare label at
+	// the left edge, outside the panel. Opening the page a second time looked
+	// right only because by then the stale mode happened to match.
 	menu_loadgame->SetSaveMode( saveMode );
+	menu_loadgame->Show();
 	menu_loadgame->UpdateList();
 }
 
diff --git a/3rdparty/mainui/menus/Main.cpp b/3rdparty/mainui/menus/Main.cpp
index a99e669..8e9a67e 100644
--- a/3rdparty/mainui/menus/Main.cpp
+++ b/3rdparty/mainui/menus/Main.cpp
@@ -39,6 +39,62 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 #define ART_CLOSEBTN_F	"gfx/shell/cls_f"
 #define ART_CLOSEBTN_D	"gfx/shell/cls_d"
 
+/*
+=================
+CMenuCoFLogo
+
+The main menu's text logo: "CRY OF FEAR" in the theme's largest handle over
+"ENHANCED" in a much smaller one, both left-aligned on the item list's own
+margin. Text only - no bitmap - and main menu only, never the pause menu.
+
+UI_DrawString advances strictly by DrawCharacter's return value, so the tracking
+the wordmark wants is not expressible through it; UI_ThemeDrawTracked walks the
+string itself and adds the extra advance.
+=================
+*/
+class CMenuCoFLogo : public CMenuBaseItem
+{
+public:
+	CMenuCoFLogo()
+	{
+		iFlags = QMF_INACTIVE;
+		szName = "CRY OF FEAR";
+		bCentered = false;
+	}
+
+	void Draw() override;
+
+	// centre both lines inside the item rect instead of left-aligning them
+	bool bCentered;
+
+	static const char *Title() { return "CRY OF FEAR"; }
+	static const char *Sub()   { return "ENHANCED"; }
+
+	// height of the whole two-line block, in virtual units
+	static int BlockHeight() { return THEME_LOGO_TALL + 6 + THEME_LOGOSUB_TALL; }
+};
+
+void CMenuCoFLogo::Draw()
+{
+	const int tall    = (int)( THEME_LOGO_TALL * uiStatic.scaleY );
+	const int subTall = (int)( THEME_LOGOSUB_TALL * uiStatic.scaleY );
+	const int gap     = (int)( 6 * uiStatic.scaleY );
+	const int track   = (int)( THEME_LOGO_TRACKING * uiStatic.scaleX );
+	int x1 = m_scPos.x, x2 = m_scPos.x;
+
+	if( bCentered )
+	{
+		x1 += ( m_scSize.w - UI_ThemeTrackedWidth( uiStatic.hThemeLogo, Title(), tall, track )) / 2;
+		x2 += ( m_scSize.w - UI_ThemeTrackedWidth( uiStatic.hThemeLogoSub, Sub(), subTall, track / 2 )) / 2;
+	}
+
+	UI_ThemeDrawTracked( uiStatic.hThemeLogo, x1, m_scPos.y,
+		Title(), THEME_TEXT_HI, tall, track );
+
+	UI_ThemeDrawTracked( uiStatic.hThemeLogoSub, x2, m_scPos.y + tall + gap,
+		Sub(), THEME_TEXT_DIM, subTall, track / 2 );
+}
+
 class CMenuMain: public CMenuFramework
 {
 public:
@@ -48,6 +104,17 @@ public:
 
 	void QuitDialogCb();
 
+	// console entry point for the second level of the Cry of Fear main list:
+	// that list is switched by a mouse click or by Enter on the Extras item,
+	// and this project may not inject either, so the level is also reachable
+	// as a command. Same pattern as the engine's cof_ui_menu_panel_probe.
+	void CoFShowLevel( int level ) { if( bCoF ) CoFSetLevel( level ); }
+
+	// console entry point for Quit to menu, for the same reason: the real way
+	// in is a click on that item and then on the confirmation's positive
+	// button. It runs exactly the callback the dialog runs.
+	void CoFQuitToMenu() { DisconnectCb(); }
+
 private:
 	void _Init() override;
 	void _VidInit( ) override;
@@ -58,6 +125,11 @@ private:
 	// Cry of Fear replaces the stock item set with its own, see InitCoF().
 	void InitCoF();
 	void VidInitCoF( bool connected );
+	// Cry of Fear's main list has a second level: Extras swaps the list in
+	// place, with the wordmark staying where it is. See CoFSetLevel().
+	void CoFSetLevel( int level );
+	void CoFExtrasCb();
+	void CoFBackCb();
 
 	void DisconnectCb();
 	void DisconnectDialogCb();
@@ -86,7 +158,11 @@ private:
 	CMenuPicButton	cofLanguage;
 	CMenuPicButton	cofUnlockables;
 	CMenuPicButton	cofExtras;
+	CMenuPicButton	cofLinks;
+	CMenuPicButton	cofCredits;
+	CMenuPicButton	cofBack;
 	CMenuPicButton	cofQuitToMenu;
+	CMenuCoFLogo	cofLogo;
 
 	// buttons on top right. Maybe should be drawn if fullscreen == 1?
 	CMenuBitmap	minimizeBtn;
@@ -100,8 +176,14 @@ private:
 	bool bCoF;
 	bool bCoFWantBackgroundMap;
 	int  iCoFBackgroundMapDelay;
+	// 0 = the main list, 1 = the Extras list. Pause menu always 0.
+	int  iCoFLevel;
 };
 
+// Cry of Fear main list levels
+#define COF_LEVEL_MAIN   0
+#define COF_LEVEL_EXTRAS 1
+
 // Frames the engine has to look completely idle before Quit to menu is allowed
 // to bring the background map back. A level change that is only queued is not
 // visible in any cvar yet, so wait a moment instead of racing it.
@@ -114,6 +196,9 @@ void CMenuMain::QuitDialogCb()
 	else
 		dialog.SetMessage( L( "GameUI_QuitConfirmationText" ) );
 
+	dialog.szPanelTitle = "QUIT";
+	if( UI_ThemeActive( ))
+		dialog.SetPositiveButton( L( "GameUI_GameMenu_Quit" ), PC_QUIT );
 	dialog.onPositive.SetCommand( false, "quit \"menu dialog\"\n" );
 	dialog.Show();
 }
@@ -135,6 +220,7 @@ void CMenuMain::DisconnectCb()
 
 void CMenuMain::DisconnectDialogCb()
 {
+	dialog.szPanelTitle = "QUIT TO MENU";
 	dialog.onPositive = VoidCb( &CMenuMain::DisconnectCb );
 	dialog.SetMessage( L( "Really disconnect?" ) );
 	dialog.Show();
@@ -156,6 +242,15 @@ bool CMenuMain::KeyDown( int key )
 {
 	if( UI::Key::IsEscape( key ) )
 	{
+		// Cry of Fear second-level list: Escape is Back, not Quit. Only the
+		// main menu ever has a second level (the pause list stays flat), and
+		// the dialog owns Escape while it is up.
+		if( bCoF && iCoFLevel != COF_LEVEL_MAIN && !CL_IsActive( ) && !dialog.IsVisible( ))
+		{
+			CoFBackCb();
+			return true;
+		}
+
 		if ( CL_IsActive( ))
 		{
 			if( !dialog.IsVisible() )
@@ -197,6 +292,7 @@ void CMenuMain::_Init( void )
 	bCoF = UI_IsCryOfFear();
 	bCoFWantBackgroundMap = false;
 	iCoFBackgroundMapDelay = COF_BACKGROUND_RESTART_DELAY;
+	iCoFLevel = COF_LEVEL_MAIN;
 
 	if( gMenu.m_gameinfo.trainmap[0] && stricmp( gMenu.m_gameinfo.trainmap, gMenu.m_gameinfo.startmap ) != 0 )
 		bTrainMap = true;
@@ -301,14 +397,19 @@ void CMenuMain::_Init( void )
 		newGame.SetGrayed( true );
 	}
 
-	if( FBitSet( gMenu.m_gameinfo.flags, GFL_ANIMATED_TITLE ))
+	// The theme has no banners at all: no animated title, no logo.avi movie
+	// banner, no WON banner bitmap.
+	if( !UI_ThemeActive( ))
 	{
-		if( animatedBanner.TryLoad())
-			AddItem( animatedBanner );
-	}
-	else
-	{
-		AddItem( movieBanner );
+		if( FBitSet( gMenu.m_gameinfo.flags, GFL_ANIMATED_TITLE ))
+		{
+			if( animatedBanner.TryLoad())
+				AddItem( animatedBanner );
+		}
+		else
+		{
+			AddItem( movieBanner );
+		}
 	}
 
 	dialog.Link( this );
@@ -386,39 +487,117 @@ void CMenuMain::InitCoF( void )
 	cofHostServer.iFlags |= QMF_NOTIFY;
 	cofHostServer.onReleased = UI_CreateGame_Menu;
 
+	// Language left the main list in the 2026-09-22 restructure; it lives on
+	// the Game options page and nowhere else. The item stays declared because
+	// the layout arrays below are index-free and an unused, never-added item
+	// costs nothing, but it is not added and not reachable from here.
 	cofLanguage.SetNameAndStatus( L( "Language" ), L( "Choose the subtitle and document language" ) );
 	cofLanguage.SetPicture( PC_CUSTOMIZE );
 	cofLanguage.iFlags |= QMF_NOTIFY;
 	cofLanguage.onReleased = UI_CoFLanguage_Menu;
 
-	cofUnlockables.SetNameAndStatus( L( "Unlockables" ), L( "Open the unlockables gallery" ) );
+	// the engine-menu page, not "map c_unlockables": that gallery panel is
+	// mispositioned at high resolutions and its MAIN MENU button returns to the
+	// client's old menu. The page keeps a button for the original route.
+	cofUnlockables.SetNameAndStatus( L( "Unlockables" ), L( "List the unlockables and open the gallery" ) );
 	cofUnlockables.SetPicture( PC_VIEW_GAME_INFO );
 	cofUnlockables.iFlags |= QMF_NOTIFY;
-	cofUnlockables.onReleased = UI_CoFUnlockables;
+	cofUnlockables.onReleased = UI_CoFUnlockables_Menu;
 
-	cofExtras.SetNameAndStatus( L( "Extras" ), L( "Soundtrack, merchandise and community links" ) );
+	// Extras is now a second level of the same list, not a page: pressing it
+	// swaps the main list for Join Server / Host Server / Unlockables / Links /
+	// Back, drawn in the same place and the same style with the wordmark left
+	// where it is. "Links" is the old Extras page, the collection of URLs.
+	cofExtras.SetNameAndStatus( L( "Extras" ), L( "Co-op, unlockables, soundtrack and community links" ) );
 	cofExtras.SetPicture( PC_VISIT_WEB_SITE );
 	cofExtras.iFlags |= QMF_NOTIFY;
-	cofExtras.onReleased = UI_CoFExtras_Menu;
+	cofExtras.onReleased = VoidCb( &CMenuMain::CoFExtrasCb );
+
+	cofLinks.SetNameAndStatus( L( "Links" ), L( "Soundtrack, merchandise and community links" ) );
+	cofLinks.SetPicture( PC_VISIT_WEB_SITE );
+	cofLinks.iFlags |= QMF_NOTIFY;
+	cofLinks.onReleased = UI_CoFExtras_Menu;
+
+	cofCredits.SetNameAndStatus( L( "Credits" ), L( "Who made Cry of Fear, this project and the Polish localisation" ) );
+	cofCredits.SetPicture( PC_VIEW_GAME_INFO );
+	cofCredits.iFlags |= QMF_NOTIFY;
+	cofCredits.onReleased = UI_CoFCredits_Menu;
+
+	cofBack.SetNameAndStatus( L( "Back" ), L( "Return to the main menu" ) );
+	cofBack.SetPicture( PC_DISCONNECT );
+	cofBack.iFlags |= QMF_NOTIFY;
+	cofBack.onReleased = VoidCb( &CMenuMain::CoFBackCb );
 
 	cofQuitToMenu.SetNameAndStatus( L( "Quit to menu" ), L( "Leave the game and return to the main menu" ) );
 	cofQuitToMenu.SetPicture( PC_DISCONNECT );
 	cofQuitToMenu.iFlags |= QMF_NOTIFY;
 	cofQuitToMenu.onReleased = VoidCb( &CMenuMain::DisconnectDialogCb );
 
-	AddItem( banner );
+	if( UI_ThemeActive( ))
+		AddItem( cofLogo );
+	else
+		AddItem( banner );
+
+	// AddItem order is also the keyboard navigation order (CMenuItemsHolder::
+	// AdjustCursor walks m_pItems and steps over everything invisible), so the
+	// two levels are added in the order each is drawn and never interleaved.
 	AddItem( resumeGame );
 	AddItem( newGame );
 	AddItem( saveRestore );
 	AddItem( cofCampaign );
+	AddItem( cofExtras );
+	AddItem( configuration );
+	AddItem( cofQuitToMenu );
+	AddItem( quit );
+
+	// second level
 	AddItem( cofJoinServer );
 	AddItem( cofHostServer );
-	AddItem( cofLanguage );
 	AddItem( cofUnlockables );
-	AddItem( cofExtras );
-	AddItem( configuration );
-	AddItem( cofQuitToMenu );
-	AddItem( quit );
+	AddItem( cofLinks );
+	AddItem( cofCredits );
+	AddItem( cofBack );
+}
+
+/*
+=================
+CMenuMain::CoFSetLevel
+
+Swap the main list for the second level, or back, without leaving CMenuMain.
+
+Everything the swap needs already exists: the items are named members added
+once in InitCoF, VidInitCoF decides per frame-size which of them are visible and
+where they sit, and both the mouse hit test (CMenuItemsHolder::MouseMove) and
+the keyboard cursor (AdjustCursor) skip invisible items on their own. So the
+level is one member, and changing it is the same four calls DisconnectCb already
+makes after it changes what the list contains.
+=================
+*/
+void CMenuMain::CoFSetLevel( int level )
+{
+	iCoFLevel = level;
+
+	VidInit( CL_IsActive( ));
+	CalcPosition();
+	CalcSizes();
+	VidInitItems();
+
+	// AdjustCursor wraps, so after a swap the cursor can land anywhere; put it
+	// on the first item of the list that is now on screen.
+	if( level == COF_LEVEL_EXTRAS )
+		SetCursorToItem( cofJoinServer, false );
+	else
+		SetCursorToItem( newGame, false );
+}
+
+void CMenuMain::CoFExtrasCb()
+{
+	CoFSetLevel( COF_LEVEL_EXTRAS );
+}
+
+void CMenuMain::CoFBackCb()
+{
+	CoFSetLevel( COF_LEVEL_MAIN );
 }
 
 /*
@@ -440,15 +619,30 @@ void CMenuMain::VidInitCoF( bool connected )
 	const int hoffset = ( 70 / 640.0 ) * 1024.0;
 	const int ygap = (( 404 - 373 ) / 480.0 ) * 768.0;
 
+	// Only the main menu has a second level; going in game always drops back to
+	// the flat pause list, and coming back out starts at the top again.
+	if( connected )
+		iCoFLevel = COF_LEVEL_MAIN;
+
+	const bool lvlMain   = !connected && iCoFLevel == COF_LEVEL_MAIN;
+	const bool lvlExtras = !connected && iCoFLevel == COF_LEVEL_EXTRAS;
+
 	resumeGame.SetVisibility( connected );
 	cofQuitToMenu.SetVisibility( connected );
-	newGame.SetVisibility( !connected );
-	cofCampaign.SetVisibility( !connected );
-	cofJoinServer.SetVisibility( !connected );
-	cofHostServer.SetVisibility( !connected );
-	cofLanguage.SetVisibility( !connected );
-	cofUnlockables.SetVisibility( !connected );
-	cofExtras.SetVisibility( !connected );
+	saveRestore.SetVisibility( connected || lvlMain );
+	configuration.SetVisibility( connected || lvlMain );
+	quit.SetVisibility( connected || lvlMain );
+	newGame.SetVisibility( lvlMain );
+	cofCampaign.SetVisibility( lvlMain );
+	cofExtras.SetVisibility( lvlMain );
+	cofJoinServer.SetVisibility( lvlExtras );
+	cofHostServer.SetVisibility( lvlExtras );
+	cofUnlockables.SetVisibility( lvlExtras );
+	cofLinks.SetVisibility( lvlExtras );
+	cofCredits.SetVisibility( lvlExtras );
+	cofBack.SetVisibility( lvlExtras );
+	// Language left the main list; the Game options page owns it now.
+	cofLanguage.SetVisibility( false );
 
 	if( connected )
 	{
@@ -463,15 +657,82 @@ void CMenuMain::VidInitCoF( bool connected )
 		saveRestore.onReleased = UI_LoadGame_Menu;
 	}
 
+	// The drawing order of both levels, in one array: only the level that is
+	// on screen has visible items in it, and the loops below skip the rest, so
+	// the geometry always follows the list actually being shown - at every
+	// resolution, because the rectangles are recomputed here on every VidInit.
 	CMenuPicButton *order[] =
 	{
-		&resumeGame, &newGame, &saveRestore, &cofCampaign, &cofJoinServer,
-		&cofHostServer, &cofLanguage, &cofUnlockables, &cofExtras,
-		&configuration, &cofQuitToMenu, &quit
+		// pause list, and the first level of the main list
+		&resumeGame, &newGame, &saveRestore, &cofCampaign, &cofExtras,
+		&configuration, &cofQuitToMenu, &quit,
+		// second level of the main list
+		&cofJoinServer, &cofHostServer, &cofUnlockables, &cofLinks, &cofCredits,
+		&cofBack
 	};
+	const size_t count = sizeof( order ) / sizeof( order[0] );
+
+	if( UI_ThemeActive( ))
+	{
+		// Main menu: a centred column, as in Cry of Fear's own menu - every
+		// line centred on the screen's horizontal middle, and the wordmark plus
+		// the list treated as one group whose centre sits at
+		// THEME_MENU_CENTER_PCT of the height.
+		//
+		// Pause menu: the Source-reference lower-left list, flush to a 64-unit
+		// margin, anchored 96 units above the bottom of the 768-unit space.
+		//
+		// Both give each item a hit box exactly as wide as its own text, so the
+		// mouse rectangle and the keyboard cursor follow the drawn position at
+		// every resolution.
+		int visible = 0;
+
+		for( size_t i = 0; i < count; i++ )
+			if( order[i]->IsVisible( )) visible++;
+
+		const bool centered = !connected;
+		const int itemsH = visible * THEME_MENU_PITCH;
+		const int blockH = CMenuCoFLogo::BlockHeight() + THEME_LOGO_GAP_CENTER + itemsH;
+		int y;
+
+		if( centered )
+			y = ( 768 * THEME_MENU_CENTER_PCT ) / 100 - blockH / 2
+				+ CMenuCoFLogo::BlockHeight() + THEME_LOGO_GAP_CENTER;
+		else
+			y = 768 - THEME_MENU_BOTTOM - THEME_ITEM_TALL - ( visible - 1 ) * THEME_MENU_PITCH;
+
+		const int firstY = y;
+		const int chSize = (int)( THEME_ITEM_TALL * uiStatic.scaleY );
+
+		for( size_t i = 0; i < count; i++ )
+		{
+			if( !order[i]->IsVisible( ))
+				continue;
+
+			int w = THEME_BTN_MIN_W;
+
+			if( uiStatic.scaleX > 0.0f )
+				w = (int)( g_FontMgr->GetTextWideScaled( uiStatic.hThemeItem,
+					order[i]->szName, chSize ) / uiStatic.scaleX ) + 12;
+
+			order[i]->eThemeStyle = CMenuPicButton::THEME_PLAIN;
+			order[i]->eTextAlignment = centered ? QM_CENTER : QM_LEFT;
+			order[i]->SetRect( centered ? ( uiStatic.width - w ) / 2 : THEME_MENU_MARGIN,
+				y, w, THEME_MENU_PITCH );
+			y += THEME_MENU_PITCH;
+		}
+
+		// the wordmark belongs to the main menu only, never the pause menu
+		cofLogo.SetVisibility( centered );
+		cofLogo.bCentered = true;
+		cofLogo.SetRect( 0,
+			firstY - THEME_LOGO_GAP_CENTER - CMenuCoFLogo::BlockHeight(),
+			uiStatic.width, CMenuCoFLogo::BlockHeight( ));
+		return;
+	}
 
 	int yoffset = 230;
-	for( size_t i = 0; i < sizeof( order ) / sizeof( order[0] ); i++ )
+	for( size_t i = 0; i < count; i++ )
 	{
 		if( !order[i]->IsVisible( ))
 			continue;
@@ -617,6 +878,70 @@ void CMenuMain::Think()
 
 ADD_MENU( menu_main, CMenuMain, UI_Main_Menu );
 
+/*
+=================
+menu_cof_extras_list / menu_cof_main_list
+
+The two levels of the Cry of Fear main list, as console commands. The real way
+in is a click on Extras and a click on Back (or Escape), which this project may
+not inject; these run exactly the same CoFSetLevel path so a cfg can screenshot
+both levels.
+=================
+*/
+static void UI_CoFMainExtrasList( void )
+{
+	menu_main->Show();
+	menu_main->CoFShowLevel( COF_LEVEL_EXTRAS );
+}
+
+static void UI_CoFMainMainList( void )
+{
+	menu_main->Show();
+	menu_main->CoFShowLevel( COF_LEVEL_MAIN );
+}
+
+static void UI_CoFMainQuitToMenu( void )
+{
+	menu_main->Show();
+	menu_main->CoFQuitToMenu();
+}
+
+/*
+=================
+menu_cof_sound_probe
+
+The menu's own sounds are only ever started by a click, a key or a pointer
+moving onto an item, and this project may not inject any of those. This plays
+one slot of uiStatic.sounds through exactly the call every control uses -
+EngFuncs::PlayLocalSound -> pfnPlaySound -> S_StartLocalSound - so a cfg can
+prove what volume the mixer is handed (see ui_sound_volume). Default slot is
+SND_LAUNCH, the one every button uses.
+=================
+*/
+static void UI_CoFSoundProbe( void )
+{
+	int slot = SND_LAUNCH;
+
+	if( EngFuncs::CmdArgc() > 1 )
+		slot = atoi( EngFuncs::CmdArgv( 1 ));
+
+	if( slot < 0 || slot >= SND_COUNT )
+	{
+		Con_Printf( "menu_cof_sound_probe: slot must be 0..%d\n", SND_COUNT - 1 );
+		return;
+	}
+
+	Con_Printf( "menu_cof_sound_probe: slot %d = \"%s\", ui_sound_volume %.2f\n",
+		slot, uiStatic.sounds[slot], EngFuncs::GetCvarFloat( "ui_sound_volume" ));
+
+	EngFuncs::PlayLocalSound( uiStatic.sounds[slot] );
+}
+
+ADD_COMMAND( menu_cof_extras_list, UI_CoFMainExtrasList );
+ADD_COMMAND( menu_cof_main_list, UI_CoFMainMainList );
+ADD_COMMAND( menu_cof_quit_to_menu, UI_CoFMainQuitToMenu );
+ADD_COMMAND( menu_cof_sound_probe, UI_CoFSoundProbe );
+
 /*
 =================
 UI_QuitDialog_f
diff --git a/3rdparty/mainui/menus/SaveLoad.cpp b/3rdparty/mainui/menus/SaveLoad.cpp
index 4d34392..b280c29 100644
--- a/3rdparty/mainui/menus/SaveLoad.cpp
+++ b/3rdparty/mainui/menus/SaveLoad.cpp
@@ -29,15 +29,33 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 class CMenuSaveLoad : public CMenuFramework
 {
 public:
-	CMenuSaveLoad() : CMenuFramework( "CMenuSaveLoad" ) { }
+	CMenuSaveLoad() : CMenuFramework( "CMenuSaveLoad" ), saveGame( NULL ), m_bCoF( false ) { }
+
+	// the pause-menu-saves switch now lives on the Game options page, so
+	// re-read it every time this menu is opened
+	void Show() override
+	{
+		CMenuFramework::Show();
+
+		if( m_bCoF && saveGame )
+		{
+			cofMenuSaves.UpdateEditable();
+			saveGame->SetVisibility( cofMenuSaves.bChecked );
+		}
+	}
 private:
 	void _Init();
+	void _VidInit();
 	void CofMenuSavesChanged();
 	void UpdateCofMenuSavesLabel();
 
 	CMenuAction	hintMessage;
 	CMenuPicButton *saveGame;
+	CMenuPicButton loadRow;
+	CMenuPicButton saveRow;
+	CMenuPicButton close;
 	CMenuCheckBox cofMenuSaves;
+	bool		m_bCoF;
 	char		cofMenuSavesLabel[CS_SIZE];
 	char		hintText[MAX_HINT_TEXT];
 };
@@ -55,7 +73,11 @@ void CMenuSaveLoad::_Init( void )
 	if( !base ) base = strrchr( gameDir, '\\' );
 	const bool cof = !stricmp( base ? base + 1 : gameDir, "cryoffear" );
 
-	if( cof )
+	m_bCoF = cof && UI_ThemeActive();
+
+	if( cof && UI_ThemeActive( ))
+		Q_strncpy( hintText, "Cry of Fear uses five shared save slots. Turn on \"Pause menu saves\" in Options > Game to save from this menu.", sizeof( hintText ));
+	else if( cof )
 		Q_strncpy( hintText, "Cry of Fear uses five shared save slots. Enable pause-menu saves to save from this menu.", sizeof( hintText ));
 	else
 		snprintf( hintText, sizeof( hintText ),
@@ -71,6 +93,47 @@ void CMenuSaveLoad::_Init( void )
 	hintMessage.szName = hintText;
 	hintMessage.SetCoord( 360, 480 );
 
+	if( UI_ThemeActive( ))
+	{
+		SetPanel( "SAVE / LOAD", 480, 300 );
+
+		loadRow.SetNameAndStatus( L( "GameUI_LoadGame" ), L( "GameUI_LoadGameHelp" ));
+		loadRow.iFlags |= QMF_NOTIFY;
+		loadRow.eThemeStyle = CMenuPicButton::THEME_ROW;
+		loadRow.eTextAlignment = QM_LEFT;
+		loadRow.onReleased = UI_LoadGame_Menu;
+		AddItem( loadRow );
+
+		saveRow.SetNameAndStatus( L( "GameUI_SaveGame" ), L( "GameUI_SaveGameHelp" ));
+		saveRow.iFlags |= QMF_NOTIFY;
+		saveRow.eThemeStyle = CMenuPicButton::THEME_ROW;
+		saveRow.eTextAlignment = QM_LEFT;
+		saveRow.onReleased = UI_SaveGame_Menu;
+		AddItem( saveRow );
+		saveGame = &saveRow;
+
+		close.szName = L( "Close" );
+		close.SetPicture( PC_DONE );
+		close.iFlags |= QMF_NOTIFY;
+		close.onReleased = VoidCb( &CMenuSaveLoad::Hide );
+		AddItem( close );
+
+		if( cof )
+		{
+			// the switch itself moved to the top-level Game options page; this
+			// menu only reads it, to decide whether Save Game is offered
+			cofMenuSaves.LinkCvar( "cof_pause_menu_saves" );
+			cofMenuSaves.UpdateEditable();
+			UpdateCofMenuSavesLabel();
+			cofMenuSaves.SetVisibility( false );
+			saveGame->SetVisibility( cofMenuSaves.bChecked );
+			AddItem( cofMenuSaves );
+		}
+
+		AddItem( hintMessage );
+		return;
+	}
+
 	AddItem( banner );
 	AddButton( L( "GameUI_LoadGame" ), L( "GameUI_LoadGameHelp" ), PC_LOAD_GAME, UI_LoadGame_Menu, QMF_NOTIFY );
 	saveGame = AddButton( L( "GameUI_SaveGame" ), L( "GameUI_SaveGameHelp" ), PC_SAVE_GAME, UI_SaveGame_Menu, QMF_NOTIFY );
@@ -88,6 +151,22 @@ void CMenuSaveLoad::_Init( void )
 	AddItem( hintMessage );
 }
 
+void CMenuSaveLoad::_VidInit()
+{
+	if( !IsPanel( ))
+		return;
+
+	Rect c = ContentRect();
+
+	loadRow.SetRect( c.pt.x, c.pt.y, c.sz.w, THEME_ROW_H );
+	saveRow.SetRect( c.pt.x, c.pt.y + THEME_ROW_PITCH, c.sz.w, THEME_ROW_H );
+	hintMessage.SetRect( c.pt.x, c.pt.y + THEME_ROW_PITCH * 2 + 14,
+		c.sz.w, THEME_LABEL_TALL * 3 );
+
+	CMenuPicButton *btns[] = { &close };
+	LayoutButtonRow( btns, 1 );
+}
+
 void CMenuSaveLoad::CofMenuSavesChanged()
 {
 	// This is an opt-in feature, so make the change durable as soon as it is
@@ -99,8 +178,15 @@ void CMenuSaveLoad::CofMenuSavesChanged()
 
 void CMenuSaveLoad::UpdateCofMenuSavesLabel()
 {
-	snprintf( cofMenuSavesLabel, sizeof( cofMenuSavesLabel ), "%s %s",
-		cofMenuSaves.bChecked ? "[ON ]" : "[OFF]", L( "Enable pause-menu saves" ));
+	// the [ON ]/[OFF] marker was only needed because Cry of Fear ships no
+	// gfx/shell/cb_* artwork and the stock checkbox drew nothing at all; the
+	// theme draws the box from primitives
+	if( UI_ThemeActive( ))
+		Q_strncpy( cofMenuSavesLabel, L( "Enable pause-menu saves" ), sizeof( cofMenuSavesLabel ));
+	else
+		snprintf( cofMenuSavesLabel, sizeof( cofMenuSavesLabel ), "%s %s",
+			cofMenuSaves.bChecked ? "[ON ]" : "[OFF]", L( "Enable pause-menu saves" ));
+
 	cofMenuSaves.szName = cofMenuSavesLabel;
 }
 
diff --git a/3rdparty/mainui/menus/ServerBrowser.cpp b/3rdparty/mainui/menus/ServerBrowser.cpp
index f7644af..3f10d1c 100644
--- a/3rdparty/mainui/menus/ServerBrowser.cpp
+++ b/3rdparty/mainui/menus/ServerBrowser.cpp
@@ -1147,8 +1147,39 @@ CMenuServerBrowser::Init
 */
 void CMenuServerBrowser::_Init( void )
 {
-	AddItem( banner );
+	const bool theme = UI_ThemeActive();
 
+	if( theme )
+		SetPanel( "JOIN SERVER", 960, 620 );
+	else
+		AddItem( banner );
+
+	// In panel mode the buttons are laid out left to right in the order they
+	// are added, so the primary action has to be added last.
+	if( theme )
+	{
+		favorite = AddButton( L( "Favorite" ), nullptr, PC_FAVORITE, VoidCb( &CMenuServerBrowser::FavoriteServer ), 0, 'o' );
+		viewGameInfo = AddButton( L( "View game info" ), nullptr, PC_VIEW_GAME_INFO, VoidCb( &CMenuServerBrowser::ViewGameInfo ), QMF_GRAYED );
+
+		createGame = AddButton( L( "GameUI_GameMenu_CreateServer" ), NULL, PC_CREATE_GAME );
+		SET_EVENT_MULTI( createGame->onReleased,
+		{
+			if( ((CMenuServerBrowser*)pSelf->Parent())->m_bLanOnly )
+				EngFuncs::CvarSetValue( "public", 0.0f );
+			else EngFuncs::CvarSetValue( "public", 1.0f );
+
+			UI_CreateGame_Menu();
+		});
+
+		refresh = AddButton( L( "Refresh" ), nullptr, PC_REFRESH, VoidCb( &CMenuServerBrowser::RefreshList ) );
+		addServer = AddButton( L( "Add server" ), nullptr, PC_ADD_SERVER, VoidCb( &CMenuServerBrowser::ShowAddServerBox ));
+		AddButton( L( "Close" ), nullptr, PC_DONE, VoidCb( &CMenuServerBrowser::Hide ) );
+
+		joinGame = AddButton( L( "Join" ), nullptr, PC_JOIN_GAME, VoidCb( &CMenuServerBrowser::JoinGame ), QMF_GRAYED );
+		joinGame->onReleasedClActive = msgBox.MakeOpenEvent();
+	}
+	else
+	{
 	joinGame = AddButton( L( "Join game" ), nullptr, PC_JOIN_GAME, VoidCb( &CMenuServerBrowser::JoinGame ), QMF_GRAYED );
 	joinGame->onReleasedClActive = msgBox.MakeOpenEvent();
 
@@ -1168,6 +1199,7 @@ void CMenuServerBrowser::_Init( void )
 	addServer = AddButton( L( "Add server" ), nullptr, PC_ADD_SERVER, VoidCb( &CMenuServerBrowser::ShowAddServerBox ));
 
 	AddButton( L( "Done" ), nullptr, PC_DONE, VoidCb( &CMenuServerBrowser::Hide ) );
+	}
 
 	msgBox.SetMessage( L( "Join a network game will exit any current game, OK to exit?" ) );
 	msgBox.SetPositiveButton( L( "GameUI_OK" ), PC_OK );
@@ -1197,8 +1229,11 @@ void CMenuServerBrowser::_Init( void )
 	tabSwitch.eTextAlignment = QM_CENTER;
 	tabSwitch.bMouseToggle = false;
 	tabSwitch.bKeepToggleWidth = true;
-	tabSwitch.iSelectColor = uiInputFgColor;
-	tabSwitch.iFgTextColor = uiInputFgColor - 0x00151515; // bit darker
+	if( !UI_ThemeActive( ))
+	{
+		tabSwitch.iSelectColor = uiInputFgColor;
+		tabSwitch.iFgTextColor = uiInputFgColor - 0x00151515; // bit darker
+	}
 	tabSwitch.onChanged = VoidCb( &CMenuServerBrowser::OnTabSwitch );
 
 	// server.dll needs for reading savefiles or startup newgame
@@ -1356,6 +1391,38 @@ void CMenuServerBrowser::_VidInit()
 	refreshTime = uiStatic.realTime + 500; // delay before update 0.5 sec
 	refreshTime2 = uiStatic.realTime + 500;
 
+	if( IsPanel( ))
+	{
+		// tab strip under the title band, list below it, filter row above the
+		// button row
+		Rect c = ContentRect();
+		const int tabH = 30;
+		const int filterH = THEME_CTRL_H;
+		int listY = c.pt.y + ( m_bLanOnly ? 0 : tabH + 10 );
+		int filterY = c.pt.y + c.sz.h - filterH;
+
+		tabSwitch.SetRect( c.pt.x, c.pt.y, c.sz.w, tabH );
+		tabSwitch.SetVisibility( !m_bLanOnly );
+
+		gameList.SetRect( c.pt.x, listY, c.sz.w, filterY - listY - 12 );
+
+		int x = c.pt.x;
+
+		filterPing.SetCoord( x, filterY );
+		x += filterPing.size.w + 10;
+		filterEmpty.SetCoord( x, filterY );
+		x += filterEmpty.size.w + 10;
+		filterFull.SetCoord( x, filterY );
+		x += filterFull.size.w + 10;
+		filterMap.SetCoord( x, filterY );
+
+		filterPing.MenuClose( );
+		filterEmpty.MenuClose( );
+		filterFull.MenuClose( );
+		filterMap.MenuClose( );
+		return;
+	}
+
 	if( m_bLanOnly )
 	{
 		gameList.SetCoord( 360, 230 );
diff --git a/3rdparty/mainui/menus/Video.cpp b/3rdparty/mainui/menus/Video.cpp
index d5216cb..d868dbb 100644
--- a/3rdparty/mainui/menus/Video.cpp
+++ b/3rdparty/mainui/menus/Video.cpp
@@ -28,6 +28,25 @@ class CMenuVideo : public CMenuFramework
 {
 public:
 	CMenuVideo() : CMenuFramework( "CMenuVideo" ) { }
+
+	void _VidInit( void ) override
+	{
+		if( !IsPanel( ))
+			return;
+
+		for( int i = 0; i < 2; i++ )
+		{
+			Rect r = RowRect( i );
+
+			row[i].SetRect( r.pt.x, r.pt.y, r.sz.w, r.sz.h );
+		}
+
+		CMenuPicButton *btns[] = { &close };
+		LayoutButtonRow( btns, 1 );
+	}
+
+	CMenuPicButton row[2];
+	CMenuPicButton close;
 };
 
 ADD_MENU3( menu_video, CMenuVideo, UI_Video_Menu );
@@ -39,6 +58,16 @@ UI_Video_Menu
 */
 void UI_Video_Menu( void )
 {
+	// Feedback round: this hub was one screen with two rows on the way to two
+	// screens that between them fit on one. In theme mode Video modes and
+	// Video options are merged into a single page (menus/VideoModes.cpp) and
+	// Options > Video opens it directly, so the hub is skipped entirely.
+	if( UI_ThemeActive( ))
+	{
+		UI_VidModes_Menu();
+		return;
+	}
+
 	if( !menu_video->WasInit() )
 	{
 		menu_video->banner.SetPicture( ART_BANNER );
diff --git a/3rdparty/mainui/menus/VideoModes.cpp b/3rdparty/mainui/menus/VideoModes.cpp
index 28f1ef6..573446e 100644
--- a/3rdparty/mainui/menus/VideoModes.cpp
+++ b/3rdparty/mainui/menus/VideoModes.cpp
@@ -22,12 +22,16 @@ Foundation, Inc., 59 Temple Place - Suite 330, Boston, MA  02111-1307, USA.
 #include "MenuStrings.h"
 #include "Table.h"
 #include "CheckBox.h"
+#include "Slider.h"
 #include "YesNoMessageBox.h"
 #include "SpinControl.h"
 #include "utlvector.h"
 #include "StringArrayModel.h"
+#include "CryOfFear.h"
+#include "Theme.h"
 
 #define ART_BANNER		"gfx/shell/head_vidmodes"
+#define ART_GAMMA		"gfx/shell/gamma"
 
 // TODO:
 // - Get rid of vid mode strings, always compare the Width X Height of the window
@@ -91,15 +95,67 @@ private:
 	CUtlVector<refdll> m_refs;
 };
 
+/*
+=================
+CMenuReadoutSlider
+
+A themed slider that prints its own current value at the right end of the label
+line the base class already draws, because the two view sliders are the first
+controls on this page whose *number* is the point - "Gamma 0.62" means nothing,
+"Field of view 90" means everything.
+
+Only the readout is added; the track, the knob, the cursor mapping and the key
+handling are CMenuSlider's, untouched. Without the theme the class behaves
+exactly like its base (the readout is drawn only in theme mode), so the WON
+path is unaffected.
+
+szFollowText, when set, replaces the number at flFollowValue - that is how
+"Viewmodel FOV" spells its zero: the slider's lowest step is one below the
+useful range and reads "Follow FOV" instead of "54".
+=================
+*/
+class CMenuReadoutSlider : public CMenuSlider
+{
+public:
+	CMenuReadoutSlider() : CMenuSlider(), szFollowText( NULL ), flFollowValue( 0.0f ) { }
+
+	void Draw() override
+	{
+		CMenuSlider::Draw();
+
+		if( !UI_ThemeActive( ))
+			return;
+
+		char text[64];
+		float value = GetCurrentValue();
+
+		if( szFollowText && value <= flFollowValue )
+			Q_strncpy( text, szFollowText, sizeof( text ));
+		else
+			snprintf( text, sizeof( text ), "%i", (int)( value + 0.5f ));
+
+		// the base draws szName at exactly this baseline, left-aligned
+		UI_DrawString( font, m_scPos.x, m_scPos.y - (int)( m_scChSize * 1.5f ),
+			m_scSize.w, m_scChSize, text,
+			FBitSet( iFlags, QMF_GRAYED ) ? THEME_DISABLED : THEME_TEXT, m_scChSize,
+			QM_TOPRIGHT, ETF_FORCECOL | ETF_NO_WRAP );
+	}
+
+	const char *szFollowText;	// drawn instead of the number at flFollowValue
+	float       flFollowValue;
+};
+
 class CMenuVidModes : public CMenuFramework
 {
 private:
 	void _Init() override;
+	void _VidInit() override;
 	void Reload() override;
 	void Draw() override; // put test mode timer here
 
 public:
-	CMenuVidModes() : CMenuFramework( "CMenuVidModes" ), restartMsgBox( true ), testModeTimer( 0 ) { }
+	CMenuVidModes() : CMenuFramework( "CMenuVidModes" ), restartMsgBox( true ),
+		hTestImage( 0 ), testModeTimer( 0 ) { }
 
 	void SetMode( int mode );
 	void SetMode( int w, int h );
@@ -126,6 +182,46 @@ public:
 	CMenuSpinControl renderers;
 	CMenuSpinControl windowMode;
 
+	// ------------------------------------------------------------------
+	// theme mode only: the former "Video options" page, merged in so there
+	// is one Video page instead of a hub with two children. The upstream
+	// CMenuVidOptions still exists and still owns the WON path.
+	// ------------------------------------------------------------------
+	void InitVideoOptions();
+	void LayoutVideoOptions( const Rect &col );
+	void ReloadVideoOptions();
+	void WriteVideoOptions();
+	void UpdateGamma();
+	void GetGamma();
+
+	// Cry of Fear only: the two view sliders, bound to the engine's cof_fov
+	// (xash.dll, V_GetRefParams) and the renderer's cof_viewmodel_fov
+	// (ref_gl.dll, R_DrawViewModel). Both apply live, on drag.
+	void UpdateFov();
+	void GetFov();
+	void UpdateViewmodelFov();
+	void GetViewmodelFov();
+
+	CMenuReadoutSlider fov;
+	CMenuReadoutSlider viewmodelFov;
+
+	CMenuSlider	gammaIntensity;
+	CMenuSlider	brightness;
+	CMenuSlider	contrast;	// Cry of Fear only, gl_contrast
+	CMenuCheckBox	vbo;
+	CMenuCheckBox	swwater;
+	CMenuCheckBox	overbright;
+	CMenuCheckBox	filtering;
+	CMenuCheckBox	detailtex;
+	CMenuCheckBox	hudscale;
+	CMenuYesNoMessageBox hudScaleMsgBox;
+	HIMAGE		hTestImage;
+
+	// Cry of Fear renders through its own Paranoia OpenGL path and honours its
+	// own cvars, so several of the boxes above do nothing in this game and are
+	// not shown; see the options audit in docs/cof-ui-m3-theme.md.
+	bool		m_bCoF;
+
 	int prevMode;
 	int prevModeX;
 	int prevModeY;
@@ -214,6 +310,475 @@ bool CMenuVidModes::IsRendererChanged()
 	return stricmp( renderersModel.GetShortName( i ), EngFuncs::GetCvarString( "r_refdll_loaded" )) != 0;
 }
 
+/*
+=================
+CMenuVidModes, the merged video-options half
+
+Feedback round: "Video modes" and "Video options" were two children of a Video
+hub, which is three clicks for one screen. They are one page now - modes and
+the resolution list on the left, image settings on the right, one button row -
+and Options > Video opens it directly (menus/Video.cpp). Everything below is
+the CMenuVidOptions behaviour, unchanged in substance: the same cvars, the same
+hud_scale warning, the same per-renderer graying.
+=================
+*/
+/*
+=================
+The Cry of Fear image cvars
+
+Measured in the fixture (stage1/ui-m1-engine-fixture-20260921, the vq-* runs):
+with the same save, the same camera and the same frame count, "gamma" 1.8 -> 3.0
+and "brightness" 0 -> 3 change nothing at all, while the client's own
+"gl_gamma", "gl_brightness" and "gl_contrast" change most of the frame. That is
+what the binary says too: client.dll registers gl_gamma "1", gl_brightness "0"
+and gl_contrast "1" itself and applies them inside its Paranoia lightmap path,
+and it never reads the engine's gamma or brightness. It also clamps its own two:
+brightness above 0.3 and contrast above 2.0 are written straight back down (at
+client.dll VA 1005A195 and 1005A1C3), so those are the real ends of the
+sliders.
+
+So for this game the two sliders drive the cvars that work, a third is added for
+contrast because the client treats the three as one group, and the engine's
+gamma test image is not touched - it previews an engine-side transform the
+Paranoia path does not perform.
+=================
+*/
+#define COF_GAMMA_MIN      0.4f
+#define COF_GAMMA_MAX      2.0f
+#define COF_BRIGHTNESS_MIN 0.0f
+#define COF_BRIGHTNESS_MAX 0.3f
+#define COF_CONTRAST_MIN   0.2f
+#define COF_CONTRAST_MAX   2.0f
+
+/*
+=================
+The Cry of Fear view cvars
+
+Both are added by the engine side of the FOV round
+(stage1/fov-investigation-20260922/RESULTS.md, stage1/fov-impl-20260922):
+
+  cof_fov            xash.dll,   FCVAR_ARCHIVE,  default 90, 70..110.
+                     Remapped in V_GetRefParams on rvp->fov_x only, tapering
+                     out towards the cinematic zoom, so ironsights and scripted
+                     cameras keep their authored FOV. The value is the 4:3 base
+                     (Source's fov_desired convention); FWGS Hor+-corrects it,
+                     90 -> 106.26 degrees at 16:9.
+  cof_viewmodel_fov  ref_gl.dll, FCVAR_GLCONFIG, default 0, 55..90.
+                     0 means "follow the world FOV", which is the stock look.
+
+Nothing is registered here. The menu library has no cvar of its own for either:
+it reads them by name and writes them by name, the way every other page does.
+A build whose engine or renderer does not carry the patch is handled instead of
+misreported - EngFuncs::GetCvarString returns an empty string for a name the
+engine never registered, which is the one reliable existence test, and the
+slider then shows the documented default rather than a bogus 0. Writing to a
+name that does not exist is harmless: Cvar_Set logs "variable not found" and
+returns (engine/common/cvar.c).
+
+The sliders apply live, on drag, like the gamma group above them - there is no
+restart and no Apply step, because both cvars are read per frame by the code
+that uses them.
+=================
+*/
+#define COF_FOV_MIN        70
+#define COF_FOV_MAX        110
+#define COF_FOV_DEFAULT    90
+#define COF_VMFOV_MIN      55
+#define COF_VMFOV_MAX      90
+// one step below the useful range, drawn as "Follow FOV" and written as 0.
+// A slider cannot hold a gap, so the zero lives at the bottom of the track
+// instead of in the middle of it.
+#define COF_VMFOV_FOLLOW   ( COF_VMFOV_MIN - 1 )
+
+// Reads a cvar that a not-yet-patched engine or renderer may not have.
+static float UI_CoFViewCvar( const char *name, float defValue )
+{
+	const char *str = EngFuncs::GetCvarString( name );
+
+	if( !str || !str[0] )
+		return defValue;
+
+	return EngFuncs::GetCvarFloat( name );
+}
+
+void CMenuVidModes::GetFov()
+{
+	float value = UI_CoFViewCvar( "cof_fov", COF_FOV_DEFAULT );
+
+	// anything the slider cannot represent - the engine cvar's own 0 "off"
+	// value, or a hand-edited config - reads as the shipped default, which is
+	// the neutral setting the remap does nothing at.
+	if( value < COF_FOV_MIN || value > COF_FOV_MAX )
+		value = COF_FOV_DEFAULT;
+
+	fov.SetCurrentValue( value );
+	fov.SetOriginalValue( value );
+}
+
+void CMenuVidModes::UpdateFov()
+{
+	EngFuncs::CvarSetValue( "cof_fov", (float)(int)( fov.GetCurrentValue() + 0.5f ));
+}
+
+void CMenuVidModes::GetViewmodelFov()
+{
+	float value = UI_CoFViewCvar( "cof_viewmodel_fov", 0.0f );
+
+	// 0 (follow the world FOV) and anything out of range sit on the bottom step
+	if( value < COF_VMFOV_MIN || value > COF_VMFOV_MAX )
+		value = COF_VMFOV_FOLLOW;
+
+	viewmodelFov.SetCurrentValue( value );
+	viewmodelFov.SetOriginalValue( value );
+}
+
+void CMenuVidModes::UpdateViewmodelFov()
+{
+	float value = viewmodelFov.GetCurrentValue();
+
+	EngFuncs::CvarSetValue( "cof_viewmodel_fov",
+		value <= COF_VMFOV_FOLLOW ? 0.0f : (float)(int)( value + 0.5f ));
+}
+
+void CMenuVidModes::UpdateGamma()
+{
+	if( m_bCoF )
+	{
+		EngFuncs::CvarSetValue( "gl_gamma",
+			RemapVal( gammaIntensity.GetCurrentValue(), 0.0f, 1.0f, COF_GAMMA_MIN, COF_GAMMA_MAX ));
+		EngFuncs::CvarSetValue( "gl_brightness",
+			RemapVal( brightness.GetCurrentValue(), 0.0f, 1.0f, COF_BRIGHTNESS_MIN, COF_BRIGHTNESS_MAX ));
+		EngFuncs::CvarSetValue( "gl_contrast",
+			RemapVal( contrast.GetCurrentValue(), 0.0f, 1.0f, COF_CONTRAST_MIN, COF_CONTRAST_MAX ));
+		return;
+	}
+
+	float val1 = RemapVal( gammaIntensity.GetCurrentValue(), 0.0, 1.0, 1.8, 3.0 );
+	float val2 = RemapVal( brightness.GetCurrentValue(), 0.0, 1.0, 0.0, 3.0 );
+
+	EngFuncs::CvarSetValue( "gamma", val1 );
+	EngFuncs::CvarSetValue( "brightness", val2 );
+
+	if( hTestImage )
+		EngFuncs::ProcessImage( hTestImage, val1, val2 );
+}
+
+void CMenuVidModes::GetGamma()
+{
+	if( m_bCoF )
+	{
+		float g = EngFuncs::GetCvarFloat( "gl_gamma" );
+		float b = EngFuncs::GetCvarFloat( "gl_brightness" );
+		float c = EngFuncs::GetCvarFloat( "gl_contrast" );
+
+		gammaIntensity.SetCurrentValue( RemapVal( g, COF_GAMMA_MIN, COF_GAMMA_MAX, 0.0f, 1.0f ));
+		brightness.SetCurrentValue( RemapVal( b, COF_BRIGHTNESS_MIN, COF_BRIGHTNESS_MAX, 0.0f, 1.0f ));
+		contrast.SetCurrentValue( RemapVal( c, COF_CONTRAST_MIN, COF_CONTRAST_MAX, 0.0f, 1.0f ));
+
+		gammaIntensity.SetOriginalValue( g );
+		brightness.SetOriginalValue( b );
+		contrast.SetOriginalValue( c );
+		return;
+	}
+
+	float val1 = EngFuncs::GetCvarFloat( "gamma" );
+	float val2 = EngFuncs::GetCvarFloat( "brightness" );
+
+	gammaIntensity.SetCurrentValue( RemapVal( val1, 1.8f, 3.0f, 0.0f, 1.0f ));
+	brightness.SetCurrentValue( RemapVal( val2, 0.0f, 3.0f, 0.0f, 1.0f ));
+
+	if( hTestImage )
+		EngFuncs::ProcessImage( hTestImage, val1, val2 );
+
+	gammaIntensity.SetOriginalValue( val1 );
+	brightness.SetOriginalValue( val2 );
+}
+
+void CMenuVidModes::WriteVideoOptions()
+{
+	// gamma, brightness and contrast are written as they are dragged.
+	// A box that is not on the page for this game is not written either: it
+	// was never shown, so it has no value the user chose.
+	if( detailtex.IsVisible( ))  detailtex.WriteCvar();
+	if( vbo.IsVisible( ))        vbo.WriteCvar();
+	if( swwater.IsVisible( ))    swwater.WriteCvar();
+	if( overbright.IsVisible( )) overbright.WriteCvar();
+	if( filtering.IsVisible( ))  filtering.WriteCvar();
+	if( hudscale.IsVisible( ))   hudscale.WriteCvar();
+}
+
+void CMenuVidModes::InitVideoOptions()
+{
+	hTestImage = EngFuncs::PIC_Load( ART_GAMMA, PIC_KEEP_SOURCE | PIC_EXPAND_SOURCE );
+
+	gammaIntensity.szName = L( "GameUI_Gamma" );
+	gammaIntensity.Setup( 0.0, 1.0, 0.025 );
+	gammaIntensity.onChanged = VoidCb( &CMenuVidModes::UpdateGamma );
+	gammaIntensity.onCvarGet = VoidCb( &CMenuVidModes::GetGamma );
+
+	brightness.szName = L( "GameUI_Brightness" );
+	brightness.Setup( 0, 1.0, 0.025 );
+	brightness.onChanged = VoidCb( &CMenuVidModes::UpdateGamma );
+	brightness.onCvarGet = VoidCb( &CMenuVidModes::GetGamma );
+
+	contrast.szName = L( "Contrast" );
+	contrast.Setup( 0, 1.0, 0.025 );
+	contrast.onChanged = VoidCb( &CMenuVidModes::UpdateGamma );
+	contrast.onCvarGet = VoidCb( &CMenuVidModes::GetGamma );
+
+	fov.szName = L( "Field of view" );
+	fov.Setup( COF_FOV_MIN, COF_FOV_MAX, 1.0f );
+	fov.onChanged = VoidCb( &CMenuVidModes::UpdateFov );
+	fov.onCvarGet = VoidCb( &CMenuVidModes::GetFov );
+	fov.szStatusText = L( "The 4:3 base field of view; 90 is Cry of Fear's own. Ironsights and scripted cameras are not rescaled." );
+
+	viewmodelFov.szName = L( "Viewmodel FOV" );
+	viewmodelFov.Setup( COF_VMFOV_FOLLOW, COF_VMFOV_MAX, 1.0f );
+	viewmodelFov.szFollowText = L( "Follow FOV" );
+	viewmodelFov.flFollowValue = COF_VMFOV_FOLLOW;
+	viewmodelFov.onChanged = VoidCb( &CMenuVidModes::UpdateViewmodelFov );
+	viewmodelFov.onCvarGet = VoidCb( &CMenuVidModes::GetViewmodelFov );
+	viewmodelFov.szStatusText = L( "Field of view of the held weapon only. At the lowest step it follows the world field of view, which is the stock look." );
+
+	detailtex.szName = L( "Detail textures" );
+	vbo.szName = L( "Use VBO" );
+	swwater.szName = L( "Water ripples" );
+	overbright.szName = L( "Overbrights" );
+	filtering.szName = L( "Texture filtering" );
+	hudscale.szName = L( "Auto scale HUD" );
+
+	hudScaleMsgBox.SetMessage( L( "^1WARNING: This is an experimental option and turning it on might break mods!^7\n\nReload the game or reconnect to the server to apply the settings."));
+	hudScaleMsgBox.onNegative.pExtra = &hudscale;
+	SET_EVENT_MULTI( hudScaleMsgBox.onNegative,
+	{
+		CMenuCheckBox *cb = (CMenuCheckBox *)pExtra;
+
+		cb->bChecked = false;
+		cb->SetCvarValue( 0.0f );
+	});
+	hudScaleMsgBox.Link( this );
+
+	SET_EVENT_MULTI( hudscale.onChanged,
+	{
+		CMenuVidModes *parent = (CMenuVidModes *)pSelf->Parent();
+
+		if( EngFuncs::ClientInGame( ))
+			parent->hudScaleMsgBox.Show();
+	});
+
+	SET_EVENT_MULTI( hudscale.onCvarWrite,
+	{
+		CMenuCheckBox *cb = (CMenuCheckBox *)pSelf;
+
+		// scale the HUD as if the screen were 1024x768, keeping the aspect
+		if( cb->bChecked )
+			EngFuncs::CvarSetValue( cb->CvarName(), 1024.0f );
+		else
+			EngFuncs::CvarSetValue( cb->CvarName(), 0.0f );
+	});
+
+	SET_EVENT_MULTI( hudscale.onCvarGet,
+	{
+		CMenuCheckBox *cb = (CMenuCheckBox *)pSelf;
+
+		cb->bChecked = EngFuncs::GetCvarFloat( cb->CvarName() ) >= 640.0f;
+	});
+
+	AddItem( gammaIntensity );
+	AddItem( brightness );
+	AddItem( contrast );
+	AddItem( detailtex );
+	AddItem( vbo );
+	AddItem( swwater );
+	AddItem( overbright );
+	AddItem( filtering );
+	AddItem( hudscale );
+	// last, so the keyboard cursor reaches them in the order they are drawn
+	AddItem( fov );
+	AddItem( viewmodelFov );
+
+	// Options audit, 2026-09-22 (docs/cof-ui-m3-theme.md). Every one of these
+	// was measured in the fixture: same save, same camera, same frame count,
+	// the cvar at two extremes, the two screenshots compared pixel by pixel on
+	// a surface whose same-settings noise floor is exactly zero. What Cry of
+	// Fear does not use is taken off its page rather than left there doing
+	// nothing.
+	//   r_detailtextures  0 -> 1   at the floor, and the game ships no
+	//                             maps/<map>_detail.txt at all; its own detail
+	//                             textures are the client's gl_detailtex,
+	//                             which measured at the floor too. Removed,
+	//                             and defaulted to 0 in UI_ThemeApplyDeferredDefaults.
+	//   gl_overbright     0 -> 1   at the floor: Cry of Fear loads its own
+	//                             "additional lightmaps" in the Paranoia path
+	//                             and lights the world there, so the engine's
+	//                             overbright never reaches what you see.
+	//                             Removed.
+	// Kept, measured well above the floor: gl_texture_nearest (20%) and
+	// hud_scale (50%). Kept without a verdict: gl_vbo (a performance option,
+	// not a visual one) and r_ripple (the test scene has no water in frame).
+	contrast.SetVisibility( m_bCoF );
+	detailtex.SetVisibility( !m_bCoF );
+	overbright.SetVisibility( !m_bCoF );
+	// hud_scale is removed for Cry of Fear: the client builds its own
+	// renderer's viewport and projection out of the same screen size the
+	// engine reports for the HUD, so a virtual screen zooms and shifts the 3D
+	// view (measured, docs/cof-ui-m4-scaling.md). The engine ignores the cvar
+	// for this game entirely; the supported control is HUD scale on the Game
+	// options page, which drives cof_ui_scale / cof_ui_scale_user instead.
+	hudscale.SetVisibility( !m_bCoF );
+
+	// The two view sliders are Cry of Fear's: cof_fov is remapped by the
+	// engine only for this game's client, and cof_viewmodel_fov overrides a
+	// projection the stock renderer has no reason to change. They sit under
+	// the image settings in the right column rather than in a column of their
+	// own - the page is 940x620 with 494 units of content height, the image
+	// block ends at 318, and two more sliders fit in the remainder with room
+	// to spare at every resolution down to 1280x720.
+	fov.SetVisibility( m_bCoF );
+	viewmodelFov.SetVisibility( m_bCoF );
+
+	gammaIntensity.LinkCvar( m_bCoF ? "gl_gamma" : "gamma" );
+	brightness.LinkCvar( m_bCoF ? "gl_brightness" : "brightness" );
+	contrast.LinkCvar( "gl_contrast" );
+	detailtex.LinkCvar( "r_detailtextures" );
+	swwater.LinkCvar( "r_ripple" );
+	vbo.LinkCvar( "gl_vbo" );
+	overbright.LinkCvar( "gl_overbright" );
+	// filtering is linked in ReloadVideoOptions: the cvar name differs between
+	// ref_gl and ref_soft
+	hudscale.LinkCvar( "hud_scale" );
+
+	// LinkCvar runs onCvarGet, which is where the missing-cvar fallback lives
+	fov.LinkCvar( "cof_fov" );
+	viewmodelFov.LinkCvar( "cof_viewmodel_fov" );
+}
+
+void CMenuVidModes::LayoutVideoOptions( const Rect &col )
+{
+	int y = col.pt.y + THEME_LABEL_TALL + 8;
+
+	CMenuSlider *sliders[] = { &gammaIntensity, &brightness, &contrast };
+
+	for( int i = 0; i < 3; i++ )
+	{
+		if( !sliders[i]->IsVisible( ))
+			continue;
+
+		sliders[i]->SetRect( col.pt.x, y, col.sz.w, THEME_SLIDER_H );
+		y += THEME_CTRL_PITCH;
+	}
+
+	y += 6;
+
+	CMenuCheckBox *boxes[] = { &detailtex, &vbo, &swwater, &overbright, &filtering, &hudscale };
+
+	// the removed boxes leave no gap: only what is on the page is placed
+	for( int i = 0; i < 6; i++ )
+	{
+		if( !boxes[i]->IsVisible( ))
+			continue;
+
+		boxes[i]->SetCoord( col.pt.x, y );
+		y += THEME_ROW_PITCH;
+	}
+
+	// The view sliders close the column. The first one pays for its own label
+	// line (THEME_CTRL_PITCH only covers the gap *between* stacked sliders),
+	// plus a visual break from the checkbox block above it.
+	CMenuSlider *view[] = { &fov, &viewmodelFov };
+	bool firstView = true;
+
+	for( int i = 0; i < 2; i++ )
+	{
+		if( !view[i]->IsVisible( ))
+			continue;
+
+		if( firstView )
+		{
+			y += 14 + THEME_LABEL_TALL + 8;
+			firstView = false;
+		}
+
+		view[i]->SetRect( col.pt.x, y, col.sz.w, THEME_SLIDER_H );
+		y += THEME_CTRL_PITCH;
+	}
+}
+
+void CMenuVidModes::ReloadVideoOptions()
+{
+	bool gl_active = !strnicmp( EngFuncs::GetCvarString( "r_refdll_loaded" ), "gl", 2 );
+	bool soft_active = !stricmp( EngFuncs::GetCvarString( "r_refdll_loaded" ), "soft" );
+
+	detailtex.SetGrayed( !gl_active );
+	detailtex.SetInactive( !gl_active );
+
+	vbo.SetGrayed( !gl_active );
+	vbo.SetInactive( !gl_active );
+
+	if( gl_active )
+	{
+		if( EngFuncs::textfuncs.pfnIsCvarReadOnly( "gl_vbo" ) > 0 )
+		{
+			SET_EVENT_MULTI( vbo.onCvarChange,
+			{
+				CMenuCheckBox *cb = (CMenuCheckBox *)pSelf;
+				cb->bChecked = false;
+				UI_ShowMessageBox( L( "Not supported on your GPU" ));
+			});
+
+			vbo.onCvarWrite = CEventCallback::NoopCb;
+		}
+		else
+		{
+			vbo.onCvarWrite.Reset();
+			vbo.onCvarChange.Reset();
+		}
+	}
+
+	swwater.SetGrayed( !gl_active );
+	swwater.SetInactive( !gl_active );
+
+	overbright.SetGrayed( !gl_active );
+	overbright.SetInactive( !gl_active );
+
+	if( soft_active || gl_active )
+	{
+		filtering.SetGrayed( false );
+		filtering.SetInactive( false );
+
+		if( soft_active )
+		{
+			// no need to invert, 0 disables filtering
+			filtering.LinkCvar( "sw_texfilt" );
+		}
+		else
+		{
+			SET_EVENT_MULTI( filtering.onCvarGet,
+			{
+				CMenuCheckBox *cb = (CMenuCheckBox *)pSelf;
+
+				if( EngFuncs::GetCvarFloat( cb->CvarName( )))
+					cb->bChecked = false;
+				else cb->bChecked = true;
+			});
+			SET_EVENT_MULTI( filtering.onCvarWrite,
+			{
+				CMenuCheckBox *cb = (CMenuCheckBox *)pSelf;
+				if( cb->bChecked )
+					EngFuncs::CvarSetValue( cb->CvarName(), 0.0f );
+				else EngFuncs::CvarSetValue( cb->CvarName(), 1.0f );
+			});
+
+			filtering.LinkCvar( "gl_texture_nearest" );
+		}
+	}
+	else
+	{
+		filtering.SetGrayed( true );
+		filtering.SetInactive( true );
+	}
+}
+
 void CMenuVidModes::GetConfig()
 {
 	float fullscreen = EngFuncs::GetCvarFloat( "fullscreen" );
@@ -280,6 +845,10 @@ void CMenuVidModes::SetConfig( )
 	vsync.WriteCvar();
 	renderers.WriteCvar();
 
+	// the image settings share this page's one Apply row in theme mode
+	if( UI_ThemeActive( ))
+		WriteVideoOptions();
+
 	if( isWindowedModeChanged )
 		EngFuncs::CvarSetValue( "fullscreen", currentWindowModeIndex );
 
@@ -367,8 +936,15 @@ void CMenuVidModes::_Init( void )
 	};
 	static CStringArrayModel windowModeModel( windowModeStr, V_ARRAYSIZE( windowModeStr ));
 
+	m_bCoF = UI_ThemeActive() && UI_IsCryOfFear();
+
 	banner.SetPicture( ART_BANNER );
 
+	// one Video page in theme mode: modes on the left, image settings on the
+	// right. Without the theme this is still only the Video modes dialog.
+	if( UI_ThemeActive( ))
+		SetPanel( "VIDEO", 940, 620 );
+
 	vidList.SetRect( 360, 230, -20, 365 );
 	vidList.SetupColumn( 0, L( "GameUI_Resolution" ), 1.0f );
 	vidList.SetModel( &vidListModel );
@@ -406,20 +982,61 @@ void CMenuVidModes::_Init( void )
 		parent->vidList.SetInactive( parent->windowMode.GetCurrentValue( ) == 2 );
 	});
 
-	AddItem( banner );
-	AddButton( L( "GameUI_Apply" ), nullptr, PC_OK, VoidCb( &CMenuVidModes::SetConfig ) );
-	AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuVidModes::Hide ) );
+	if( !UI_ThemeActive( ))
+		AddItem( banner );
+
+	// secondary first, so the button row reads Cancel then Apply
+	if( UI_ThemeActive( ))
+	{
+		AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuVidModes::Hide ) );
+		AddButton( L( "GameUI_Apply" ), nullptr, PC_OK, VoidCb( &CMenuVidModes::SetConfig ) );
+	}
+	else
+	{
+		AddButton( L( "GameUI_Apply" ), nullptr, PC_OK, VoidCb( &CMenuVidModes::SetConfig ) );
+		AddButton( L( "GameUI_Cancel" ), nullptr, PC_CANCEL, VoidCb( &CMenuVidModes::Hide ) );
+	}
+
 	AddItem( renderers );
 	AddItem( windowMode );
 	AddItem( vsync );
 	AddItem( vidList );
 
+	if( UI_ThemeActive( ))
+		InitVideoOptions();
+
 	renderers.LinkCvar( "r_refdll", CMenuEditable::CVAR_STRING );
 }
 
+void CMenuVidModes::_VidInit()
+{
+	if( !IsPanel( ))
+		return;
+
+	Rect left = ColumnRect( 0, 2 );
+	Rect right = ColumnRect( 1, 2 );
+	int y = left.pt.y + THEME_LABEL_TALL + 8;
+
+	renderers.SetRect( left.pt.x, y, left.sz.w, THEME_CTRL_H );
+	y += THEME_CTRL_PITCH;
+	windowMode.SetRect( left.pt.x, y, left.sz.w, THEME_CTRL_H );
+	y += THEME_CTRL_PITCH;
+	vsync.SetCoord( left.pt.x, y );
+	y += THEME_ROW_PITCH + 8;
+
+	// the mode list takes the rest of the left column
+	vidList.SetRect( left.pt.x, y, left.sz.w, left.pt.y + left.sz.h - y );
+
+	LayoutVideoOptions( right );
+}
+
 void CMenuVidModes::Reload()
 {
 	GetConfig();
+
+	if( UI_ThemeActive( ))
+		ReloadVideoOptions();
+
 	CMenuFramework::Reload();
 }
 
diff --git a/3rdparty/mainui/menus/VideoOptions.cpp b/3rdparty/mainui/menus/VideoOptions.cpp
index 69dd50a..b6ca2bd 100644
--- a/3rdparty/mainui/menus/VideoOptions.cpp
+++ b/3rdparty/mainui/menus/VideoOptions.cpp
@@ -118,7 +118,7 @@ CMenuVidOptions::Ownerdraw
 */
 void CMenuVidOptions::CMenuVidPreview::Draw( )
 {
-	int		color = 0xFFFF0000; // 255, 0, 0, 255
+	int		color = UI_ThemeActive() ? THEME_INPUT_BORDER : 0xFFFF0000; // 255, 0, 0, 255
 	int		viewport[4];
 	int		viewsize, size, sb_lines;
 
@@ -165,6 +165,12 @@ void CMenuVidOptions::_Init( void )
 
 	banner.SetPicture( ART_BANNER );
 
+	if( UI_ThemeActive( ))
+	{
+		SetPanel( "VIDEO OPTIONS", 860, 560 );
+		done.szName = L( "GameUI_OK" );
+	}
+
 	testImage.iFlags = QMF_INACTIVE;
 	testImage.SetRect( 390, 225, 480, 450 );
 	testImage.SetPicture( ART_GAMMA );
@@ -304,6 +310,43 @@ void CMenuVidOptions::_VidInit()
 {
 	outlineWidth = 2;
 	UI_ScaleCoords( NULL, NULL, &outlineWidth, NULL );
+
+	if( !IsPanel( ))
+		return;
+
+	// left column: the two sliders, then the checkbox block
+	// right column: the gamma test image
+	Rect left = ColumnRect( 0, 2 );
+	Rect right = ColumnRect( 1, 2 );
+	int y = left.pt.y + THEME_LABEL_TALL + 8;
+
+#if LEGACY_VIEWSIZE
+	screenSize.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+	y += THEME_CTRL_PITCH;
+#endif
+	gammaIntensity.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+	y += THEME_CTRL_PITCH;
+	brightness.SetRect( left.pt.x, y, left.sz.w, THEME_SLIDER_H );
+	y += THEME_CTRL_PITCH + 6;
+
+	CMenuCheckBox *boxes[] = { &detailtex, &vbo, &swwater, &overbright, &filtering, &hudscale };
+
+	for( int i = 0; i < 6; i++ )
+	{
+		boxes[i]->SetCoord( left.pt.x, y );
+		y += THEME_ROW_PITCH;
+	}
+
+	// keep the preview on a 4:3 box inside the right column. Cry of Fear ships
+	// no gfx/shell/gamma, so an empty framed box would be the only thing in the
+	// column - hide it instead and let the left column keep the whole panel.
+	if( EngFuncs::PIC_Width( hTestImage ) > 0 )
+		testImage.SetRect( right.pt.x, right.pt.y, right.sz.w, right.sz.w * 3 / 4 );
+	else
+		testImage.SetVisibility( false );
+
+	CMenuPicButton *btns[] = { &done };
+	LayoutButtonRow( btns, 1 );
 }
 
 void CMenuVidOptions::Reload()
diff --git a/3rdparty/mainui/model/BaseModel.h b/3rdparty/mainui/model/BaseModel.h
index 5c58341..83d365a 100644
--- a/3rdparty/mainui/model/BaseModel.h
+++ b/3rdparty/mainui/model/BaseModel.h
@@ -46,6 +46,13 @@ public:
 	virtual bool GetLineColor( int line, unsigned int &fillColor, bool &force ) const { return false; }
 	virtual bool GetCellColors( int line, int column, unsigned int &textColor, bool &force ) const { return false; }
 	virtual bool IsCellTextWrapped( int line, int column ) { return true; }
+
+	// A section boundary rather than a row of data. In theme mode CMenuTable
+	// draws these as a small uppercase caption with a hairline under it, does
+	// not fill or select them, and keyboard navigation steps over them.
+	// GoldSrc list files mark sections with a row of '=' characters
+	// (gfx/shell/kb_act.lst); those rows are the ones this hides.
+	virtual bool IsLineSectionHeader( int line ) const { return false; }
 	// virtual CMenuBaseItem *GetCellItem( int line, int column ) { return NULL; }
 
 	// sorting
diff --git a/3rdparty/mainui/model/KbActListModel.h b/3rdparty/mainui/model/KbActListModel.h
index 802936c..c35e486 100644
--- a/3rdparty/mainui/model/KbActListModel.h
+++ b/3rdparty/mainui/model/KbActListModel.h
@@ -18,8 +18,10 @@ GNU General Public License for more details.
 
 #include "BaseModel.h"
 #include "Utils.h"
+#include "Theme.h"
 #include "enginecallback_menu.h"
 #include "utlvector.h"
+#include <ctype.h>
 
 // Shared model over gfx/shell/kb_act.lst, the list of game actions used
 // for the keyboard configuration screen and (read-only) the touch button
@@ -44,6 +46,7 @@ public:
 		char bind[64];     // empty when entry is a separator
 		char first[20];    // formatted first bound key  (VIEW_BINDINGS only)
 		char second[20];   // formatted second bound key (VIEW_BINDINGS only)
+		bool header;       // a section caption (theme mode, see CollapseSections)
 	};
 
 	void Update() override
@@ -96,6 +99,72 @@ public:
 		}
 
 		EngFuncs::COM_FreeFile( afile );
+
+		if( UI_ThemeActive( ))
+			CollapseSections();
+	}
+
+	/*
+	=================
+	CollapseSections
+
+	gfx/shell/kb_act.lst marks a section the GoldSrc way:
+
+	    "blank" "=========================="
+	    "blank" "Controls - Movement"
+	    "blank" "=========================="
+
+	The WON list simply drew all three, which in a themed table reads as two
+	rows of noise around the caption. Drop every rule-of-equals row and turn
+	what is left of each separator into a real section caption, uppercased and
+	free of colour codes; CMenuTable draws those as a small dim label with a
+	hairline under it and skips them when navigating.
+	=================
+	*/
+	void CollapseSections()
+	{
+		for( int i = entries.Count() - 1; i >= 0; i-- )
+		{
+			if( entries[i].bind[0] )
+				continue; // a real binding row
+
+			if( IsRule( entries[i].display ))
+			{
+				entries.Remove( i );
+				continue;
+			}
+
+			entries[i].header = true;
+			StripAndUpper( entries[i].display, sizeof( entries[i].display ));
+		}
+	}
+
+	// true for a label that is nothing but '=' (and colour codes)
+	static bool IsRule( const char *s )
+	{
+		bool any = false;
+
+		for( ; *s; s++ )
+		{
+			if( IsColorString( s )) { s++; continue; }
+			if( *s == '=' ) { any = true; continue; }
+			return false;
+		}
+		return any;
+	}
+
+	static void StripAndUpper( char *s, size_t size )
+	{
+		char tmp[96];
+		size_t n = 0;
+
+		for( const char *p = s; *p && n < sizeof( tmp ) - 1; p++ )
+		{
+			if( IsColorString( p )) { p++; continue; }
+			tmp[n++] = (char)toupper( (unsigned char)*p );
+		}
+		tmp[n] = 0;
+		Q_strncpy( s, tmp, size );
 	}
 
 	int GetColumns() const override { return m_view == VIEW_BINDINGS ? 3 : 2; }
@@ -126,6 +195,11 @@ public:
 		return entries.IsValidIndex( line ) && entries[line].bind[0] != 0;
 	}
 
+	bool IsLineSectionHeader( int line ) const override
+	{
+		return entries.IsValidIndex( line ) && entries[line].header;
+	}
+
 	// Walk all keys, return up to two bound to `command` (in input order, then swapped).
 	static void LookupBoundKeys( const char *command, int twoKeys[2] )
 	{
